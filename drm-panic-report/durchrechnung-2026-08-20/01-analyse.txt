{
  "befunde": [
    {
      "frage": "(a) Unter welchen Sperren laeuft nv50_wndw_prepare_fb? Was haelt der Aufrufer drm_atomic_helper_prepare_planes()?",
      "antwort": "Der einzige .atomic_commit von nv50 ist nv50_disp_atomic_commit (disp.c:2659, Funktion ab :2427); dort wird drm_atomic_helper_prepare_planes an disp.c:2446 gerufen. Der Helper selbst nimmt keine einzige Sperre: er iteriert nur ueber die neuen Plane-States und ruft funcs->prepare_fb (drm_atomic_helper.c:2831-2840). Gehalten sind zu diesem Zeitpunkt genau zwei Dinge: (1) die drm_modeset_lock-ww-Mutexe des Acquire-Kontexts, den der Atomic-Ioctl anlegt (drm_atomic_uapi.c:1663 drm_modeset_acquire_init, Commit an :1760), (2) eine Runtime-PM-Referenz (disp.c:2432 pm_runtime_get_sync). Es ist kein dma_resv gehalten, keine TTM-Reservierung, nicht drm->ttm.io_reserve_mutex, und der Kontext ist normaler, schlaffaehiger Prozesskontext (interruptible ww-Locks, pm_runtime_get_sync schlaeft).",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/disp.c:2659 \".atomic_commit = nv50_disp_atomic_commit,\"; :2427-2446 \"nv50_disp_atomic_commit(...) { ret = pm_runtime_get_sync(dev->dev); ... ret = drm_atomic_helper_prepare_planes(dev, state);\"; drivers/gpu/drm/drm_atomic_helper.c:2814-2840 \"int drm_atomic_helper_prepare_planes(...) { ... for_each_new_plane_in_state(state, plane, new_plane_state, i) { ... if (funcs->prepare_fb) { ret = funcs->prepare_fb(plane, new_plane_state);\" (keine Lock-Anweisung im ganzen Rumpf); drivers/gpu/drm/drm_atomic_uapi.c:1663 \"drm_modeset_acquire_init(&ctx, DRM_MODESET_ACQUIRE_INTERRUPTIBLE);\", :1760 \"ret = drm_atomic_commit(state);\"",
      "folge_fuer_den_patch": "In prepare_fb darf man schlafen und darf ttm_bo_reserve nehmen. Alles, was der Panic-Callback spaeter nur noch lesen soll, gehoert genau hierher.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a) Ist das BO beim Eintritt in prepare_fb bereits per ttm_bo_reserve/dma_resv reserviert?",
      "antwort": "Nein. Drei unabhaengige Belege. Erstens reserviert drm_atomic_helper_prepare_planes nichts (siehe oben). Zweitens ruft nv50_wndw_prepare_fb selbst nouveau_bo_pin (wndw.c:554), und nouveau_bo_pin beginnt mit ttm_bo_reserve(bo, false, false, NULL) (nouveau_bo.c:639-643); ttm_bo_reserve landet fuer no_wait=false bei dma_resv_lock (ttm_bo.h:306), einem ww_mutex ohne Ticket, also nicht rekursiv. Waere das BO beim Eintritt reserviert, wuerde jeder einzelne Atomic-Commit sofort im Selbst-Deadlock stehen. Drittens benutzt drm_gem_plane_helper_prepare_fb (aus wndw.c:579 gerufen) den RCU-Lesepfad dma_resv_get_singleton (drm_gem_atomic_helper.c:149) und nicht dma_resv_lock, was ebenfalls nur ohne gehaltene Reservierung Sinn ergibt.",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:554 \"ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);\"; drivers/gpu/drm/nouveau/nouveau_bo.c:639-651 \"int nouveau_bo_pin(...) { ret = ttm_bo_reserve(bo, false, false, NULL); ... ret = nouveau_bo_pin_locked(nvbo, domain, contig); ttm_bo_unreserve(bo); return ret; }\"; include/drm/ttm/ttm_bo.h:303-306 \"if (interruptible) ret = dma_resv_lock_interruptible(...); else ret = dma_resv_lock(bo->base.resv, ticket);\"; drivers/gpu/drm/drm_gem_atomic_helper.c:149 \"ret = dma_resv_get_singleton(obj->resv, usage, &new);\"",
      "folge_fuer_den_patch": "nouveau_bo_map() darf aus prepare_fb gerufen werden, so wie nouveau_bo_pin() dort heute schon gerufen wird. Kein Sonderfall noetig.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Gibt es eine Verklemmung oder ein doppeltes Reservieren, wenn nouveau_bo_map() aus prepare_fb gerufen wird?",
      "antwort": "Nein. nouveau_bo_pin gibt die Reservierung vor der Rueckkehr wieder frei (nouveau_bo.c:649 ttm_bo_unreserve). Ein anschliessendes nouveau_bo_map (:668-680) ist ein zweiter, unabhaengiger reserve/kmap/unreserve-Zyklus, keine Rekursion. Genau diese Reihenfolge ist in nouveau bereits etabliert: nouveau_bo_new_map (:437-458) macht nouveau_bo_new_pin gefolgt von nouveau_bo_map, und dispnv04 ruft nouveau_bo_map fuer den Cursor aus KMS-Kontext (dispnv04/crtc.c:1003, dispnv04/disp.c:150). Auch die Sperrklassen sind unveraendert: prepare_fb nimmt heute schon einen dma_resv-ww_mutex innerhalb der gehaltenen drm_modeset-ww_mutexe, ein zusaetzliches nouveau_bo_map fuegt keine neue Klasse und keine neue Reihenfolge hinzu.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:668-680 \"nouveau_bo_map(struct nouveau_bo *nvbo) { ret = ttm_bo_reserve(&nvbo->bo, false, false, NULL); ... ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ttm_bo_unreserve(&nvbo->bo); return ret; }\"; :437-458 \"nouveau_bo_new_map(...) { ret = nouveau_bo_new_pin(...); ... ret = nouveau_bo_map(nvbo);\"; drivers/gpu/drm/nouveau/dispnv04/crtc.c:1003 \"ret = nouveau_bo_map(cursor);\"",
      "folge_fuer_den_patch": "Der Ansatz (a) aus der Aufgabenstellung ist sperrtechnisch tragfaehig. nouveau_bo_map() nach nouveau_bo_pin() in prepare_fb ist zulaessig.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Was ist der Unterschied zwischen nouveau_bo_pin() und nouveau_bo_pin_locked(), und was sagt er ueber den Sperrzustand in prepare_fb?",
      "antwort": "nouveau_bo_pin_locked (nouveau_bo.c:544) beginnt mit dma_resv_assert_held(bo->base.resv) (:549) und ist ausdruecklich fuer Aufrufer, die die Reservierung schon halten. Der einzige Aufrufer ausserhalb von nouveau_bo.c ist nouveau_gem_prime_pin (nouveau_prime.c:87-98), und der wird von drm_gem_map_attach zwischen dma_resv_lock und dma_resv_unlock gerufen (drm_prime.c:618-622). nouveau_bo_pin (:639) ist die Variante fuer Aufrufer ohne Reservierung. prepare_fb benutzt die zweite. Die Aufteilung ist also selbst der Beweis dafuer, dass in prepare_fb nichts gehalten wird. Dasselbe Paar existiert fuer unpin (:617 _locked mit assert, :653 ohne).",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:544-549 \"int nouveau_bo_pin_locked(...) { ... dma_resv_assert_held(bo->base.resv);\"; :617-621 \"void nouveau_bo_unpin_locked(...) { ... dma_resv_assert_held(bo->base.resv);\"; drivers/gpu/drm/nouveau/nouveau_prime.c:87-98 \"int nouveau_gem_prime_pin(struct drm_gem_object *obj) { ... ret = nouveau_bo_pin_locked(nvbo, NOUVEAU_GEM_DOMAIN_GART, false);\"; drivers/gpu/drm/drm_prime.c:618-622 \"ret = dma_resv_lock(obj->resv, NULL); ... ret = obj->funcs->pin(obj); dma_resv_unlock(obj->resv);\"",
      "folge_fuer_den_patch": "Wenn man Pin und Map in prepare_fb in einem Zug erledigen will, ist der saubere Bauplan: ttm_bo_reserve, nouveau_bo_pin_locked, ttm_bo_kmap, ttm_bo_unreserve, also ein neuer Helfer nouveau_bo_pin_map() nach dem Muster von nouveau_bo_pin(). Das vermeidet zwei Reservierungszyklen und schliesst das Fenster zwischen ihnen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Braucht ttm_bo_kunmap() eine Reservierung? Und was bedeutet das fuer cleanup_fb?",
      "antwort": "Formal nein. ttm_bo_kunmap (ttm_bo_util.c:459-484) enthaelt kein dma_resv_assert_held; es macht nur iounmap/vunmap/kunmap und danach ttm_mem_io_free (:478). Entsprechend nimmt nouveau_bo_unmap (nouveau_bo.c:682-689) keine Reservierung, und nouveau_bo_unpin_del (:404-421) ruft erst unmap, dann unpin, also unmap explizit ohne Reservierung. Ein nouveau_bo_unmap() in nv50_wndw_cleanup_fb waere also nicht formal falsch. Beachtenswert: ttm_mem_io_free ruft nouveau_ttm_io_mem_free (nouveau_bo.c:1364-1371), das drm->ttm.io_reserve_mutex nimmt, eine schlafende Mutex. cleanup_fb laeuft aus drm_atomic_helper_cleanup_planes (drm_atomic_helper.c:3209-3214) im commit_tail, also in Prozess- bzw. Workqueue-Kontext; das ist unkritisch. Asymmetrie zum kmap: ttm_bo_kmap braucht die Reservierung fuer den Systemspeicherpfad sehr wohl, ueber ttm_bo_kmap_ttm nach ttm_bo_populate (ttm_bo_util.c:355), und ttm_bo_populate hat dma_resv_assert_held (ttm_bo.c:1265).",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:459-484 \"void ttm_bo_kunmap(struct ttm_bo_kmap_obj *map) { if (!map->virtual) return; switch (map->bo_kmap_type) { case ttm_bo_map_iomap: iounmap(map->virtual); ... } ttm_mem_io_free(map->bo->bdev, map->bo->resource); map->virtual = NULL;\"; drivers/gpu/drm/ttm/ttm_bo.c:1265 \"dma_resv_assert_held(bo->base.resv);\" in ttm_bo_populate; drivers/gpu/drm/nouveau/nouveau_bo.c:682-689 \"nouveau_bo_unmap(struct nouveau_bo *nvbo) { if (!nvbo) return; ttm_bo_kunmap(&nvbo->kmap); }\"; :404-414 \"nouveau_bo_unpin_del(...) { ... nouveau_bo_unmap(nvbo); nouveau_bo_unpin(nvbo);\"",
      "folge_fuer_den_patch": "Ein nouveau_bo_unmap() vor nouveau_bo_unpin() in cleanup_fb ist die minimalinvasive Form und folgt nouveau_bo_unpin_del. Besser ist aber die gesperrte Form, siehe naechster Befund.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Gibt es beim Abbauen ein Rennen, das fuer die gesperrte Form spricht?",
      "antwort": "Ja. nvbo->kmap ist ein einziger Slot pro BO ohne Refcount, und der Pushbuf-Ioctl benutzt genau denselben Slot: nouveau_gem.c:696-704 und :901-909 machen \"if (!nvbo->kmap.virtual) ttm_bo_kmap(..., &nvbo->kmap); nvbo->validate_mapped = true;\", und validate_fini_no_ticket macht ttm_bo_kunmap(&nvbo->kmap) (:434-436). Dieser gesamte Abschnitt laeuft unter der BO-Reservierung: validate_init reserviert mit ww-Ticket (:468 ww_acquire_init, :501 ttm_bo_reserve, :508 ttm_bo_reserve_slowpath), reloc_apply laeuft danach (:846), und das kunmap (:435) steht vor dem ttm_bo_unreserve (:439). Damit ist die Reservierung heute faktisch der Schutz von nvbo->kmap, und nouveau_bo_unmap waere in cleanup_fb der einzige Schreiber ohne diesen Schutz.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_gem.c:696-704 \"if (!nvbo->kmap.virtual) { ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ... nvbo->validate_mapped = true; }\"; :433-439 \"if (unlikely(nvbo->validate_mapped)) { ttm_bo_kunmap(&nvbo->kmap); nvbo->validate_mapped = false; } list_del(&nvbo->entry); nvbo->reserved_by = NULL; ttm_bo_unreserve(&nvbo->bo);\"; :468 \"ww_acquire_init(&op->ticket, &reservation_ww_class);\", :501 \"ret = ttm_bo_reserve(&nvbo->bo, true, false, &op->ticket);\"",
      "folge_fuer_den_patch": "cleanup_fb sollte reservieren und dann ttm_bo_kunmap plus nouveau_bo_unpin_locked unter derselben Reservierung machen (Gegenstueck zum vorgeschlagenen nouveau_bo_pin_map). Dann stehen ausnahmslos alle Schreiber von nvbo->kmap unter der BO-Reservierung, und das Feld hat einen benennbaren Schutz, den man im Commit-Text auch behaupten darf.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Darf get_scanout_buffer nvbo->kmap lesen, ohne irgendetwas zu halten?",
      "antwort": "Ja, genau dann, wenn die Abbildung in prepare_fb angelegt und in cleanup_fb abgebaut wird. drm_panic.h nennt das woertlich als Garantie: \"Anything set up by &drm_plane_helper_funcs.fb_prepare and cleaned up &drm_plane_helper_funcs.fb_cleanup is safe to access, as long as it stays invariant between these two calls\" (drm_panic.h:115-118, die Doku schreibt fb_prepare/fb_cleanup, gemeint sind prepare_fb/cleanup_fb). Die Garantie stammt daher, dass drm_atomic_helper_swap_state plane->state unter drm_panic_lock umhaengt (drm_atomic_helper.c:3352-3362) und prepare_planes vor swap_state laeuft (disp.c:2446 vor :2457). Ebenso ausdruecklich verboten ist der Umweg ueber begin_fb_access/end_fb_access: \"software and hardware state set up by &drm_plane_helper_funcs.begin_fb_access and end_fb_access is not safe to access\" (drm_panic.h:122-124). Der Panic-Callback selbst laeuft unter raw_spin_trylock_irqsave (drm_panic.c:927, get_scanout_buffer an :930) und kann daher gar nichts weiter nehmen.",
      "beleg": "include/drm/drm_panic.h:115-118 \" * - Anything set up by &drm_plane_helper_funcs.fb_prepare and cleaned up / *   &drm_plane_helper_funcs.fb_cleanup is safe to access, as long as it stays / *   invariant between these two calls.\"; :122-124 \" * - Importantly, software and hardware state set up by / *   &drm_plane_helper_funcs.begin_fb_access and / *   &drm_plane_helper_funcs.end_fb_access is not safe to access.\"; drivers/gpu/drm/drm_atomic_helper.c:3352-3362 \"drm_panic_lock(state->dev, flags); for_each_oldnew_plane_in_state(...) { ... plane->state = new_plane_state; } drm_panic_unlock(state->dev, flags);\"; drivers/gpu/drm/drm_panic.c:927-930 \"if (!drm_panic_trylock(plane->dev, flags)) return; ret = plane->helper_private->get_scanout_buffer(plane, &sb);\"",
      "folge_fuer_den_patch": "prepare_fb ist der von drm_panic vorgesehene Ort, nicht begin_fb_access, obwohl die vtable-Doku (drm_modeset_helper_vtables.h:1211-1217) vmap-artige Dinge sonst nach begin_fb_access schieben will. Diesen scheinbaren Widerspruch sollte der Commit-Text mit dem Zitat aus drm_panic.h aufloesen, sonst kommt der Einwand im Review.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Was schuetzt nvbo->kmap sonst, ausser der prepare_fb/cleanup_fb-Klammer?",
      "antwort": "Nichts Eigenes. Das Feld ist ein blankes struct ttm_bo_kmap_obj im nouveau_bo (nouveau_bo.h:20), ohne eigene Sperre und ohne Refcount; der einzige Zaehler in der Naehe ist das separate Flag validate_mapped (nouveau_bo.h:28) fuer den Pushbuf-Pfad. Faktisch geschuetzt wird es durch die BO-Reservierung, weil alle Schreiber ausser nouveau_bo_unmap sie halten (siehe voriger Befund). Fuer den Patch entscheidend und guenstig: der Pushbuf-Pfad prueft \"if (!nvbo->kmap.virtual)\" (nouveau_gem.c:696, :901) und setzt validate_mapped nur dann, folglich reisst validate_fini_no_ticket eine von prepare_fb angelegte Abbildung nicht ab. In die Gegenrichtung kann prepare_fb nicht mitten in den Pushbuf-Abschnitt geraten, weil nouveau_bo_map die Reservierung nimmt, die der Pushbuf ueber den ganzen Abschnitt haelt.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.h:20 \"struct ttm_bo_kmap_obj kmap;\", :28 \"bool validate_mapped;\"; drivers/gpu/drm/nouveau/nouveau_gem.c:696 \"if (!nvbo->kmap.virtual) {\", :901 \"if (!nvbo->kmap.virtual) {\"; :433-435 \"if (unlikely(nvbo->validate_mapped)) { ttm_bo_kunmap(&nvbo->kmap);\"",
      "folge_fuer_den_patch": "Die Koexistenz mit dem Pushbuf-Pfad ist gegeben und muss nicht extra abgesichert werden, aber sie haengt an der einen Zeile \"if (!nvbo->kmap.virtual)\". Das gehoert als Kommentar an die neue Map-Stelle, sonst reisst der naechste Umbau es unbemerkt ab.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Zusatz aus der Sperrlinse: ist nouveau_bo_map() idempotent genug fuer prepare_fb?",
      "antwort": "Nein. nouveau_bo_map (nouveau_bo.c:668-680) prueft nvbo->kmap.virtual nicht und ruft ttm_bo_kmap bedingungslos; ein zweiter Aufruf ueberschreibt nvbo->kmap und verliert die vorherige ioremap-Abbildung ersatzlos. Das ist erreichbar, sobald dasselbe BO an zwei Planes haengt, denn prepare_fb laeuft je Plane einmal (drm_atomic_helper.c:2831-2840), oder wenn der Pushbuf-Pfad gerade eine Abbildung stehen hat. Die Pin-Seite hat mit bo->pin_count einen Zaehler (ttm_bo.c:631), die kmap-Seite hat keinen.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:668-680 (kein Test auf kmap.virtual vor ttm_bo_kmap); drivers/gpu/drm/ttm/ttm_bo.c:624-634 \"void ttm_bo_pin(...) { dma_resv_assert_held(bo->base.resv); ... if (!bo->pin_count++ && bo->resource) ...\"",
      "folge_fuer_den_patch": "Die neue Map-Stelle braucht unter der Reservierung ein \"if (!nvbo->kmap.virtual)\" und die Unmap-Stelle ein passendes Gegenstueck, oder besser einen eigenen kleinen Zaehler im nouveau_bo. Sonst leckt der Patch ioremap-Bereiche und BAR1-Fenster genau in dem Fall, den er absichern soll.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Zusatz: welche Planes brauchen die Abbildung ueberhaupt?",
      "antwort": "Nur die Primary-Planes. wndw.c:704-717 definiert zwei Helper-Strukturen: nv50_wndw_helper ohne get_scanout_buffer und nv50_wndw_primary_helper mit get_scanout_buffer; zugewiesen wird nach plane->type (wndw.c:891-894). drm_panic_register (drm_panic.c:1028 ff.) haengt seinen kmsg_dumper nur an Planes mit get_scanout_buffer. prepare_fb ist aber fuer beide Strukturen dieselbe Funktion.",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:704-717 \"nv50_wndw_helper = { .prepare_fb = nv50_wndw_prepare_fb, .cleanup_fb = nv50_wndw_cleanup_fb, .atomic_check = nv50_wndw_atomic_check, };\" gegenueber \"nv50_wndw_primary_helper = { ... .get_scanout_buffer = nv50_wndw_get_scanout_buffer, };\"; :891-894 \"if (type == DRM_PLANE_TYPE_PRIMARY) drm_plane_helper_add(&wndw->plane, &nv50_wndw_primary_helper); else drm_plane_helper_add(&wndw->plane, &nv50_wndw_helper);\"",
      "folge_fuer_den_patch": "Die Abbildung in prepare_fb an plane->type == DRM_PLANE_TYPE_PRIMARY oder an das gesetzte get_scanout_buffer knuepfen. Eine bedingungslose Abbildung verbraucht fuer Overlays und Cursor zusaetzliche BAR1-Fenster und Kernel-VA, und erhoeht damit genau den ENOSPC-Druck aus Punkt 7 der Aufgabenstellung.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Zusatz: taugen die Fehlerpfade von prepare_fb als Aufhaenger fuer ein zusaetzliches Map?",
      "antwort": "Nur bedingt, denn sie sind schon heute defekt. Nach erfolgreichem nouveau_bo_pin (wndw.c:554) kehrt die Funktion an zwei Stellen ohne nouveau_bo_unpin zurueck: :580-581 bei Fehler von drm_gem_plane_helper_prepare_fb und :587-588 bei IS_ERR(asyh). Der Helper faengt das nicht auf: die Fehlerschleife fail_prepare_fb ruft cleanup_fb nur fuer Planes mit j < i, also nicht fuer die gescheiterte Plane selbst (drm_atomic_helper.c:2874-2886). Der Pin leckt dort also bereits.",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:579-581 \"ret = drm_gem_plane_helper_prepare_fb(plane, state); if (ret) return ret;\" und :585-588 \"if (wndw->func->prepare) { asyh = nv50_head_atom_get_new(...); if (IS_ERR(asyh)) return PTR_ERR(asyh);\" (beide ohne nouveau_bo_unpin, waehrend :562 im ctxdma-Fall korrekt unpinnt); drivers/gpu/drm/drm_atomic_helper.c:2874-2886 \"fail_prepare_fb: for_each_new_plane_in_state(state, plane, new_plane_state, j) { ... if (j >= i) continue; ... if (funcs->cleanup_fb) funcs->cleanup_fb(plane, new_plane_state); }\"",
      "folge_fuer_den_patch": "Entweder das Map unmittelbar nach dem Pin setzen und die beiden Rueckkehrpunkte mit unmap plus unpin nachruesten, oder die Leckpfade in einem eigenen Vorpatch reparieren. Sonst leckt zusaetzlich zur Pin-Referenz eine ioremap-Abbildung. Ein sauber getrennter Vorpatch fuer die Pin-Lecks duerfte im Review gut ankommen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Zusatz: bleibt die Abbildung ueber Suspend/Resume korrekt?",
      "antwort": "Ja, ohne Sonderbehandlung. nouveau_display_suspend ruft drm_atomic_helper_suspend (nouveau_display.c:772), nouveau_display_resume ruft drm_atomic_helper_resume (:794). Beide gehen ueber die normalen prepare_planes/cleanup_planes-Wege, das heisst die Abbildung wird beim Suspend zusammen mit dem Pin abgebaut und beim Resume zusammen mit dem Pin neu angelegt. Ein zusaetzlicher Wiederherstellungspfad ist nicht noetig.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_display.c:770-778 \"if (drm_drv_uses_atomic_modeset(dev)) { if (!runtime) { disp->suspend = drm_atomic_helper_suspend(dev);\"; :792-796 \"if (drm_drv_uses_atomic_modeset(dev)) { if (disp->suspend) { drm_atomic_helper_resume(dev, disp->suspend);\"",
      "folge_fuer_den_patch": "Keine zusaetzliche Arbeit. Sollte im Commit-Text kurz erwaehnt werden, weil der Reviewer sonst danach fragt.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Zusatz zur Sperrlinse: was haelt die Verdraengung aus Punkt 7, und ist ein gepinntes Scanout-BO davor sicher?",
      "antwort": "Die Verdraengungsschleife in nouveau_ttm_io_mem_reserve (nouveau_bo.c:1341-1356) haelt ausschliesslich drm->ttm.io_reserve_mutex (genommen an :1256). Sie nimmt die dma_resv des Opfers nicht und prueft bo->pin_count nicht. Ein gepinntes, per prepare_fb abgebildetes Scanout-BO kann also Opfer werden: drm_vma_node_unmap (:1350) raeumt nur Userspace-PTEs, die Kernel-ioremap in nvbo->kmap.virtual bleibt bestehen, aber nouveau_ttm_io_mem_free_locked gibt das BAR1-Fenster frei und bus.offset wird auf 0 gesetzt (:1351-1353). Die ioremapte physische Adresse bildet danach nicht mehr dieses BO ab. Auf die io_reserve_lru kommt ein BO ueber nouveau_ttm_fault (nouveau_gem.c:57-60), also sobald Userspace es mmap't und beruehrt, was fuer Dumb- und Scanout-Puffer der Normalfall ist. Auf der Zielmaschine (BAR1 256 MB, VRAM 256 MB) kann -ENOSPC nicht eintreten, auf Karten mit VRAM groesser als BAR sehr wohl.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:1341-1356 \"if (ret == -ENOSPC) { ... nvbo = list_first_entry_or_null(&drm->ttm.io_reserve_lru, ...); if (nvbo) { list_del_init(&nvbo->io_reserve_lru); drm_vma_node_unmap(&nvbo->bo.base.vma_node, bdev->dev_mapping); nouveau_ttm_io_mem_free_locked(drm, nvbo->bo.resource); nvbo->bo.resource->bus.offset = 0; nvbo->bo.resource->bus.addr = NULL; goto retry; } }\" (keine Pruefung von pin_count, kein dma_resv); drivers/gpu/drm/nouveau/nouveau_gem.c:57-60 \"nouveau_bo_del_io_reserve_lru(bo); ... ret = ttm_bo_vm_fault_reserved(vmf, prot, TTM_BO_VM_NUM_PREFAULT); nouveau_bo_add_io_reserve_lru(bo);\"; drivers/gpu/drm/nouveau/nouveau_bo.c:763-771 \"void nouveau_bo_add_io_reserve_lru(...) { mutex_lock(&drm->ttm.io_reserve_mutex); list_move_tail(&nvbo->io_reserve_lru, &drm->ttm.io_reserve_lru); mutex_unlock(...); }\"",
      "folge_fuer_den_patch": "Die Absicherung ist sperrtechnisch billig, weil beide Seiten unter io_reserve_mutex laufen. Zwei gangbare Formen: das Scanout-BO beim Abbilden aus der Liste nehmen (nouveau_bo_del_io_reserve_lru, nouveau_bo.c:773-781) und beim Abbau nichts wieder eintragen, oder in der Verdraengungsschleife Eintraege mit bo->pin_count ueberspringen. Die zweite Form ist allgemeiner und repariert nebenbei denselben Fehler fuer den fbdev-Puffer, ist aber ein zweiter, eigenstaendiger Patch. Diese Verdraengung ist unabhaengig vom Panic-Fehler bereits heute ein Fehler, das gehoert getrennt eingereicht.",
      "sicherheit": "belegt"
    }
  ],
  "fazit": "Der vorgeschlagene Ansatz haelt der Sperrpruefung stand: prepare_fb laeuft in schlaffaehigem Prozesskontext, haelt nur die drm_modeset-ww-Locks und eine PM-Referenz, und das BO ist dort nachweislich nicht reserviert, weil nv50_wndw_prepare_fb selbst nouveau_bo_pin und damit ttm_bo_reserve ruft (wndw.c:554, nouveau_bo.c:640). Ein zusaetzliches nouveau_bo_map() ist deshalb weder eine Doppelreservierung noch eine Verklemmung, und drm_panic.h:115-118 nennt genau die prepare_fb/cleanup_fb-Klammer als den erlaubten Weg, waehrend begin_fb_access an :122-124 ausdruecklich ausgeschlossen wird. Drei Dinge muss der Patch aber zusaetzlich leisten: das Map an ein \"if (!nvbo->kmap.virtual)\" unter der Reservierung knuepfen (nouveau_bo_map ist nicht idempotent und es gibt keinen kmap-Refcount), das Unmap in cleanup_fb unter derselben Reservierung machen (sonst ist es der einzige Schreiber von nvbo->kmap ohne diesen Schutz, waehrend der Pushbuf-Pfad ihn haelt), und nur Primary-Planes abbilden, da nur diese ueberhaupt get_scanout_buffer haben (wndw.c:891-894). Zwei Vorpatches draengen sich auf: die beiden Pin-Lecks in den Fehlerpfaden von prepare_fb (wndw.c:580, :587) und die Verdraengung in nouveau_ttm_io_mem_reserve (nouveau_bo.c:1341-1356), die ohne dma_resv und ohne pin_count-Pruefung einem gepinnten Scanout-BO das BAR1-Fenster wegnimmt."
}