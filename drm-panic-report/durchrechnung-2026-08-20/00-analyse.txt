{
  "befunde": [
    {
      "frage": "(a) Landet ein Puffer, den nur der Kern per ttm_bo_kmap abbildet, auf der io_reserve_lru? Alle Aufrufer von nouveau_bo_add_io_reserve_lru() suchen.",
      "antwort": "Nein. Es gibt baumweit genau EINEN Aufrufer, und der liegt im Userspace-mmap-Fehlerpfad. Eine reine Kernabbildung per ttm_bo_kmap()/nouveau_bo_map() fasst die Liste nie an. ABER: die Liste ist puffer-, nicht abbildungsbezogen. Sobald derselbe Puffer irgendwann per mmap durch den Userspace gefaultet wird, kommt er ueber genau diesen Pfad auf die LRU, unabhaengig davon, dass der Kern gerade eine ioremap-Abbildung darauf haelt. Ein Scanout-Puffer ist genau so ein Puffer: nouveau_gem.c:227 setzt .mmap = drm_gem_ttm_mmap und .vm_ops = &nouveau_ttm_vm_ops fuer JEDES GEM-Objekt, es gibt keinerlei Pruefung, die einen aktuell angezeigten oder gepinnten Puffer vom mmap ausnaehme. Der Compositor, der seinen eigenen Dumb-Buffer schreibt, waehrend er angezeigt wird, ist der Normalfall.",
      "beleg": "git grep ueber c21bb4193868 (ganzer Baum) liefert fuer nouveau_bo_add_io_reserve_lru nur drei Treffer: Definition drivers/gpu/drm/nouveau/nouveau_bo.c:763, Deklaration nouveau_bo.h:90, Aufruf nouveau_gem.c:60.\n\ndrivers/gpu/drm/nouveau/nouveau_gem.c:43-66 nouveau_ttm_fault():\n\tret = ttm_bo_vm_reserve(bo, vmf);\n\t...\n\tret = nouveau_ttm_fault_reserve_notify(bo);\n\t...\n:57\tnouveau_bo_del_io_reserve_lru(bo);\n\tprot = vm_get_page_prot(vma->vm_flags);\n\tret = ttm_bo_vm_fault_reserved(vmf, prot, TTM_BO_VM_NUM_PREFAULT);\n:60\tnouveau_bo_add_io_reserve_lru(bo);\n\ndrivers/gpu/drm/nouveau/nouveau_bo.c:763-771:\nvoid nouveau_bo_add_io_reserve_lru(struct ttm_buffer_object *bo)\n{ ... mutex_lock(&drm->ttm.io_reserve_mutex);\n:769\tlist_move_tail(&nvbo->io_reserve_lru, &drm->ttm.io_reserve_lru); ... }\nKeine Pruefung auf pin_count, kmap.virtual oder Scanout-Eigenschaft.\n\nAbgaenge (nur der Vollstaendigkeit): nouveau_bo.c:144 (nouveau_bo_del_ttm), nouveau_bo.c:1078 (nouveau_bo_move_ntfy), nouveau_gem.c:57.\n\ndrivers/gpu/drm/nouveau/nouveau_gem.c:217-229:\nconst struct drm_gem_object_funcs nouveau_gem_object_funcs = {\n\t... .vmap = drm_gem_ttm_vmap, .vunmap = drm_gem_ttm_vunmap,\n:227\t.mmap = drm_gem_ttm_mmap,\n:228\t.vm_ops = &nouveau_ttm_vm_ops, };",
      "folge_fuer_den_patch": "Die Formulierung \"Auf die io_reserve_lru kommt ein Puffer nur ueber den Userspace-mmap-Fehlerpfad\" stimmt, ist aber irrefuehrend beruhigend. Der Patch muss davon ausgehen, dass GENAU der Puffer, den er im Panic-Fall lesen will, auf der LRU liegt, denn er ist per Definition der sichtbare Framebuffer, und der wird typischerweise auch per mmap beschrieben. Eine Absicherung ist also noetig, nicht optional (auf Karten mit VRAM > BAR1).",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Fuehrt ttm_bo_kmap() fuer VRAM ueber nouveau_ttm_io_mem_reserve()? Gibt es dort einen Refcount, der eine gehaltene Kernabbildung schuetzt?",
      "antwort": "Ja zur Kette, klares Nein zum Refcount. ttm_bo_kmap() ruft ttm_mem_io_reserve(), das bdev->funcs->io_mem_reserve() ruft, und das ist bei nouveau nouveau_ttm_io_mem_reserve(). ttm_mem_io_reserve() benutzt (mem->bus.offset || mem->bus.addr) als BOOLESCHES \"schon reserviert\", nicht als Zaehler. ttm_mem_io_free() reisst die Reservierung beim ERSTEN Aufrufer ab, egal wie viele Kernabbildungen noch darauf zeigen. Damit gilt: (1) eine zweite Abbildung desselben Puffers (etwa drm_gem_ttm_vmap -> ttm_bo_vmap, ttm_bo_util.c:505) bekommt kostenlos denselben bus.offset, (2) deren Abbau (ttm_bo_vunmap, ttm_bo_util.c:579, bzw. ttm_bo_kunmap, :478) ruft ttm_mem_io_free() bedingungslos und gibt das BAR1-Fenster frei, waehrend die erste Abbildung noch lebt. Auch die NVKM-Seite zaehlt nicht: nvkm_umem_map() gibt bei bereits bestehender Abbildung -EEXIST zurueck (umem.c:92-93), also einen Fehler statt einer Referenz.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:439-448 (ttm_bo_kmap):\n\tret = ttm_mem_io_reserve(bo->bdev, res);\n\tif (ret) return ret;\n\tif (!res->bus.is_iomem) { return ttm_bo_kmap_ttm(...); }\n\telse { ... return ttm_bo_ioremap(bo, offset, size, map); }\n\nttm_bo_util.c:49-59:\nint ttm_mem_io_reserve(struct ttm_device *bdev, struct ttm_resource *mem)\n{\n\tif (mem->bus.offset || mem->bus.addr)\n\t\treturn 0;\n\tmem->bus.is_iomem = false;\n\tif (!bdev->funcs->io_mem_reserve) return 0;\n\treturn bdev->funcs->io_mem_reserve(bdev, mem);\n}\n\nttm_bo_util.c:62-76:\nvoid ttm_mem_io_free(...)\n{ if (!mem) return;\n  if (!mem->bus.offset && !mem->bus.addr) return;\n  if (bdev->funcs->io_mem_free) bdev->funcs->io_mem_free(bdev, mem);\n  mem->bus.offset = 0; mem->bus.addr = NULL; }\nKein Zaehlerfeld, siehe auch struct ttm_bus_placement, include/drm/ttm/ttm_resource.h:242-247 (addr, offset, is_iomem, caching, sonst nichts).\n\nVerdrahtung: drivers/gpu/drm/nouveau/nouveau_bo.c:1489-1499\nstruct ttm_device_funcs nouveau_bo_driver = { ... .io_mem_reserve = &nouveau_ttm_io_mem_reserve, .io_mem_free = &nouveau_ttm_io_mem_free, };\nVRAM-Zweig in nouveau_ttm_io_mem_reserve: nouveau_bo.c:1284ff (case TTM_PL_VRAM ... reg->bus.is_iomem = true ... nvif_object_map_handle(...) ... reg->bus.offset = handle).\n\nZweiter Abbildungsweg ohne Zaehler: nouveau_gem.c:225 .vmap = drm_gem_ttm_vmap -> ttm_bo_vmap, ttm_bo_util.c:498-553 (:505 ttm_mem_io_reserve, :515/523 ioremap_wc/ioremap), Abbau ttm_bo_vunmap ttm_bo_util.c:564-580 mit :579 ttm_mem_io_free(bo->bdev, mem) bedingungslos.\nGleiches bei ttm_bo_kunmap, ttm_bo_util.c:478: ttm_mem_io_free(map->bo->bdev, map->bo->resource);\n\ndrivers/gpu/drm/nouveau/nvkm/subdev/mmu/umem.c:86-93 (nvkm_umem_map):\n\tif (!umem->mappable) return -EINVAL;\n\tif (umem->map) return -EEXIST;",
      "folge_fuer_den_patch": "Selbst wenn die LRU-Verdraengung vollstaendig entschaerft wird, bleibt ein zweiter Weg, die Panic-Abbildung stillschweigend zu entwerten: jede PRIME/dma-buf-vunmap desselben Puffers ruft ttm_mem_io_free(). Das ist eine echte TTM-Luecke, kein nouveau-Fehler, und sie ist mit einem nouveau-Patch nicht sauber zu schliessen. Fuer die Einreichung: als bekannte Restgefahr benennen, nicht mitreparieren wollen. Zusaetzlich: nouveau_bo_map() (nouveau_bo.c:667-680) ruft ttm_bo_kmap() OHNE Pruefung auf nvbo->kmap.virtual und wuerde eine bestehende Abbildung ueberschreiben. Die Ueberlappung mit dem Pushbuf-Reloc-Pfad ist durch die Reservierung serialisiert (nouveau_bo_map nimmt ttm_bo_reserve blockierend, nouveau_bo.c:672), und der Reloc-Pfad setzt validate_mapped nur, wenn er selbst abgebildet hat (nouveau_gem.c:696-704), sodass nouveau_gem.c:434-437 eine fremde Abbildung nicht abraeumt. Trotzdem gehoert in nouveau_bo_map() oder in den Aufrufer eine Idempotenz-Pruefung.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Schuetzt nouveau_bo_pin() den Puffer vor dieser Verdraengung?",
      "antwort": "Nein. Die Verdraengungsschleife nimmt bedingungslos den Kopf der Liste und prueft weder bo.pin_count noch sonst etwas. Sie haelt dabei nicht einmal die Reservierung des Opfers, nur drm->ttm.io_reserve_mutex. Auch nouveau_bo_pin_locked() fasst die io_reserve_lru nicht an, Pin bedeutet in nouveau ausschliesslich \"nicht im VRAM verschieben\" (ttm_bo_pin plus Buchhaltung von vram_available), nicht \"BAR1-Fenster festhalten\".",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:1341-1358:\nout:\n\tif (ret == -ENOSPC) {\n\t\tstruct nouveau_bo *nvbo;\n\t\tnvbo = list_first_entry_or_null(&drm->ttm.io_reserve_lru,\n\t\t\t\t\t\ttypeof(*nvbo), io_reserve_lru);\n\t\tif (nvbo) {\n\t\t\tlist_del_init(&nvbo->io_reserve_lru);\n\t\t\tdrm_vma_node_unmap(&nvbo->bo.base.vma_node, bdev->dev_mapping);\n\t\t\tnouveau_ttm_io_mem_free_locked(drm, nvbo->bo.resource);\n\t\t\tnvbo->bo.resource->bus.offset = 0;\n\t\t\tnvbo->bo.resource->bus.addr = NULL;\n\t\t\tgoto retry;\n\t\t}\n\t}\nKein if (nvbo->bo.pin_count), kein if (nvbo->kmap.virtual), kein dma_resv_trylock. Nur mutex_lock(&drm->ttm.io_reserve_mutex) bei :1262 und mutex_unlock bei :1359.\n\nnouveau_bo_pin_locked, nouveau_bo.c:580-614: ttm_bo_pin(&nvbo->bo) und die Zaehler drm->gem.vram_available / gart_available, sonst nichts. nouveau_bo_pin, :640-651: nur ttm_bo_reserve + nouveau_bo_pin_locked + ttm_bo_unreserve.\n\nnv50_wndw_prepare_fb, dispnv50/wndw.c:553-556: nvbo = nouveau_gem_object(fb->obj[0]); ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);\nnv50_wndw_cleanup_fb, wndw.c:533-534: nouveau_bo_unpin(nvbo);",
      "folge_fuer_den_patch": "Der Pin aus prepare_fb traegt die Absicherung nicht. Wer sie ueber pin_count bauen will, muss die Schleife aktiv aendern (Pruefung plus Iteration statt list_first_entry), nicht auf bestehende Semantik hoffen. Nebenpunkt fuer die Einreichung: die Schleife dereferenziert nvbo->bo.resource ungeprueft (:1352-1354), das ist ein eigener, unabhaengiger Mangel.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Welche Absicherung ist richtig: nouveau_bo_del_io_reserve_lru() waehrend der Abbildung, oder die Verdraengungsschleife Puffer mit gesetztem kmap.virtual ueberspringen lassen? Gibt es eine dritte, bessere?",
      "antwort": "Beide einzeln sind falsch beziehungsweise unvollstaendig.\n\nVariante 1 (del_io_reserve_lru waehrend der Abbildung) ist WIRKUNGSLOS. Der Eintrag kommt beim naechsten Userspace-Seitenfehler auf denselben Puffer sofort zurueck: nouveau_ttm_fault ruft bei JEDEM Fault erst del (nouveau_gem.c:57) und dann add (nouveau_gem.c:60), bedingungslos. Ein einmaliges Herausnehmen zur Abbildungszeit haelt also nur bis zum naechsten Schreibzugriff des Compositors auf seinen Framebuffer, das heisst typischerweise Millisekunden.\n\nVariante 2 (Schleife ueberspringt kmap.virtual) wirkt, hat aber drei Loecher: (i) sie sieht Abbildungen ueber drm_gem_ttm_vmap/ttm_bo_vmap nicht, die nvbo->kmap gar nicht anfassen (nouveau_gem.c:225, ttm_bo_util.c:498ff); (ii) Ordnungsfenster: nouveau_ttm_io_mem_reserve setzt bus.offset und gibt io_reserve_mutex frei, ERST DANACH setzt ttm_bo_ioremap map->virtual (ttm_bo_util.c:313-336), in diesem Fenster ist kmap.virtual noch NULL und der eigene Puffer verdraengbar; (iii) sie schuetzt nicht gegen ttm_mem_io_free aus einem zweiten Abbildungsabbau (siehe (b)).\n\nDritte, bessere Loesung: die Absicherung an den EINZIGEN Erzeuger haengen und zusaetzlich in der Schleife gegenpruefen, gestuetzt auf einen expliziten Zustand statt auf kmap.virtual. Konkret drei Stellen:\n  1. beim Anlegen der Panic-Abbildung einmal nouveau_bo_del_io_reserve_lru() (raeumt einen Alteintrag ab),\n  2. in nouveau_ttm_fault das nouveau_bo_add_io_reserve_lru() bei nouveau_gem.c:60 auslassen, solange der Kern eine Abbildung haelt (neues Feld in struct nouveau_bo, etwa ein Zaehler io_reserve_pinned, gesetzt in prepare_fb, geloescht in cleanup_fb),\n  3. in der Verdraengungsschleife Eintraege mit diesem Zustand ueberspringen und zum naechsten Kandidaten weitergehen (list_for_each_entry_safe statt list_first_entry_or_null), als Verteidigung gegen Wettlaeufe.\nDas ist wasserdicht gegen den LRU-Weg, drei kleine Aenderungen, und die Bedingung sagt genau, was gemeint ist (\"der Kern haelt eine ioremap auf dieses BAR1-Fenster\"), statt es aus kmap.virtual zu erraten.\n\nNebenwirkungen im Vergleich: Variante ueber pin_count waere ebenfalls denkbar und braeuchte kein neues Feld, ueberdehnt aber die Bedeutung von Pin (Pin heisst \"nicht verschieben\", nicht \"BAR-Fenster halten\") und wuerde jeden gepinnten und gleichzeitig gemmapten Puffer dauerhaft BAR1 belegen lassen. Allen Varianten gemeinsam: auf Karten mit VRAM > BAR1 verkleinert sich der verdraengbare Vorrat um die Groesse des Scanout-Puffers. Ist die LRU nur noch mit geschuetzten Eintraegen besetzt, faellt -ENOSPC bis in den Userspace-Fault durch und wird dort zu SIGBUS. Das ist der Preis und gehoert in den Commit-Text.",
      "beleg": "nouveau_gem.c:57 und :60 (Volltext siehe Befund (a)): del und add liegen unbedingt um ttm_bo_vm_fault_reserved herum, pro Fault.\n\nttm_bo_util.c:313-336 (ttm_bo_ioremap):\n\tstruct ttm_resource *mem = bo->resource;\n\tif (mem->bus.addr) { map->bo_kmap_type = ttm_bo_map_premapped; map->virtual = ((u8 *)mem->bus.addr) + offset; }\n\telse { resource_size_t res = mem->bus.offset + offset;\n\t\tmap->bo_kmap_type = ttm_bo_map_iomap;\n\t\tif (mem->bus.caching == ttm_write_combined) map->virtual = ioremap_wc(res, size); ... }\nDieser Code laeuft NACH dem mutex_unlock in nouveau_bo.c:1359.\n\nnouveau_gem.c:225-226: .vmap = drm_gem_ttm_vmap, .vunmap = drm_gem_ttm_vunmap (zweiter Kernabbildungsweg, beruehrt nvbo->kmap nicht).\n\nVerdraengungsschleife nouveau_bo.c:1345-1355 (Volltext siehe (c)): list_first_entry_or_null, also Kopf oder gar nichts, kein Weiterruecken.\n\nnouveau_bo.c:763-771 add und :773-781 del, beide nur list_move_tail bzw. list_del_init unter drm->ttm.io_reserve_mutex.",
      "folge_fuer_den_patch": "Empfehlung fuer die Einreichung: nicht \"del waehrend der Abbildung\" allein einreichen, das faellt bei Jocelyn oder Lyude sofort auf, weil nouveau_gem.c:60 den Eintrag zurueckbringt. Der Dreiklang (einmal del, add unterdruecken, Schleife ueberspringen lassen) laesst sich in wenigen Zeilen zeigen und im Commit-Text mit genau diesen Zeilennummern begruenden. Auf der Zielmaschine (BAR1 256 MB = VRAM 256 MB) ist der Pfad nicht testbar, das muss im Commit-Text stehen, sonst behauptet man Testabdeckung, die es nicht gibt.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(e) Was passiert mit einer bestehenden Kernabbildung, wenn der Puffer verdraengt WIRD? Zeigt der Zeiger auf fremden Speicher, oder faengt etwas ab?",
      "antwort": "Der Zeiger zeigt danach auf fremden Speicher, und nichts faengt das ab. Es gibt keinen Absturz, keine Warnung, keine Invalidierung, nur stille Datenverfaelschung.\n\nIm Einzelnen: drm_vma_node_unmap() reisst ausschliesslich die Userspace-PTEs des shared file mapping ab (unmap_mapping_range), die ioremap-Abbildung des Kerns liegt nicht in diesem address_space und bleibt unangetastet. nouveau_ttm_io_mem_free_locked -> nvif_object_unmap_handle -> nvkm_umem_unmap gibt fuer den IO-Fall die BAR1-VMA per nvkm_vmm_put an den BAR1-Allokator zurueck. Der naechste nv50_mem_map holt sich mit nvkm_vmm_get einen Bereich aus demselben freien Baum, kann also exakt dasselbe BAR1-Fenster bekommen, und nvkm_memory_map programmiert die BAR1-Seitentabellen dieses Bereichs auf einen ANDEREN Puffer um. Die alte ioremap bleibt als gueltige CPU-Abbildung derselben physischen BAR1-Adressen bestehen, faengt also ab diesem Moment den VRAM eines fremden Puffers. Im Panic-Fall hiesse das: das Panic-Bild wird in einen beliebigen anderen Puffer geschrieben, sichtbar wird nichts.\n\nEinzige (unzureichende) Bremse: bus.offset und bus.addr werden auf 0 gesetzt (nouveau_bo.c:1353-1354). Dadurch faellt ein spaeteres ttm_bo_kunmap zwar korrekt in den Frueh-Ausstieg von ttm_mem_io_free (kein doppeltes unmap_handle), das iounmap selbst geschieht aber weiterhin und ist auch richtig. Es gibt keine Rueckmeldung an den Halter der Abbildung. In struct ttm_bus_placement existiert kein Feld, ueber das eine Invalidierung signalisiert werden koennte.",
      "beleg": "nouveau_bo.c:1350-1354 (Verdraengung, Volltext siehe (c)).\n\ninclude/drm/drm_vma_manager.h:220-227:\nstatic inline void drm_vma_node_unmap(struct drm_vma_offset_node *node, struct address_space *file_mapping)\n{ if (drm_mm_node_allocated(&node->vm_node))\n\tunmap_mapping_range(file_mapping, drm_vma_node_offset_addr(node), drm_vma_node_size(node) << PAGE_SHIFT, 1); }\n\nKette des Handle-Abbaus:\nnouveau_bo.c:1244-1245 case TTM_PL_VRAM: nvif_object_unmap_handle(&mem->mem.object);\nnvif/object.c:134-145 nvif_object_unmap_handle: .ioctl.type = NVIF_IOCTL_V0_UNMAP; nvif_object_ioctl(...)\nnvkm/core/ioctl.c:230-241 nvkm_ioctl_unmap -> ret = nvkm_object_unmap(object);\nnvkm/core/object.c:128-132: if (likely(object->func->unmap)) return object->func->unmap(object);\nnvkm/subdev/mmu/umem.c:138 .unmap = nvkm_umem_unmap\nnvkm/subdev/mmu/umem.c:62-80:\nstatic int nvkm_umem_unmap(struct nvkm_object *object)\n{ struct nvkm_umem *umem = nvkm_umem(object);\n  if (!umem->map) return -EEXIST;\n  if (umem->io) { if (!IS_ERR(umem->bar)) { struct nvkm_device *device = umem->mmu->subdev.device;\n\t\tnvkm_vmm_put(nvkm_bar_bar1_vmm(device), &umem->bar); } else { umem->bar = NULL; } }\n  else { vunmap(umem->map); umem->map = NULL; }\n  return 0; }\n(umem->bar und umem->map sind eine union, umem.h:17-20, deshalb greift die !umem->map-Pruefung auch im IO-Fall.)\n\nWiedervergabe desselben BAR1-Bereichs:\nnvkm/subdev/mmu/memnv50.c:33-64 nv50_mem_map:\n\tstruct nvkm_vmm *bar = nvkm_bar_bar1_vmm(device);\n\tret = nvkm_vmm_get(bar, 12, size, pvma);\n\t*paddr = device->func->resource_addr(device, NVKM_BAR1_FB) + (*pvma)->addr;\n\treturn nvkm_memory_map(memory, 0, bar, *pvma, &uvmm, sizeof(uvmm));\nnvkm/subdev/mmu/vmm.c:1774-1786: Suche im freien rb-Baum vmm->free, -ENOSPC nur wenn nichts passt, sonst wird der Bereich vergeben.\n\nKein Zugriff auf ttm_bo_kmap_obj.virtual in der gesamten Verdraengung; ttm_bo_kunmap (ttm_bo_util.c:462-481) macht iounmap(map->virtual) und danach ttm_mem_io_free, das wegen offset==0 und addr==NULL bei ttm_bo_util.c:66-68 frueh zurueckkehrt.\n\ninclude/drm/ttm/ttm_resource.h:242-247 struct ttm_bus_placement: nur addr, offset, is_iomem, caching. Kein Gueltigkeitsvermerk, kein Zaehler.",
      "folge_fuer_den_patch": "Der Fehlermodus ist still. Ein Patch, der die Abbildung ausserhalb des Panic-Kontexts anlegt und im Callback nur noch nvbo->kmap.virtual liest, kann die Gueltigkeit dieses Zeigers zur Panic-Zeit mit keinem verfuegbaren Mittel pruefen: es gibt kein Feld, das \"noch gueltig\" sagt. Deshalb muss die Absicherung praeventiv sein (die Verdraengung darf gar nicht erst zugreifen), nicht defensiv im Callback. Auf der Zielmaschine (BAR1 = VRAM = 256 MB) ist der Weg nicht erreichbar, auf Karten mit VRAM > BAR1 schon, dort waere die Folge ein leerer oder falsch beschriebener Panic-Bildschirm statt eines Absturzes.",
      "sicherheit": "belegt"
    }
  ],
  "fazit": "Eine reine Kernabbildung landet nicht selbst auf der io_reserve_lru (einziger Erzeuger ist nouveau_gem.c:60 im Userspace-mmap-Fehlerpfad), aber der Scanout-Puffer landet dort trotzdem, weil er nebenbei per mmap beschrieben wird, und weder nouveau_bo_pin() noch irgendeine Pruefung in der Verdraengungsschleife (nouveau_bo.c:1341-1358) schuetzt ihn. Einen Refcount gibt es nirgends: ttm_mem_io_reserve/ttm_mem_io_free (ttm_bo_util.c:49-76) behandeln die Reservierung als Boolean, und nvkm_umem_map antwortet auf eine zweite Abbildung mit -EEXIST statt mit einer Referenz. Wird verdraengt, gibt nvkm_umem_unmap die BAR1-VMA per nvkm_vmm_put frei, der naechste nvkm_vmm_get kann denselben Bereich einem anderen Puffer geben, und die gehaltene ioremap zeigt ab da still auf fremden VRAM, ohne dass etwas abfaengt. Von den beiden vorgeschlagenen Absicherungen ist die erste wirkungslos (jeder Seitenfehler haengt den Puffer bei nouveau_gem.c:60 wieder ein) und die zweite loechrig (sieht drm_gem_ttm_vmap nicht und hat ein Ordnungsfenster); richtig ist die Kombination aus einmaligem del, unterdruecktem add bei nouveau_gem.c:60 und einer ueberspringenden statt kopffixierten Verdraengungsschleife, gestuetzt auf ein explizites Feld in struct nouveau_bo."
}