{
  "treffer": true,
  "schwere": "hoch",
  "beschreibung": "Keine klassische Verklemmung. Die Sperrreihenfolge des neuen Codes ist sauber (siehe unten). Der Patch bricht aber das Schutzprotokoll um `nvbo->kmap`: Er koppelt die Lebensdauer der Abbildung an `bo->pin_count`, obwohl dieser Zaehler mit `nouveau_bo_pin()`/`nouveau_bo_unpin()` geteilt wird, die der Patch fuer Nicht-Primary-Planes ausdruecklich beibehaelt (`nv50_wndw_pin_fb()`/`nv50_wndw_unpin_fb()`). Sobald ein und derselbe BO gleichzeitig von einer Primary-Plane (Map-Pfad) und einer Overlay-Plane (Nicht-Map-Pfad) gepinnt ist, ist die Freigabe asymmetrisch, und zwar in der Reihenfolge, die garantiert eintritt.\n\nAblauf (Tesla, Zielmaschine): fb F liegt auf Primary P und Overlay O, `pin_count == 2`, `kmap` lebt.\n1. `drm_atomic_helper_cleanup_planes()` laeuft `for_each_old_plane_in_state`, also in Plane-Index-Reihenfolge. `nv50_head_create()` legt die Primary (`nv50_base_new`, head.c:594) VOR dem Overlay (`nv50_ovly_new`, head.c:595) an. Die Primary wird also zuerst aufgeraeumt.\n2. Primary: `nouveau_bo_unpin_unmap()` → `pin_count` 2→1 → `if (!bo->pin_count)` ist falsch → **kein `ttm_bo_kunmap()`**.\n3. Overlay: `nouveau_bo_unpin()` → `pin_count` 1→0. Diese Funktion weiss nichts von der Abbildung.\n\nErgebnis: `nvbo->kmap.virtual` bleibt gesetzt, waehrend `pin_count == 0` ist. Damit ist der BO wieder verschiebbar und verdraengbar (`ttm_bo_evict_cb` ueberspringt nur gepinnte BOs, ttm_bo.c:524), waehrend eine ioremap-Abbildung seines alten BAR1-Fensters weiterlebt. Und diese Abbildung ist erreichbar: Der Pushbuf-Reloc-Pfad prueft genau `!nvbo->kmap.virtual` (nouveau_gem.c:696, :901) und schreibt sonst durch sie hindurch mit `nouveau_bo_wr32()` (nouveau_gem.c:737, :912 → nouveau_bo.c:828-839). Nach einem Move/Evict landet der Reloc-Schreibvorgang in dem, was inzwischen in diesem BAR1-Fenster liegt: stille VRAM-Korruption eines fremden Puffers. Die neue Veralterungspruefung `kmap_bus_offset` des Patches sitzt ausschliesslich in `nv50_wndw_panic_map()` und deckt diesen Verbraucher nicht ab.\n\nZweitfolge: `nouveau_bo_del_ttm()` (nouveau_bo.c:135-155) enthaelt kein `ttm_bo_kunmap()`. Die geleckte Abbildung ueberlebt also die BO-Zerstoerung: ioremap-Bereich und BAR1-Handle (`nvif_object_unmap_handle()` via `ttm_mem_io_free()`, ttm_bo_util.c:62-76) werden nie freigegeben, `nvbo->kmap.bo` bleibt als toter Zeiger stehen. Der `WARN_ON(nvbo->bo.pin_count > 0)` in :142 schlaegt nicht an, weil der Pin ja korrekt auf 0 steht.\n\nDas Szenario ist erreichbar: Das nv50-Overlay akzeptiert `DRM_FORMAT_XRGB8888` (ovly507e.c:138), also dasselbe Format wie die Primary. Userspace darf denselben Framebuffer auf beide Planes legen. Auf GV100+ gilt dieselbe Asymmetrie zwischen `nv50_wndw_primary_helper` und `nv50_wndw_helper` (wndw.c:712-717 vs. :705-710).\n\nNebenbefund im selben Bereich: Der `-EBUSY`-Zweig von `nouveau_bo_pin_locked()` (nouveau_bo.c:575-582) setzt `ret = -EBUSY` und ruft **trotzdem** `ttm_bo_pin(&nvbo->bo)`. `nouveau_bo_pin_map()` macht daraufhin `goto out` ohne `nouveau_bo_unpin_locked()`, gibt den Fehler an `prepare_fb` weiter, und `drm_atomic_helper_prepare_planes()` ruft fuer diese Plane kein `cleanup_fb` (drm_atomic_helper.c:2880, `if (j >= i) continue;`). Der Pin leckt also. Das ist zwar auch in `nouveau_bo_pin()` schon so, aber der Patch beansprucht im Commit-Text ausdruecklich, die Pin-Lecks in `prepare_fb` zu beseitigen, und laesst dieses stehen.\n\nWas ich geprueft und NICHT gefunden habe (damit die Negativaussage belastbar ist): (a) Sperrreihenfolge des neuen Codes ist `dma_resv` → `drm->ttm.io_reserve_mutex` (nouveau_bo.c:1262, :1368). Alle bestehenden Nehmer dieses Mutex halten dabei entweder ebenfalls die Reservierung (nouveau_bo.c:1078 aus `move_notify`, nouveau_gem.c:57-60 aus dem Fault-Pfad unter `ttm_bo_vm_reserve`) oder gar nichts (nouveau_bo.c:144 im Zerstoerungspfad). Es gibt baumweit keinen Nehmer in umgekehrter Ordnung, also keine ABBA-Verklemmung. (b) `ttm_bo_kmap()`/`ttm_bo_kunmap()` unter der Reservierung ist der dokumentierte Kontext; `ttm_bo_kmap()` selbst nimmt keine Reservierung (ttm_bo_util.c:406-430). (c) Runtime-PM: der neu in `cleanup_fb` erreichbare nvkm-RPC `nvif_object_unmap_handle()` laeuft nie an einem schlafenden Geraet, weil `nv50_disp_atomic_commit()` die Referenz bei disp.c:2434 nimmt, sie bei :2474 fuer den Tail noch einmal nimmt und erst bei :2414-2415 nach `drm_atomic_helper_cleanup_planes()` (:2409) abgibt. (d) `prepare_fb`/`cleanup_fb` laufen ausschliesslich im Prozesskontext (disp.c:2419-2423 Worker, :2479 inline), kein Schlafen im Atomic-Kontext. (e) Selbstverklemmung auf der Reservierung: kein Aufrufer von `drm_atomic_helper_prepare_planes()`/`_cleanup_planes()` haelt die `dma_resv` des Framebuffers; `drm_fbdev_ttm_damage_blit()` gibt sie in `drm_client_buffer_vunmap_local()` frei, bevor `helper->fb->funcs->dirty` laeuft (drm_fbdev_ttm.c:126-133 vs. :152-158).",
  "beleg": "1) Der asymmetrische Zaehler, Patchdatei /home/neo/projects/nouveau-nvac-patches/0001-drm-nouveau-dispnv50-panic-map.diff, Hunk wndw.c:\n   static void nv50_wndw_unpin_fb(struct drm_plane *plane, struct nouveau_bo *nvbo)\n   { if (nv50_wndw_needs_panic_map(plane)) nouveau_bo_unpin_unmap(nvbo); else nouveau_bo_unpin(nvbo); }\n   und Hunk nouveau_bo.c:\n   nouveau_bo_unpin_locked(nvbo);\n   if (!bo->pin_count) { ttm_bo_kunmap(&nvbo->kmap); nvbo->kmap_bus_offset = 0; }\n\n2) Aufraeum-Reihenfolge ist Plane-Index-Reihenfolge, include/drm/drm_atomic.h:1178-1184:\n   #define for_each_old_plane_in_state(__state, plane, old_plane_state, __i) \\\n       for ((__i) = 0; (__i) < (__state)->dev->mode_config.num_total_plane; (__i)++) \\\n           for_each_if ((__state)->planes[__i].ptr && ...)\n   und drivers/gpu/drm/drm_atomic_helper.c:3209-3214:\n   for_each_old_plane_in_state(state, plane, old_plane_state, i) { ... if (funcs->cleanup_fb) funcs->cleanup_fb(plane, old_plane_state); }\n\n3) Primary vor Overlay angelegt, drivers/gpu/drm/nouveau/dispnv50/head.c:594-595:\n   594: ret = nv50_base_new(drm, head->base.index, &base);\n   595: ret = nv50_ovly_new(drm, head->base.index, &ovly);\n   Helper-Zuweisung drivers/gpu/drm/nouveau/dispnv50/wndw.c (Zuweisung nach plane->type, im Hunk-Kontext bestaetigt):\n   nv50_wndw_primary_helper hat .get_scanout_buffer = nv50_wndw_get_scanout_buffer, nv50_wndw_helper hat es nicht.\n\n4) Overlay nimmt dasselbe Format, drivers/gpu/drm/nouveau/dispnv50/ovly507e.c:138:\n   DRM_FORMAT_XRGB8888,\n\n5) Unpinnter BO ist verdraengbar, drivers/gpu/drm/ttm/ttm_bo.c:524:\n   if (bo->pin_count || !bo->bdev->funcs->eviction_valuable(bo, evict_walk->place)) return 0;\n\n6) Der ungeschuetzte Verbraucher der geleckten Abbildung, drivers/gpu/drm/nouveau/nouveau_gem.c:696-703:\n   if (!nvbo->kmap.virtual) { ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ... nvbo->validate_mapped = true; }\n   :737: nouveau_bo_wr32(nvbo, r->reloc_bo_offset >> 2, data);\n   identisch :901-912 im Push-Pfad.\n   drivers/gpu/drm/nouveau/nouveau_bo.c:828-839:\n   nouveau_bo_wr32(...) { u32 *mem = ttm_kmap_obj_virtual(&nvbo->kmap, &is_iomem); mem += index; if (is_iomem) iowrite32_native(val, (void __force __iomem *)mem); else *mem = val; }\n   Nur der Panic-Pfad des Patches prueft kmap_bus_offset, dieser hier nicht.\n\n7) Kein Aufraeumen bei BO-Zerstoerung, drivers/gpu/drm/nouveau/nouveau_bo.c:135-146:\n   static void nouveau_bo_del_ttm(struct ttm_buffer_object *bo) { ... WARN_ON(nvbo->bo.pin_count > 0); nouveau_bo_del_io_reserve_lru(bo); nv10_bo_put_tile_region(dev, nvbo->tile, NULL); ... }\n   kein ttm_bo_kunmap(&nvbo->kmap).\n\n8) Nebenbefund Pin-Leck, drivers/gpu/drm/nouveau/nouveau_bo.c:575-582:\n   if (error) { NV_ERROR(drm, \"bo %p pinned elsewhere: ...\"); ret = -EBUSY; }\n   ttm_bo_pin(&nvbo->bo);\n   goto out;\n   und drivers/gpu/drm/drm_atomic_helper.c:2877-2880:\n   for_each_new_plane_in_state(state, plane, new_plane_state, j) { if (j >= i) continue; ... funcs->cleanup_fb(plane, new_plane_state); }\n\n9) Sperrreihenfolge, keine Inversion: drivers/gpu/drm/nouveau/nouveau_bo.c:1262 (mutex_lock(&drm->ttm.io_reserve_mutex) in nouveau_ttm_io_mem_reserve) und :1368 (in nouveau_ttm_io_mem_free); Gegenrichtung existiert nicht, die uebrigen Nehmer sind :768/:778 aus nouveau_bo_add/del_io_reserve_lru, gerufen aus nouveau_bo.c:1078 (unter Reservierung) und nouveau_gem.c:57-60 (Fault-Pfad, unter Reservierung).",
  "empfehlung": "Die Abbildung nicht an `bo->pin_count` haengen, weil dieser Zaehler zwei Bedeutungen traegt (gepinnt vs. gepinnt-und-abgebildet). Drei gangbare Wege, in dieser Reihenfolge bevorzugt:\n\n1. Eigener Zaehler. In `struct nouveau_bo` ein `unsigned kmap_pin_count` neben `kmap`/`kmap_bus_offset` fuehren, unter der BO-Reservierung inkrementiert von `nouveau_bo_pin_map()` und dekrementiert von `nouveau_bo_unpin_unmap()`. `ttm_bo_kunmap()` erst bei `--kmap_pin_count == 0`, unabhaengig von `bo->pin_count`. Damit ist die Abbildung gegen jede Mischung aus Map- und Nicht-Map-Pinnern robust und die Reihenfolge der cleanup_fb-Aufrufe egal.\n\n2. Falls kein neues Feld gewuenscht ist: die Fallunterscheidung in `nv50_wndw_pin_fb()`/`nv50_wndw_unpin_fb()` streichen und ALLE nv50-Planes ueber `nouveau_bo_pin_map()`/`nouveau_bo_unpin_unmap()` fuehren. Dann ist `pin_count` wieder ein einheitlicher Zaehler und die Asymmetrie verschwindet. Preis: BAR1-Fenster und Kernel-VA auch fuer Overlay und Cursor, was der Entwurf gerade sparen wollte, und auf Karten mit VRAM > BAR1 ein zusaetzlicher `-ENOSPC`-Pfad in `prepare_fb`. Das sollte im Commit-Text stehen.\n\n3. Unabhaengig von 1 oder 2: die Veralterungspruefung dorthin ziehen, wo `nvbo->kmap` gelesen wird, statt sie nur im Panic-Callback zu haben. Konkret die Pruefung `nvbo->bo.resource->bus.offset == nvbo->kmap_bus_offset` in einen Helfer `nouveau_bo_kmap_valid(nvbo)` fassen und ihn auch an nouveau_gem.c:696 und :901 vor `if (!nvbo->kmap.virtual)` setzen, oder besser: `kmap_bus_offset` in `nouveau_bo_move_ntfy()` bzw. dort, wo der BO seine Ressource wechselt, aktiv invalidieren (Abbildung unter der dann ohnehin gehaltenen Reservierung abreissen). Sonst bleibt der Reloc-Pfad ein ungepruefter Schreiber einer moeglicherweise veralteten Abbildung.\n\nAusserdem: `ttm_bo_kunmap()` in `nouveau_bo_del_ttm()` ergaenzen (bzw. sicherstellen, dass es nie noetig ist), damit ein Programmierfehler dieser Art nicht in einen dauerhaften ioremap- und BAR1-Handle-Verlust muendet, sondern hoechstens in einen WARN.\n\nUnd den `-EBUSY`-Pfad: in `nouveau_bo_pin_map()` bei `ret` aus `nouveau_bo_pin_locked()` zusaetzlich `nouveau_bo_unpin_locked()` rufen, oder besser `nouveau_bo_pin_locked()` so reparieren, dass es im Fehlerfall nicht pinnt. Sonst leckt der Fehlerpfad genau den Pin, dessen Leck der Commit-Text zu beheben verspricht.\n\nZur Formulierung im Commit-Text: die Behauptung \"That also keeps every writer of nouveau_bo.kmap under the buffer reservation\" stimmt zwar, traegt aber nicht das, was sie tragen soll. Unter Reservierung zu schreiben schuetzt gegen gleichzeitige Schreiber, nicht gegen eine Abbildung, die laenger lebt als ihr Erzeuger. Der Satz sollte durch die Zaehler-Begruendung ersetzt werden."
}