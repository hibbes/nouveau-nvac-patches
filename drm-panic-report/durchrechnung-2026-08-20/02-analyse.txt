{
  "befunde": [
    {
      "frage": "(a) Ist cleanup_fb fuer jedes erfolgreiche prepare_fb garantiert?",
      "antwort": "Fuer jede Plane, deren prepare_fb 0 zurueckgab, ja. Fuer die Plane, deren prepare_fb einen Fehler zurueckgab, NEIN. Der Abwickelpfad in drm_atomic_helper_prepare_planes ueberspringt den Index der fehlgeschlagenen Plane. Danach gibt es zwei getrennte Wege: bei einem Fehler nach prepare_planes ruft der Treiber drm_atomic_helper_unprepare_planes auf die NEUEN Zustaende, im Erfolgsfall drm_atomic_helper_cleanup_planes auf die ALTEN Zustaende. nouveau bedient beide korrekt und ohne Doppelaufruf, weil ret nach erfolgreichem swap_state 0 ist.",
      "beleg": "drivers/gpu/drm/drm_atomic_helper.c:2814 drm_atomic_helper_prepare_planes; :2837-2840 `if (funcs->prepare_fb) { ret = funcs->prepare_fb(plane, new_plane_state); if (ret) goto fail_prepare_fb; }`; :2876-2887 `fail_prepare_fb: for_each_new_plane_in_state(state, plane, new_plane_state, j) { ... if (j >= i) continue; ... if (funcs->cleanup_fb) funcs->cleanup_fb(plane, new_plane_state); }` (j >= i uebergeht genau die gescheiterte Plane); :2903 drm_atomic_helper_unprepare_planes (Schleife ueber ALLE new_plane_state, ohne j>=i-Wache); :3202 drm_atomic_helper_cleanup_planes (Schleife ueber old_plane_state). nouveau: drivers/gpu/drm/nouveau/dispnv50/disp.c:2446 prepare_planes, :2483 `if (ret) drm_atomic_helper_unprepare_planes(dev, state);`, :2409 drm_atomic_helper_cleanup_planes im commit_tail.",
      "folge_fuer_den_patch": "Ein in prepare_fb angelegtes Mapping muss in JEDEM Fehlerpfad von prepare_fb selbst wieder abgebaut werden, nicht nur in cleanup_fb. Praktisch heisst das: das Mapping als letzten Schritt von prepare_fb anlegen, oder ein sauberes goto-Unwind einziehen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a-Zusatz) Haelt sich nv50_wndw_prepare_fb heute an diese Regel?",
      "antwort": "Nein. Zwei Fehlerpfade kehren zurueck, ohne das bereits erfolgte nouveau_bo_pin() rueckgaengig zu machen. Das ist ein vorhandenes Leck (pin_count bleibt erhoeht), das spaeter als WARN_ON_ONCE(bo->pin_count) in ttm_bo_release auffaellt. drm_gem_plane_helper_prepare_fb kann tatsaechlich scheitern (-EINVAL, -ENOMEM aus dma_fence_chain_alloc).",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:554 `ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);`; :562 einziger korrekter Unwind `nouveau_bo_unpin(nvbo);` im ctxdma-Fehlerfall; :579-581 `ret = drm_gem_plane_helper_prepare_fb(plane, state); if (ret) return ret;` ohne unpin; :586-589 `asyh = nv50_head_atom_get_new(...); if (IS_ERR(asyh)) return PTR_ERR(asyh);` ohne unpin. Fehlerquellen des Helfers: drivers/gpu/drm/drm_gem_atomic_helper.c:143-160 (`ret = -EINVAL; goto error;` und `ret = -ENOMEM; goto error;`). Folgeeffekt: drivers/gpu/drm/ttm/ttm_bo.c:256 `WARN_ON_ONCE(bo->pin_count);`.",
      "folge_fuer_den_patch": "Vor dem eigentlichen Panic-Fix ein kleiner Vorpatch, der prepare_fb ein gemeinsames Unwind-Label gibt. Sonst erbt der neue nouveau_bo_map()-Aufruf dieselben zwei Lecks, nur mit ioremap-Adressraum obendrauf. Das ist zugleich ein guter, unabhaengig sinnvoller Patch 1/2 fuer die Serie.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Sind waehrend eines Flips kurzzeitig zwei Puffer abgebildet, und wie lange?",
      "antwort": "Ja. prepare_planes laeuft vor swap_state, cleanup_planes ganz am Ende des commit_tail, nachdem nv50_wndw_wait_armed auf das Armieren des neuen Bildes gewartet hat. Alter und neuer Puffer sind also von prepare_planes bis zum Ende des Tails gleichzeitig abgebildet: bei blockierenden Commits innerhalb eines ioctl, bei nicht blockierenden ueber den Workqueue-Lauf hinweg, also mindestens ueber einen Frame. Die Zahl ist auf zwei pro Plane begrenzt: die Modeset-Sperren werden ueber drm_atomic_commit hinweg gehalten, und nicht blockierende Commits bekommen -EBUSY, wenn der Vorgaenger noch nicht flip_done ist.",
      "beleg": "dispnv50/disp.c:2446 prepare_planes, :2456 swap_state, :2405 wait_armed-Schleife (`nv50_wndw_wait_armed`), :2409 cleanup_planes; wndw.c:121-130 nv50_wndw_wait_armed wartet per ntfy_wait_begun. Begrenzung: drivers/gpu/drm/drm_atomic_helper.c:2595-2606 `if (nonblock && old_plane_state->commit && !try_wait_for_completion(&old_plane_state->commit->flip_done)) ... return -EBUSY;`; drivers/gpu/drm/drm_atomic_uapi.c:1663-1780 (Acquire-Kontext wird erst nach drm_atomic_commit mit drm_modeset_drop_locks freigegeben).",
      "folge_fuer_den_patch": "Kein unbegrenztes Wachstum, hoechstens ein zusaetzliches Mapping pro Plane. Der Patch braucht keine Drosselung, muss aber damit rechnen, dass zwei Zustaende derselben Plane gleichzeitig ein gueltiges Mapping halten.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b-Zusatz) Was heisst das fuer den BAR-Bedarf?",
      "antwort": "ioremap verbraucht Kernel-vmalloc-Adressraum, nicht BAR-Fenster. BAR1 verbraucht nur die Reservierung in nouveau_ttm_io_mem_reserve ueber nvif_object_map_handle, und die haengt an der ttm_resource, nicht am Mapping: ttm_mem_io_reserve kehrt sofort zurueck, wenn bus.offset schon gesetzt ist. Ein zweites kmap desselben Puffers kostet also KEIN zusaetzliches BAR-Fenster. Zusaetzlicher BAR-Bedarf entsteht nur dadurch, dass waehrend des Flips der alte und der neue Puffer gleichzeitig eine io-Reservierung halten, also ein Framebuffer mehr als heute. Auf Karten mit VRAM <= BAR1 (Zielmaschine 256 MB zu 256 MB) irrelevant, auf VRAM > BAR1 ein realer Mehrbedarf.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:49-53 `int ttm_mem_io_reserve(...) { if (mem->bus.offset || mem->bus.addr) return 0; ...}`; nouveau_bo.c:1322-1331 `nvif_object_map_handle(&mem->mem.object, &args, argc, &handle, &length); ... reg->bus.offset = handle;`; nouveau_bo.c:1341-1355 der -ENOSPC-Verdraengungspfad ueber io_reserve_lru.",
      "folge_fuer_den_patch": "Der Mehrbedarf ist ein Framebuffer BAR1-Fenster waehrend des Flip-Fensters. Das ist vertretbar, gehoert aber in den Commit-Text, weil es auf VRAM > BAR1 die -ENOSPC-Verdraengung aus Punkt 7 wahrscheinlicher macht.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) System-Suspend: wird VRAM geraeumt, und was wird aus einer gehaltenen Abbildung?",
      "antwort": "Bei nicht-runtime Suspend ist die Reihenfolge sicher. nouveau_display_suspend ruft drm_atomic_helper_suspend, das ueber drm_atomic_helper_disable_all jeder Plane fb = NULL setzt und damit cleanup_fb fuer den alten Zustand ausloest. Erst DANACH raeumt nouveau_do_suspend den VRAM per ttm_resource_manager_evict_all. Eine in prepare_fb angelegte Abbildung ist zu diesem Zeitpunkt also schon abgebaut. Beim Resume laeuft drm_atomic_helper_resume ueber commit_duplicated_state wieder durch prepare_planes, also durch prepare_fb.",
      "beleg": "nouveau_display.c:764-783, insbesondere :770-772 `if (drm_drv_uses_atomic_modeset(dev)) { if (!runtime) { disp->suspend = drm_atomic_helper_suspend(dev);`; drm_atomic_helper.c:3813-3826 (suspend ruft duplicate_state und disable_all), :3554 disable_all, :3605 `drm_atomic_set_fb_for_plane(plane_state, NULL);`; nouveau_drm.c:955 nouveau_display_suspend, :962-963 `man = ttm_manager_type(&drm->ttm.bdev, TTM_PL_VRAM); ttm_resource_manager_evict_all(&drm->ttm.bdev, man);`.",
      "folge_fuer_den_patch": "Kein Sonderfall noetig. Wichtig ist nur die Reihenfolge INNERHALB von cleanup_fb: erst nouveau_bo_unmap(), dann nouveau_bo_unpin(). Umgekehrt koennte der Puffer zwischen unpin und unmap verschoben werden, und ttm_bo_handle_move_mem ruft ttm_bo_unmap_virtual, das bus.offset unter dem gehaltenen kmap auf 0 setzt.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c-Zusatz) Runtime-PM: was passiert dort?",
      "antwort": "Anders und schlechter. nouveau_display_suspend ueberspringt drm_atomic_helper_suspend, wenn runtime == true. Die Planes behalten also ihren fb und bleiben gepinnt, und ttm_resource_manager_evict_all uebergeht gepinnte Puffer. Eine gehaltene Abbildung ueberlebt den Runtime-Suspend und zeigt danach auf ein BAR eines Geraets in D3cold. drm_panic prueft keinerlei PM-Zustand, bevor es get_scanout_buffer aufruft und Pixel schreibt. Auf der Zielmaschine greift das nicht, weil nouveau_pmops_runtime() Runtime-PM nur fuer Optimus oder v1-DSM erlaubt (oder per Modulparameter erzwungen).",
      "beleg": "nouveau_display.c:770-778 (`if (!runtime) { ... drm_atomic_helper_suspend ... }`, kein else-Zweig); nouveau_drm.c:962-963 evict_all wird auch im runtime-Fall ausgefuehrt; drivers/gpu/drm/ttm/ttm_bo.c:524 `if (bo->pin_count || !bo->bdev->funcs->eviction_valuable(bo, evict_walk->place)) return 0;`; nouveau_drm.c:1160-1163 `pci_disable_device(pdev); pci_ignore_hotplug(pdev); pci_set_power_state(pdev, PCI_D3cold);`; nouveau_drm.c:1139-1143 nouveau_pmops_runtime(); drivers/gpu/drm/drm_panic.c:920-950 draw_panic_plane, keine PM-Pruefung.",
      "folge_fuer_den_patch": "Kein Regressionsrisiko durch den Patch, denn heute wuerde nv50_wndw_get_scanout_buffer im selben Zustand ebenfalls auf ein totes Geraet schreiben (bzw. vorher in ioremap sterben). Aber ein Panic-Bild aus einem D3cold-Geraet bleibt sinnlos. Ehrlich im Commit-Text erwaehnen und als getrennte Frage an Jocelyn Falempe weiterreichen, ob drm_panic den PM-Zustand pruefen sollte. Nicht in diesem Patch loesen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Modul entladen oder Geraet wegnehmen, waehrend eine Plane einen abgebildeten Puffer haelt",
      "antwort": "Die Reihenfolge ist guenstig: drm_dev_unplug ruft drm_dev_unregister, das drm_panic_unregister ausfuehrt. Erst danach ruft nouveau_drm_device_fini nouveau_display_fini(dev, false, false), das per drm_atomic_helper_shutdown alle Planes abschaltet und damit cleanup_fb ausloest. Der Panic-Handler ist also schon abgemeldet, bevor die Abbildung verschwindet, und die Abbildung verschwindet, bevor ttm abgebaut wird. Das unmap_mapping_range in drm_dev_unplug betrifft nur Userspace-PTEs, nicht die Kernel-ioremap. Restrisiko: schlaegt drm_atomic_helper_disable_all fehl, wird der Plane-Zustand spaeter von drm_plane_cleanup ohne cleanup_fb zerstoert, dann leckt die Abbildung zusammen mit dem heute schon leckenden Pin.",
      "beleg": "nouveau_drm.c:920-928 nouveau_drm_device_remove: `drm_dev_unplug(drm->dev); nouveau_drm_device_fini(drm);`; drivers/gpu/drm/drm_drv.c:486-501 drm_dev_unplug (`drm_dev_unregister(dev);` :497, `unmap_mapping_range(dev->anon_inode->i_mapping, 0, 0, 1);` :500); drm_drv.c:1150-1153 in drm_dev_unregister `drm_panic_unregister(dev);`; nouveau_drm.c:597 `nouveau_display_fini(dev, false, false);`; nouveau_display.c:583-585 `if (!suspend) { if (drm_drv_uses_atomic_modeset(dev)) drm_atomic_helper_shutdown(dev);`; drm_atomic_helper.c:3674-3692 shutdown ruft disable_all; drivers/gpu/drm/drm_plane.c:703-705 `WARN_ON(plane->state && !plane->funcs->atomic_destroy_state); if (plane->state && ...) plane->funcs->atomic_destroy_state(plane, plane->state);`; wndw.c:719-726 nv50_wndw_atomic_destroy_state ruft nur __drm_atomic_helper_plane_destroy_state und kfree.",
      "folge_fuer_den_patch": "Keine zusaetzliche Absicherung noetig. Der Fehlerfall ist eine Verschaerfung eines bestehenden Lecks, keine neue Fehlerklasse. Wer will, kann in nv50_wndw_atomic_destroy_state ein WARN_ON auf ein noch gesetztes Mapping setzen, um solche Faelle sichtbar zu machen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(e) Kann derselbe Puffer mehrfach abgebildet werden, und ist nouveau_bo_map/unmap dann symmetrisch?",
      "antwort": "Nein, es ist nicht symmetrisch, und es braucht zwingend einen Zaehler. nvbo->kmap ist EIN einziges ttm_bo_kmap_obj pro Puffer. ttm_bo_kmap setzt map->virtual beim Eintritt auf NULL und bildet neu ab, ohne zu pruefen, ob schon eine Abbildung existiert. ttm_bo_kunmap kehrt sofort zurueck, wenn map->virtual NULL ist. Zwei Abbildungen ergeben also: erste ioremap-Adresse geleakt, und das erste kunmap zerstoert die Abbildung, die der zweite Nutzer noch haelt. Der Pin dagegen IST gezaehlt (ttm_bo_pin/ttm_bo_unpin), deshalb faellt die Asymmetrie heute nicht auf.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.h:20 `struct ttm_bo_kmap_obj kmap;` (genau eine pro nouveau_bo); nouveau_bo.c:667-680 nouveau_bo_map, :682-689 nouveau_bo_unmap (beide ohne Zaehler); drivers/gpu/drm/ttm/ttm_bo_util.c:424-433 `int ttm_bo_kmap(...) { struct ttm_resource *res = bo->resource; ... map->virtual = NULL; map->bo = bo;`; :459-464 `void ttm_bo_kunmap(struct ttm_bo_kmap_obj *map) { if (!map->virtual) return;`; Gegenbeispiel Pin: drivers/gpu/drm/ttm/ttm_bo.c:631 `if (!bo->pin_count++ && bo->resource)` und :647-651 `if (WARN_ON_ONCE(!bo->pin_count)) return; ... if (!--bo->pin_count && bo->resource) {`.",
      "folge_fuer_den_patch": "Ein Zaehler ist Pflicht, nicht optional. Entweder ein map_count im struct nouveau_bo mit nouveau_bo_map/unmap als Refcount-Paar, oder eine getrennte Funktion (z.B. nouveau_bo_map_get/put), damit die vorhandenen Kernel-Puffer-Nutzer unveraendert bleiben. Vorbild fuer den Zaehler: drivers/gpu/drm/drm_gem_vram_helper.c:352 `if (gbo->vmap_use_count > 0)`, :367 `++gbo->vmap_use_count;`, :389-395 `if (drm_WARN_ON_ONCE(dev, !gbo->vmap_use_count)) ... if (--gbo->vmap_use_count > 0) return;`. Der von nouveau benutzte TTM-GEM-Helfer hat so etwas ausdruecklich NICHT: drm_gem_ttm_helper.c:67-91 reicht ttm_bo_vmap/vunmap ungezaehlt durch, nouveau_gem.c:225-226 `.vmap = drm_gem_ttm_vmap, .vunmap = drm_gem_ttm_vunmap,`.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(e-Konkret) Braucht es dafuer ueberhaupt einen exotischen Aufbau mit zwei Planes?",
      "antwort": "Nein. Der einfachste Ausloeser ist ein Commit, in dem neuer und alter fb derselbe sind, etwa eine Aenderung einer Plane-Eigenschaft oder ein Modeset ohne Pufferwechsel. prepare_planes laeuft ueber JEDE Plane im Zustand, unabhaengig davon ob sich der fb geaendert hat, und cleanup_planes ebenso. Ablauf mit dem naiv umgesetzten Ansatz (a): prepare_fb(neu) bildet Puffer B ein zweites Mal ab (erste ioremap-Adresse geleakt), cleanup_fb(alt) baut ab, danach zeigt plane->state auf einen Puffer mit kmap.virtual == NULL. Ergebnis: der Panic-Bildschirm ist ab dem ersten solchen Commit dauerhaft weg, plus ein vmalloc-Leck pro Commit. Zusaetzlich moeglich, aber nicht am Code bewiesen: derselbe fb auf zwei Planes gleichzeitig (Klonbetrieb ueber zwei Heads).",
      "beleg": "drm_atomic_helper.c:2832 `for_each_new_plane_in_state(state, plane, new_plane_state, i)` ohne fb-Vergleich; :3209 `for_each_old_plane_in_state(state, plane, old_plane_state, i)` ebenso; wndw.c:524-535 cleanup_fb prueft nur `if (!old_state->fb) return;`; wndw.c:549-551 prepare_fb prueft nur `if (!asyw->state.fb) return 0;`. Der Klonfall ist mechanisch moeglich, weil prepare_fb pro Plane gerufen wird und nouveau_bo_pin gezaehlt ist, ich habe aber keine Stelle gelesen, die ihn ausdruecklich erlaubt oder verbietet.",
      "folge_fuer_den_patch": "Der Zaehler muss vor der ersten Einreichung drin sein, sonst faellt der Patch schon beim normalen Desktop-Betrieb um. Fuer den Test: zweimal hintereinander denselben fb committen (z.B. drm_mode_atomic mit unveraenderter fb_id) und danach den drm_panic-Debugfs-Ausloeser bedienen (CONFIG_DRM_PANIC_DEBUG, drivers/gpu/drm/drm_panic.c:968-1000 `debugfs_trigger_write` schreibt nach drm_panic_plane_N).",
      "sicherheit": "teilweise belegt"
    },
    {
      "frage": "(Design) Ist der Ansatz, das Mapping an den Plane-Zustand zu haengen, ueberhaupt der gesegnete Weg?",
      "antwort": "Ja, ausdruecklich. Die drm_panic-Dokumentation nennt genau prepare_fb/cleanup_fb als die erlaubte Klammer, und plane->state wird in drm_atomic_helper_swap_state unter der Panic-Sperre umgehaengt, also genau der Sperre, die draw_panic_plane per trylock nimmt. Damit ist ein im Plane-Zustand abgelegter Zeiger im Panic-Kontext gefahrlos lesbar.",
      "beleg": "include/drm/drm_panic.h:115-119 `- Anything set up by &drm_plane_helper_funcs.fb_prepare and cleaned up &drm_plane_helper_funcs.fb_cleanup is safe to access, as long as it stays invariant between these two calls. This also means that for drivers using dynamic buffer management the framebuffer is pinned, and therefer all relevant datastructures can be accessed without taking any further locks`; :133-134 `#define drm_panic_trylock(dev, flags) raw_spin_trylock_irqsave(&(dev)->mode_config.panic_lock, flags)`; drm_atomic_helper.c:3352-3362 `drm_panic_lock(state->dev, flags); for_each_oldnew_plane_in_state(...) { ... plane->state = new_plane_state; } drm_panic_unlock(state->dev, flags);`; drm_panic.c:927 `if (!drm_panic_trylock(plane->dev, flags)) return;`.",
      "folge_fuer_den_patch": "Den fertigen iosys_map im Plane-Zustand ablegen (nv50_wndw_atom), nicht in nvbo->kmap nachschlagen. Der Callback macht dann nur noch iosys_map kopieren plus Geometrie fuellen, und gibt sauber auf, wenn der Zeiger leer ist. Der Zaehler aus (e) sitzt darunter im nouveau_bo und schuetzt die gemeinsame ttm-Abbildung.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(Design-Falle) Was ist beim Zustandsobjekt zu beachten?",
      "antwort": "nv50_wndw_atomic_duplicate_state benutzt kmalloc, nicht kzalloc, und setzt danach die Felder einzeln. Ein neu hinzugefuegtes Feld (etwa der iosys_map) ist dort uninitialisierter Muell, wenn es nicht ausdruecklich geloescht wird. Ausserdem erzeugt drm_atomic_helper_duplicate_state (Suspend-Pfad) Plane-Zustaende, fuer die prepare_fb nie gelaufen ist, die aber eine fb-Referenz halten.",
      "beleg": "wndw.c:729-746 nv50_wndw_atomic_duplicate_state, :733 `if (!(asyw = kmalloc_obj(*asyw))) return NULL;` und danach die Einzelzuweisungen `asyw->sema = armw->sema; ... asyw->clr.mask = 0; asyw->set.mask = 0;`; wndw.c:756-766 nv50_wndw_reset benutzt dagegen kzalloc_obj; drm_atomic_helper.c:3736-3785 drm_atomic_helper_duplicate_state.",
      "folge_fuer_den_patch": "In nv50_wndw_atomic_duplicate_state das neue Feld ausdruecklich leeren (iosys_map_clear), analog zu `asyw->ilut = NULL;`. Sonst greift der Panic-Handler nach dem ersten Suspend oder nach jedem Commit auf einen Zufallszeiger zu, und zwar im Panic-Kontext, wo das nicht mehr diagnostizierbar ist.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(Nebenbefund) Ist die io-Reservierung unter der Abbildung gezaehlt?",
      "antwort": "Nein, auf keiner Ebene. ttm_mem_io_reserve kehrt bei schon gesetztem bus.offset einfach zurueck, ttm_mem_io_free gibt bedingungslos frei. Nutzer sind sowohl ttm_bo_kmap als auch der Userspace-mmap-Fehlerpfad. Auch die nvkm-Seite zaehlt nicht: nvkm_umem_map liefert -EEXIST bei doppelter Abbildung, nvkm_umem_unmap gibt die BAR1-VMM-Reservierung genau einmal zurueck.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:49-56 und :62-75 (ttm_mem_io_reserve/ttm_mem_io_free); Nutzer: ttm_bo_util.c:439 (in ttm_bo_kmap), :505 (in ttm_bo_vmap), drivers/gpu/drm/ttm/ttm_bo_vm.c:207 `err = ttm_mem_io_reserve(bo->bdev, bo->resource);`; Freigabe: ttm_bo_util.c:478 (in ttm_bo_kunmap), :579 (in ttm_bo_vunmap), ttm_bo.c:1052-1058 ttm_bo_unmap_virtual, aufgerufen aus ttm_bo.c:133 in ttm_bo_handle_move_mem; nvkm: drivers/gpu/drm/nouveau/nvkm/subdev/mmu/umem.c:63-70 `if (!umem->map) return -EEXIST;` und :88-90 `if (umem->map) return -EEXIST;`, Speicher geteilt per union in umem.h (`union { struct nvkm_vma *bar; void *map; };`).",
      "folge_fuer_den_patch": "Der Zaehler aus (e) muss die io-Reservierung mitschuetzen, nicht nur den virtuellen Zeiger. Ein blosses Kopieren des iosys_map in den Plane-Zustand reicht nicht: das erste kunmap gaebe die BAR1-Reservierung frei, waehrend der zweite Nutzer noch darauf zeigt. Genau deshalb ist auch ttm_bo_vmap/vunmap kein Ausweg, obwohl es einen eigenen iosys_map liefert.",
      "sicherheit": "belegt"
    }
  ],
  "fazit": "Der Lebenszyklus traegt den Ansatz (a), aber nicht in der naiven Form: cleanup_fb ist fuer jedes ERFOLGREICHE prepare_fb garantiert, nicht jedoch fuer das gescheiterte (drm_atomic_helper.c:2880-2881, `if (j >= i) continue;`), und nv50_wndw_prepare_fb leckt in zwei Fehlerpfaden schon heute den Pin (wndw.c:579-581, 586-589). Der harte Blocker ist (e): nvbo->kmap ist ein einziges Objekt ohne Zaehler, ttm_bo_kmap ueberschreibt es kommentarlos und ttm_bo_kunmap gibt beim ersten Aufruf frei, waehrend der Pin sehr wohl gezaehlt ist. Schon ein ganz normaler Commit mit unveraendertem Framebuffer bildet damit doppelt ab und raeumt einfach ab, wonach der Panic-Bildschirm dauerhaft weg ist und pro Commit eine ioremap-Adresse leckt. Suspend, Resume und Modulentladen sind dagegen unkritisch, weil dort ueberall zuerst fb = NULL committet wird (nouveau_display.c:770-772, 583-585) und drm_panic_unregister vor dem Abschalten laeuft (drm_drv.c:1150-1153); nur Runtime-PM haelt fb und Pin (nouveau_display.c:770-778, ttm_bo.c:524) und laesst ein Mapping auf ein D3cold-Geraet zeigen, was der Patch weder verursacht noch loest."
}