{
  "befunde": [
    {
      "frage": "(a) Was genau muss get_scanout_buffer setzen, damit drm_panic zeichnen kann?",
      "antwort": "Vier Pflichtangaben plus eine Schreibmoeglichkeit: sb->format (nicht NULL, num_planes==1 und aus XRGB8888 konvertierbar), sb->width, sb->height, sb->pitch[0] und mindestens eines aus set_pixel, pages, map[0]. Nur bei Rueckgabe 0 wird sb ueberhaupt angesehen; bei ret != 0 bricht drm_panic vor dem format-Zugriff ab (Kurzschluss im ||), ein NULL-format kann also nur schaden, wenn man faelschlich 0 zurueckgibt. Gelesen wird der Puffer nie, drm_draw.c enthaelt keinen einzigen Lesezugriff (kein iosys_map_rd, kein memcpy_fromio), es wird ausschliesslich geschrieben, bei iomem ueber writeb/writew/writel.",
      "beleg": "drm_panic.c:923 `struct drm_scanout_buffer sb = { };` (Struktur ist genullt); :930 `ret = plane->helper_private->get_scanout_buffer(plane, &sb);`; :932 `if (ret || !drm_panic_is_format_supported(sb.format)) goto unlock;`; :936 `if (!sb.set_pixel && !sb.pages && iosys_map_is_null(&sb.map[0])) goto unlock;`; :871-876 drm_panic_is_format_supported prueft format->num_planes und drm_draw_can_convert_from_xrgb8888; Nutzung der Felder: :485/:492/:555 sb->width/height fuer Layout, :299/:303/:388/:392 sb->pitch[0] und format->cpp[0] fuer Adressrechnung; include/drm/drm_panic.h:24-79 Struktur; iosys-map.h:411-418 iosys_map_wr, :366-370 __iosys_map_wr_io (writeb/writew/writel); drm_draw.c enthaelt kein iosys_map_rd (grep leer); include/drm/drm_modeset_helper_vtables.h:1456-1460 \"The returned drm_scanout_buffer.map must be valid if no error code is returned. Return: %0 on success, negative errno on failure.\"",
      "folge_fuer_den_patch": "Die bestehende Reihenfolge in nv50_wndw_get_scanout_buffer (erst map, dann width/height/pitch/format, dann optional set_pixel) bleibt richtig. Neu ist nur: die Abbildung wird nicht mehr erzeugt, sondern uebernommen. Wichtig ist, dass der Callback bei fehlender Abbildung nicht 0 zurueckgibt, denn dann wuerden format und Masse zwar stimmen, aber ohne Schreibziel.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a-Zusatz) Reicht bei nouveau ein gesetztes set_pixel ohne map, so wie amdgpu es macht?",
      "antwort": "Nein. Beide nouveau-set_pixel-Varianten schreiben selbst durch sb->map[0]. Ein Rueckgabewert 0 mit gesetztem set_pixel, aber leerer map wuerde die Plausibilitaetspruefung in drm_panic passieren und dann in einen Schreibzugriff auf NULL+offset laufen, also einen zweiten Oops im Panic erzeugen. amdgpu darf das, weil dessen set_pixel ueber MMIO-Register schreibt und die map gar nicht anfasst.",
      "beleg": "wndw.c:632 `iosys_map_wr(&sb->map[0], off, u32, color);` (nv50_set_pixel_swizzle) und wndw.c:647 dasselbe in nv50_set_pixel; drm_panic.c:936 laesst set_pixel allein genuegen; amd/amdgpu/amdgpu_display.c:1843-1862 amdgpu_display_set_pixel schreibt mit WREG32_NO_KIQ(mmMM_INDEX/mmMM_DATA), ohne sb->map",
      "folge_fuer_den_patch": "Der Aufgabepfad \"keine Abbildung\" muss vor dem Setzen von sb->set_pixel liegen. Der Kachelpfad ist kein Ersatz fuer die fehlende Abbildung, sondern haengt an ihr.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Ist der Zugriff auf nvbo->kmap.virtual im Panic-Kontext unbedenklich, wenn die Abbildung vorher angelegt wurde? Cache und Sichtbarkeit?",
      "antwort": "Der reine Lese- und Schreibzugriff ist unbedenklich, die Verwaltung des Zeigers ist es nicht. Im Detail: (1) Kein Schlafen, kein Allozieren, nur ein Zeigerlesen, das ist unter raw_spin_trylock_irqsave, in Hard-IRQ und in NMI zulaessig. (2) Seitentabellen: auf x86-64 muss keine vmalloc-Nachsynchronisierung stattfinden, die PGD/PUD-Eintraege des vmalloc/ioremap-Bereichs sind beim Booten vorbelegt, vmalloc_fault ist ausschliesslich x86-32. Auf x86-32 waere der Nachtragspfad sperrenfrei, also im Panic ebenfalls tolerierbar. (3) Cache: nouveau bildet VRAM nur uncached oder write combining ab, nie cached. Es gibt also keine kohaerente Zweitabbildung und kein clflush ist noetig. (4) Andere CPUs sind gestoppt, bevor kmsg_dump laeuft, und Interrupts sind aus, es kann also waehrend des Zeichnens niemand mehr abbauen. (5) Genau deshalb kann der Zustand aber eingefroren inkonsistent sein: eine andere CPU kann mitten in ttm_bo_kmap gestoppt worden sein, wo bo_kmap_type schon gesetzt, virtual aber noch NULL ist. Deshalb: immer virtual auf NULL pruefen und nie annehmen, dass Typ und Zeiger zusammenpassen.",
      "beleg": "kernel/panic.c:600 `local_irq_disable();`, :673 `panic_other_cpus_shutdown(...)` vor :685 `kmsg_dump_desc(KMSG_DUMP_PANIC, buf);`; arch/x86/mm/fault.c:1153 `#ifdef CONFIG_X86_32` um den vmalloc_fault-Aufruf :1178-1181, Kommentar :1172-1176 \"On x86-64 the race does not exist as the vmalloc mappings don't need to be synchronized there\"; arch/x86/mm/init_64.c:1331-1363 preallocate_vmalloc_pages ueber VMALLOC_START..VMEMORY_END; nouveau_bo.c:1290-1295 setzt bus.caching auf ttm_uncached oder ttm_write_combined, nie ttm_cached; ttm_bo_util.c:432 `map->virtual = NULL;` vor der Abbildung, :331/:336 Typ wird vor virtual gesetzt",
      "folge_fuer_den_patch": "Der Callback darf genau zwei Dinge tun: nvbo->kmap.virtual lesen, gegen NULL pruefen, und danach den Typ nur noch fuer die Wahl zwischen iosys_map_set_vaddr_iomem und iosys_map_set_vaddr benutzen. Kein Mapping, kein Reservieren, kein Warten.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b-Kern) Ist nvbo->kmap ein sicherer Ablageort fuer die Panic-Abbildung?",
      "antwort": "Nein, das ist die schwaechste Stelle des vorgeschlagenen Entwurfs. nvbo->kmap ist ein einziger, nicht refgezaehlter Platz pro BO, den ausser dem Display auch der GEM-Pushbuf-Pfad belegt und wieder abraeumt. Der Reloc-Pfad mappt nur, wenn noch nichts da ist, und raeumt nur ab, wenn er selbst gemappt hat, dieser Fall ist also entschaerft. Umgekehrt gilt das nicht: ein unbedingtes nouveau_bo_map in prepare_fb ueberschreibt eine fremde Abbildung, die dann in validate_fini abgeraeumt wird, und laesst eine ioremap-Leiche zurueck. Verschaerfend: ttm_mem_io_free ist nicht refgezaehlt, der erste Abbau zieht ueber nouveau_ttm_io_mem_free_locked das BAR1-Handle unter jeder noch lebenden Abbildung weg.",
      "beleg": "nouveau_bo.c:668-680 nouveau_bo_map ruft ttm_bo_kmap unbedingt auf dasselbe &nvbo->kmap; nouveau_gem.c:696-704 `if (!nvbo->kmap.virtual) { ttm_bo_kmap(...); nvbo->validate_mapped = true; }` und :901-910 dasselbe im Push-Pfad; nouveau_gem.c:434-437 `if (unlikely(nvbo->validate_mapped)) { ttm_bo_kunmap(&nvbo->kmap); ... }`; ttm_bo_util.c:62-74 ttm_mem_io_free ohne Zaehler, ruft bdev->funcs->io_mem_free; nouveau_bo.c:1233-1251 nouveau_ttm_io_mem_free_locked ruft nvif_object_unmap_handle; ttm_bo_util.c:479 `map->virtual = NULL;`",
      "folge_fuer_den_patch": "Entweder einen echten Zaehler einfuehren (map nur wenn nvbo->kmap.virtual == NULL, Zaehler hoch, unmap erst bei Zaehler 0), oder die Abbildung ausserhalb von nvbo->kmap halten. Ein zweiter, unabhaengiger ttm_bo_vmap pro Plane-State loest das Problem nur scheinbar, weil auch ttm_bo_vunmap ueber ttm_mem_io_free das BAR-Handle freigibt (ttm_bo_util.c:564-580). Sauber ist genau ein Abbildungsbesitzer pro BO.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b-Folge) Haelt die in prepare_fb angelegte Abbildung ueberhaupt bis zum Panic durch?",
      "antwort": "Beim vorgeschlagenen symmetrischen Paar prepare_fb mappt, cleanup_fb entmappt: haeufig nicht. drm_atomic_helper_prepare_planes ruft prepare_fb fuer jede Plane im Commit auf, ohne Filter auf \"fb unveraendert\", cleanup_fb laeuft danach fuer den alten State. Wird derselbe Framebuffer zweimal hintereinander committet oder liegt er gleichzeitig auf zwei Planes, ergibt sich map, map, unmap. Nach dem letzten Schritt ist nvbo->kmap.virtual NULL, obwohl genau dieser Puffer angezeigt wird. Der Panic-Bildschirm faellt dann still aus, und zwar im Normalfall, nicht im Sonderfall.",
      "beleg": "drm_atomic_helper.c:2832-2851 `for_each_new_plane_in_state(...) { if (funcs->prepare_fb) ret = funcs->prepare_fb(plane, new_plane_state); ... }` ohne Vergleich mit old_plane_state->fb; wndw.c:524-535 nv50_wndw_cleanup_fb arbeitet auf old_state->fb; ttm_bo_util.c:459-481 ttm_bo_kunmap setzt virtual auf NULL; nouveau_bo.c:683-689 nouveau_bo_unmap ohne Zaehler",
      "folge_fuer_den_patch": "Punkt (a) des Vorschlags ist in der naiven Form fehlerhaft. Notwendig ist eine Zaehlung oder eine Bindung der Abbildung an das BO statt an den einzelnen Commit. Andernfalls ist der Patch schlechter als sein Ruf: er entfernt zwar den Panic-Verstoss, liefert aber in der Mehrzahl der Faelle trotzdem kein Bild.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Welche bo_kmap_type-Werte gibt es, welche koennen bei einem Scanout-Puffer auftreten, insbesondere ttm_bo_map_premapped?",
      "antwort": "Es gibt vier: ttm_bo_map_iomap (1|0x80), ttm_bo_map_vmap (2), ttm_bo_map_kmap (3), ttm_bo_map_premapped (4|0x80). Bei einem nv50-Scanout-Puffer kann praktisch nur ttm_bo_map_iomap auftreten. Begruendung Schritt fuer Schritt: prepare_fb pinnt ausschliesslich nach VRAM, fuer VRAM setzt nouveau bus.is_iomem = true, ttm_bo_kmap nimmt damit den ioremap-Zweig, und in ttm_bo_ioremap entscheidet allein mem->bus.addr zwischen premapped und iomap. nouveau setzt bus.addr nirgends auf einen Wert, ttm_resource_init initialisiert ihn auf NULL und nouveau schreibt ihn nur einmal wieder auf NULL. Also nie premapped. vmap und kmap setzen !is_iomem voraus, also Systemspeicher oder ungekacheltes TT, was der VRAM-Pin ausschliesst; kmap zusaetzlich einen Ein-Seiten-Puffer.",
      "beleg": "include/drm/ttm/ttm_bo.h:155-164 enum, :281 `#define TTM_BO_MAP_IOMEM_MASK 0x80`; wndw.c:554 `ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);`; nouveau_bo.c:1285-1288 VRAM-Zweig mit `reg->bus.is_iomem = true;`; ttm_bo_util.c:442-447 `if (!res->bus.is_iomem) return ttm_bo_kmap_ttm(...) else ... ttm_bo_ioremap(...)`; ttm_bo_util.c:318-337 ttm_bo_ioremap, :319 `if (mem->bus.addr) { map->bo_kmap_type = ttm_bo_map_premapped; ...}`; git grep bus.addr in drivers/gpu/drm/nouveau liefert nur nouveau_bo.c:1354 `nvbo->bo.resource->bus.addr = NULL;`; ttm_resource.c:350 `res->bus.addr = NULL;`; ttm_bo_util.c:353-360 kmap nur bei num_pages == 1 und ttm_cached",
      "folge_fuer_den_patch": "Die Fallunterscheidung bei wndw.c:676-679 kann so bleiben, sie ist korrekt und der else-Zweig ist nur totes, harmloses Beiwerk. Wer sie vereinfachen will, darf das, muss dann aber begruenden, dass der Puffer immer iomem ist. Fuer premapped waere ttm_bo_kunmap ohnehin ein No-op (ttm_bo_util.c:473-474), es entstuende also auch kein doppelter Abbau.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Bleiben sb->set_pixel und die statische Variable nv50_panic_blk_h mit der Aenderung korrekt?",
      "antwort": "Ja, die Aenderung beruehrt beides nicht. nv50_panic_blk_h wird weiterhin im Callback gesetzt, bevor drm_panic zeichnet, und beide Funktionen laufen im selben, durch drm_panic_trylock geschuetzten Abschnitt. Die im Kommentar behauptete Absicherung durch den drm_panic-Spinlock traegt streng genommen nur pro Geraet, die eigentliche Garantie ist, dass im Panic nur eine CPU laeuft und kmsg_dump die Dumper nacheinander abarbeitet. Einzige neue Regel: der Aufgabepfad darf nicht zwischen dem Setzen von nv50_panic_blk_h und dem Setzen von set_pixel liegen.",
      "beleg": "wndw.c:596-599 Kommentar und `static u32 nv50_panic_blk_h;`; wndw.c:693-699 Zuweisung und Auswahl von set_pixel innerhalb des Callbacks; drm_panic.c:927 drm_panic_trylock, :930 Callback, :940 draw_panic_dispatch im selben kritischen Abschnitt; kernel/panic.c:673 CPUs gestoppt vor :685 kmsg_dump_desc",
      "folge_fuer_den_patch": "Unveraendert uebernehmen. Nur die Reihenfolge im Fehlerfall pruefen: erst Abbildung pruefen und gegebenenfalls verlassen, dann Masse und Kachelparameter setzen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d-Nebenbefund) Stimmt die Blockspalten-Rechnung in nv50_set_pixel?",
      "antwort": "Nein, hier liegt ein vorhandener Fehler, unabhaengig vom Panic-Thema. nv50_get_block_off erwartet als dritten Parameter eine Bytezeilenlaenge, denn nouveau_get_width_in_blocks teilt durch 64 Byte GOB-Breite. nv50_set_pixel_swizzle uebergibt korrekt sb->pitch[0], nv50_set_pixel uebergibt sb->width, also Pixel statt Bytes. Bei 32 Bit pro Pixel ist die Spaltenzahl damit um Faktor vier zu klein. Dass die Bytelesart richtig ist, zeigt nouveau_check_bl_size, das denselben Helfer mit mode_cmd->pitches[i] fuettert und das Ergebnis mit der GOB-Groesse in Bytes multipliziert.",
      "beleg": "wndw.c:603-612 nv50_get_block_off, :607 `blk_columns = nouveau_get_width_in_blocks(pitch);`; wndw.c:621 `blk_off = nv50_get_block_off(x, y, sb->pitch[0]);` gegen wndw.c:641 `blk_off = nv50_get_block_off(x, y, sb->width);`; dispnv50/tile.h:21-24 `return DIV_ROUND_UP(stride, NV_TILE_GOB_WIDTH_BYTES);` mit :18 `#define NV_TILE_GOB_WIDTH_BYTES 64`; nouveau_display.c:239 `bw = nouveau_get_width_in_blocks(stride);` und :243 `bl_size = bw * bh * gobs_in_block * gob_size;` mit Aufruf :305-308 mit mode_cmd->pitches[i]",
      "folge_fuer_den_patch": "Als eigenen, kleinen Patch vor oder nach dem Panic-Fix vorschlagen (sb->width durch sb->pitch[0] ersetzen). Fuer die Testmaschine ist das relevant: NVAC ist Tesla mit chipset 0xac, also unter 0x160, damit genau der nv50_set_pixel-Zweig (wndw.c:696-699). Ohne diese Korrektur waere ein Panic-Bild auf gekacheltem Puffer dort auch nach dem Panic-Fix verzerrt, und der Test wuerde das Falsche zeigen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(e) Was soll der Callback tun, wenn keine Abbildung vorliegt, welcher Rueckgabewert ist richtig, und was macht drm_panic damit?",
      "antwort": "Ohne Abbildung: nichts in sb schreiben, insbesondere kein set_pixel, und einen negativen errno zurueckgeben. drm_panic wertet den Wert nur binaer aus, springt bei ret != 0 sofort zu unlock, laesst sb unberuehrt und ruft auch panic_flush nicht. Der konkrete Wert ist also reine Kosmetik und wird nirgends protokolliert; -ENODEV passt zur i915-Konvention fuer \"kein Puffer\", -ENOMEM waere die amdgpu-Konvention. Ein knappes drm_warn ist zulaessig, printk ist im Panic der Normalfall, aber WARN() oder WARN_ON() waeren falsch, weil sie im Panic einen Stack-Dump ausloesen. Andere Planes und andere Geraete sind nicht betroffen, jede Plane haengt an einem eigenen kmsg_dumper.",
      "beleg": "drm_panic.c:932 `if (ret || !drm_panic_is_format_supported(sb.format)) goto unlock;`, :942-943 panic_flush nur im Erfolgsfall, :944-947 unlock-Pfad; drm_panic.c:1036-1047 je Plane ein eigener kmsg_dumper via kmsg_dump_register; i915 display/intel_plane.c:1597-1598 `return -ENODEV;`; amdgpu_display.c:1907-1911 `drm_warn(...); return -ENOMEM;`; kernel/panic.c:634 bust_spinlocks(1) und :675 printk_legacy_allow_panic_sync(); Vorbild fuer die Struktur: i915_gem_pages.c:445-476 \"Use current vaddr if it exists, or setup a list of pages\", mit `return -EOPNOTSUPP;` am Ende",
      "folge_fuer_den_patch": "Konkret: `if (!nvbo->kmap.virtual) { drm_warn(plane->dev, \"nouveau: no kernel mapping for scanout bo, panic screen unavailable\\n\"); return -ENODEV; }` an der Stelle des heutigen nouveau_bo_map-Aufrufs (wndw.c:671-674). Damit verschwindet der Verstoss, ohne dass drm_panic je einen halbfertigen sb sieht.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(Zusatz) Fehlt nach dem Zeichnen eine Sichtbarkeitsgarantie fuer write combining, und wie testet man das Ganze ohne echten Panic?",
      "antwort": "Zwei praktische Punkte. Erstens: drm_panic setzt nach dem Zeichnen keine Schreibbarriere, es gibt nur den optionalen panic_flush-Haken, den nouveau nicht implementiert. Bei write combining koennen die letzten Schreibvorgaenge daher in den WC-Puffern der CPU stehen, wenn die Maschine unmittelbar danach stehen bleibt. Ob das in der Praxis stoert, ist offen, es ist aber ein billiger Zusatz. Zweitens: fuer den Test gibt es den debugfs-Ausloeser, der draw_panic_plane ausserhalb eines echten Panic aufruft, allerdings im Prozesskontext, also ohne CPU-Stopp und ohne IRQ-Sperre. Er belegt die Zeichenlogik, nicht die Panic-Tauglichkeit.",
      "beleg": "drm_panic.c enthaelt kein wmb und keinen Flush ausser :942-943 `if (plane->helper_private->panic_flush) plane->helper_private->panic_flush(plane);`; wndw.c:713-717 nv50_wndw_primary_helper setzt nur prepare_fb, cleanup_fb, atomic_check und get_scanout_buffer, kein panic_flush; nouveau_bo.c:1291-1295 waehlt ttm_write_combined ausser bei NVIF_MEM_UNCACHED; drm_panic.c:970-1002 CONFIG_DRM_PANIC_DEBUG, :973-983 debugfs_trigger_write ruft draw_panic_plane(plane, \"Test from debugfs\")",
      "folge_fuer_den_patch": "Optional ein winziges nv50_wndw_panic_flush mit wmb() anbieten, klar als Zusatz gekennzeichnet, damit der Review es getrennt bewerten kann. Fuer Jocelyns Test auf CONFIG_DRM_PANIC_DEBUG hinweisen und dazusagen, dass nur ein echter Panic (oder ein Panic aus IRQ-Kontext) den urspruenglichen BUG_ON-Pfad abdeckt.",
      "sicherheit": "teilweise belegt"
    }
  ],
  "fazit": "Der Callback darf im Panic ausschliesslich nvbo->kmap.virtual lesen, gegen NULL pruefen und bei Fehlen einen negativen errno liefern; alles andere (Masse, Format, Kachelparameter, set_pixel) bleibt unveraendert korrekt, und der Zugriff selbst ist im Panic unbedenklich, weil x86-64 keine vmalloc-Nachsynchronisierung braucht, nouveau VRAM nur uncached oder write combining abbildet und die anderen CPUs vor kmsg_dump gestoppt sind. Der Kachelpfad ist dabei kein Ausweg: beide nv50-set_pixel-Funktionen schreiben selbst durch sb->map[0], eine gueltige Abbildung ist also in jedem Fall Pflicht. Der vorgeschlagene Entwurf hat aber eine echte Luecke: prepare_fb wird ohne Filter auf unveraenderte Framebuffer fuer jede Plane gerufen, und nvbo->kmap ist ein einziger, nicht refgezaehlter Platz, den auch der GEM-Pushbuf-Pfad benutzt; die Folge von map, map, unmap laesst den angezeigten Puffer unabgebildet zurueck, weshalb der Patch ohne Zaehlung oder einen eigenen Abbildungsbesitzer den Verstoss zwar beseitigt, aber meist trotzdem kein Bild liefert. Nebenbefund fuer denselben Testrechner: nv50_set_pixel uebergibt sb->width statt sb->pitch[0] an eine Funktion, die eine Bytezeilenlaenge erwartet (wndw.c:641 gegen :621), was auf Tesla die Blockspalten um Faktor vier verfehlt und einen eigenen kleinen Patch verdient."
}