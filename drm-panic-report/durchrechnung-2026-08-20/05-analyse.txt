{
  "befunde": [
    {
      "frage": "(a) Wer benutzt nvbo->kmap sonst noch? Alle Stellen, die kmap bzw. nouveau_bo_map/nouveau_bo_unmap auf einem scanout-tauglichen Puffer anfassen.",
      "antwort": "Vollstaendige Liste im Basis-Commit. nvbo->kmap wird an genau sechs Stellen geschrieben: nouveau_bo_map() und nouveau_bo_unmap() (nouveau_bo.c:668, :683), der drm_panic-Callback (wndw.c:671, :676-679), und der Userspace-Pushbuf-Pfad in nouveau_gem.c an drei Stellen (:434-436 kunmap, :696-704 kmap fuer Relocations, :901-909 kmap fuer den Suffix-Patch). Gelesen wird es zusaetzlich von nouveau_bo_rd16/rd32/wr32 (nouveau_bo.c:803, :817, :831), von nvbo_kmap_obj_iovirtual() (nouveau_bo.h:100-107) und von nouveau_chan.c:174, :440-456. Von diesen fasst NUR nouveau_gem.c einen Puffer an, der zugleich ein Scanout-Puffer sein kann: dort ist nvbo der vom Userspace per GEM-Handle benannte Reloc-Container bzw. Push-Puffer, und nichts hindert den Userspace daran, dafuer den fb-BO zu nennen. Alle uebrigen nouveau_bo_map-Aufrufer arbeiten auf treibereigenen BOs, die nie Scanout-Puffer werden: dispnv04-Cursor (crtc.c:1003, :1322 via nouveau_bo_new_map; disp.c:92, :150 ueber Suspend/Resume), disp->sync (dispnv50/disp.c:2853), Push-Puffer und Semaphoren (nouveau_chan.c:165, :195), Fence-BOs (nv17_fence.c:133, nv50_fence.c:84, nv84_fence.c:224), nouveau_dmem (nouveau_dmem.c:328).\n\nDie gute Nachricht: die drei nouveau_gem-Stellen sind bereits defensiv geschrieben. Beide kmap-Stellen pruefen zuerst 'if (!nvbo->kmap.virtual)' (nouveau_gem.c:696, :901) und setzen validate_mapped nur, wenn sie selbst abgebildet haben; validate_fini_no_ticket() ruft ttm_bo_kunmap() ausschliesslich unter 'if (unlikely(nvbo->validate_mapped))' (nouveau_gem.c:434-437). Eine vorbestehende Abbildung wird also weiterbenutzt und nicht abgeraeumt. Die umgekehrte Reihenfolge ist durch die Reservierung ausgeschlossen: validate_fini_no_ticket() ruft ttm_bo_kunmap() VOR ttm_bo_unreserve() (nouveau_gem.c:435 vs :441), und nouveau_bo_map() nimmt ttm_bo_reserve() (nouveau_bo.c:672). Solange also beide Seiten die Reservierung halten, kollidieren sie nicht.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_gem.c:696-704: 'if (!nvbo->kmap.virtual) { ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); if (ret) { NV_PRINTK(err, cli, \"failed kmap for reloc\\n\"); break; } nvbo->validate_mapped = true; }' | drivers/gpu/drm/nouveau/nouveau_gem.c:434-441: 'if (unlikely(nvbo->validate_mapped)) { ttm_bo_kunmap(&nvbo->kmap); nvbo->validate_mapped = false; } list_del(&nvbo->entry); nvbo->reserved_by = NULL; ttm_bo_unreserve(&nvbo->bo);' | drivers/gpu/drm/nouveau/nouveau_bo.c:668-681: 'ret = ttm_bo_reserve(&nvbo->bo, false, false, NULL); ... ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ttm_bo_unreserve(&nvbo->bo);'",
      "folge_fuer_den_patch": "Der Pushbuf-Pfad selbst ist vertraeglich, dort ist kein Umbau noetig. ABER: nouveau_bo_unmap() nimmt KEINE Reservierung (nouveau_bo.c:683-689, nur ttm_bo_kunmap). Ein neuer Aufruf in nv50_wndw_cleanup_fb() liefe also unsynchronisiert gegen nouveau_gem.c:696/:901, die unter Reservierung nvbo->kmap.virtual lesen und benutzen. Konkret: CPU A in ttm_bo_kunmap() hat map->virtual schon gelesen und iounmap() begonnen, aber map->virtual noch nicht auf NULL gesetzt (ttm_bo_util.c:460-479, die Nullung steht erst in :479); CPU B im Pushbuf sieht virtual != NULL und schreibt in die gerade freigegebene Abbildung. Der Patch MUSS deshalb unter Reservierung entpinnen und entmappen, also eine Variante nouveau_bo_unmap_locked() einfuehren oder das Entmappen in den bereits reservierten Abschnitt von nouveau_bo_unpin() ziehen. Ein blosses nouveau_bo_unmap() vor nouveau_bo_unpin() in cleanup_fb ist falsch.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Ist nouveau_bo_unmap() sicher, wenn der Puffer nicht abgebildet ist oder jemand anderes ihn abgebildet hat? Was passiert bei doppeltem kunmap?",
      "antwort": "Zwei getrennte Antworten.\n\nNicht abgebildet und doppeltes kunmap: harmlos. nouveau_bo_unmap() faengt nvbo == NULL ab und ruft sonst ttm_bo_kunmap(). ttm_bo_kunmap() beginnt mit 'if (!map->virtual) return;' und endet mit 'map->virtual = NULL; map->page = NULL;'. Ein zweites kunmap ist damit ein No-op. Ein nie abgebildeter nvbo hat kmap.virtual == 0, weil nouveau_bo struct-zero alloziert wird und ttm_bo_kmap() ausserdem als erstes 'map->virtual = NULL' setzt (ttm_bo_util.c:428). Es gibt also keinen BUG und keinen doppelten iounmap. Beachte allerdings, dass ttm_bo_kunmap() bo_kmap_type NICHT zuruecksetzt; das ist unschaedlich, weil der Typ nur unter der virtual-Pruefung ausgewertet wird, aber wndw.c:676 liest 'nvbo->kmap.bo_kmap_type' OHNE virtual zu pruefen, was nach einem kunmap einen Stale-Typ liefert. Ein Patch muss also zuerst virtual pruefen und erst dann den Typ.\n\nWenn jemand anderes abgebildet hat: NICHT sicher, und das ist der eigentliche Sprengsatz. ttm_bo_kunmap() ruft am Ende ttm_mem_io_free(map->bo->bdev, map->bo->resource) (ttm_bo_util.c:478). ttm_mem_io_free() ist NICHT zaehlend: es prueft nur 'if (!mem->bus.offset && !mem->bus.addr) return;' und reisst dann bedingungslos ab (ttm_bo_util.c:62-76). Genauso ist ttm_mem_io_reserve() nicht zaehlend, sondern nur idempotent ueber 'if (mem->bus.offset || mem->bus.addr) return 0;' (ttm_bo_util.c:49-52). Auf nouveau-Seite haengt daran nvkm_umem_unmap(), das ohne jeden Refcount nvkm_vmm_put(nvkm_bar_bar1_vmm(device), &umem->bar) macht (umem.c:63-83), und nvkm_umem_map() weigert sich mit -EEXIST, ein zweites Handle auszugeben (umem.c:94-95). Es gibt also pro nvkm_umem genau EINE BAR1-Abbildung ohne Refcount, und der erste, der loslaesst, nimmt sie allen weg.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:459-481: 'void ttm_bo_kunmap(struct ttm_bo_kmap_obj *map) { if (!map->virtual) return; switch (map->bo_kmap_type) { case ttm_bo_map_iomap: iounmap(map->virtual); break; ... } ttm_mem_io_free(map->bo->bdev, map->bo->resource); map->virtual = NULL; map->page = NULL; }' | drivers/gpu/drm/ttm/ttm_bo_util.c:49-76: 'int ttm_mem_io_reserve(...) { if (mem->bus.offset || mem->bus.addr) return 0; ... } void ttm_mem_io_free(...) { if (!mem) return; if (!mem->bus.offset && !mem->bus.addr) return; if (bdev->funcs->io_mem_free) bdev->funcs->io_mem_free(bdev, mem); mem->bus.offset = 0; mem->bus.addr = NULL; }' | drivers/gpu/drm/nouveau/nvkm/subdev/mmu/umem.c:63-83 und :94-95: 'if (umem->map) return -EEXIST;' bzw. 'nvkm_vmm_put(nvkm_bar_bar1_vmm(device), &umem->bar);' | drivers/gpu/drm/nouveau/nouveau_bo.c:683-689: 'void nouveau_bo_unmap(struct nouveau_bo *nvbo) { if (!nvbo) return; ttm_bo_kunmap(&nvbo->kmap); }'",
      "folge_fuer_den_patch": "Der Patch darf sich nicht darauf verlassen, dass eine einmal angelegte nvbo->kmap-Abbildung bis zum eigenen Entmappen gueltig bleibt. Solange ttm_mem_io_reserve/ttm_mem_io_free nicht zaehlen, kann jeder fremde vmap/vunmap-Zyklus auf demselben BO die BAR1-Unterlage unter der Abbildung wegziehen (siehe Befund (c)). Entweder wird die Zaehlung in nouveau_ttm_io_mem_reserve/free nachgeruestet (nouveau_bo.c:1233-1246 und :1253ff, mit eigenem Zaehler unter drm->ttm.io_reserve_mutex), oder der Callback muss die Gueltigkeit anders absichern. Zusaetzlich: in wndw.c erst 'if (!nvbo->kmap.virtual) return -ENODEV;' pruefen und erst danach bo_kmap_type auswerten, weil kunmap den Typ stehen laesst.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) fbcon/fbdev-Emulation: bildet die den Scanout-Puffer schon ab, und kollidiert das?",
      "antwort": "nouveau benutzt drm_fbdev_ttm (nouveau_drm.c:37 inkludiert drm/drm_fbdev_ttm.h). Der Konsolinhalt liegt in einem vmalloc-Schattenpuffer im Systemspeicher, nicht im Scanout-Puffer: drm_fbdev_ttm_driver_fbdev_probe() macht 'screen_buffer = vzalloc(screen_size)' und haengt das an info->screen_buffer (drm_fbdev_ttm.c:200-210), mit fb_deferred_io bei HZ/20 (:214-221). Der Scanout-Puffer wird nur beim Damage-Blit angefasst, und zwar mit einer KURZLEBIGEN Abbildung: drm_fbdev_ttm_damage_blit() ruft drm_client_buffer_vmap_local(buffer, &map), blittet und ruft sofort drm_client_buffer_vunmap_local(buffer) (drm_fbdev_ttm.c:126-133).\n\nDieser Zyklus ist die Kollision. drm_client_buffer_vmap_local() geht ueber drm_gem_vmap_locked() (drm_client.c:290-308), das obj->funcs->vmap direkt und OHNE Refcount aufruft (drm_gem.c:1413-1429). nouveau setzt .vmap = drm_gem_ttm_vmap / .vunmap = drm_gem_ttm_vunmap (nouveau_gem.c:225-226), also landet man in ttm_bo_vmap()/ttm_bo_vunmap(). ttm_bo_vmap() ruft ttm_mem_io_reserve() (ttm_bo_util.c:505) und findet bei einer schon bestehenden Abbildung bus.offset gesetzt vor, macht also kein neues Handle, sondern nur ein zweites ioremap_wc auf dieselbe Adresse (:513-522). ttm_bo_vunmap() dagegen ruft am Ende bedingungslos ttm_mem_io_free() (ttm_bo_util.c:579), und das reisst das gemeinsame BAR1-Handle ab, siehe Befund (b). Der Dokumentationstext sagt das sogar selbst: 'Client buffer mappings are not ref'counted' (drm_client.c:277-280).\n\nErgebnis: mit einer dauerhaften nvbo->kmap-Abbildung aus prepare_fb zeigt nvbo->kmap.virtual nach dem ersten Konsolen-Blit auf einen BAR1-Bereich, dessen BAR1-VMM-Eintrag per nvkm_vmm_put freigegeben wurde. Der Zeiger sieht gueltig aus, kmap.virtual ist non-NULL, bo_kmap_type sagt weiterhin iomap, aber die Schreibzugriffe im Panic gehen ins Leere oder in fremdes VRAM, sobald der BAR1-Bereich neu vergeben wurde. Genau der Zustand, in dem man panickt, naemlich Konsole aktiv, ist der Zustand, in dem der Blit gelaufen ist. Das ist die schwerwiegendste Regression des Ansatzes (a).",
      "beleg": "drivers/gpu/drm/drm_fbdev_ttm.c:126-133: 'ret = drm_client_buffer_vmap_local(buffer, &map); if (ret) goto out; dst = map; drm_fbdev_ttm_damage_blit_real(fb_helper, clip, &dst); drm_client_buffer_vunmap_local(buffer);' | drivers/gpu/drm/drm_fbdev_ttm.c:199-203: 'screen_size = buffer->gem->size; screen_buffer = vzalloc(screen_size);' | drivers/gpu/drm/drm_client.c:277-280 (Kommentar): 'Client buffer mappings are not ref'counted. Each call to drm_client_buffer_vmap_local() should be closely followed by a call to drm_client_buffer_vunmap_local().' | drivers/gpu/drm/drm_gem.c:1413-1429: 'ret = obj->funcs->vmap(obj, map);' ohne Zaehler | drivers/gpu/drm/nouveau/nouveau_gem.c:225-226: '.vmap = drm_gem_ttm_vmap, .vunmap = drm_gem_ttm_vunmap,' | drivers/gpu/drm/ttm/ttm_bo_util.c:505 'ret = ttm_mem_io_reserve(bo->bdev, mem);' und :579 'ttm_mem_io_free(bo->bdev, mem);'",
      "folge_fuer_den_patch": "Ansatz (a) in der vorgeschlagenen Form ist ohne Zusatzarbeit nicht tragfaehig. Es reicht NICHT, nur die Verdraengung aus dem io_reserve_lru (Punkt 7 der Aufgabenstellung) abzusichern: der io_reserve_lru-Pfad ist der seltene, der fbcon-vunmap-Pfad ist der haeufige und trifft jede Maschine mit aktiver Textkonsole, unabhaengig von BAR1-Groesse. Erforderlich ist eine Zaehlung der BAR1-Reservierung. Sauberster Ort: ein Zaehler im nouveau_mem bzw. in der ttm_resource, den nouveau_ttm_io_mem_reserve() hochzaehlt und nouveau_ttm_io_mem_free_locked() herunterzaehlt, wobei nvif_object_unmap_handle() erst bei Null faellt (nouveau_bo.c:1233-1246). Das ist ohnehin die Reparatur, die Punkt 7 der Aufgabenstellung verlangt; sie deckt beide Faelle ab. Alternativ, und deutlich kleiner: die Abbildung gar nicht dauerhaft halten, sondern nur den drm_panic-Callback panic-tauglich machen, indem man die BAR1-Basisadresse plus nvbo->offset direkt bestimmt und im Callback nichts mappt. Das ist aber ein anderer Patch als (a) und muss gegen ttm_bo_ioremap()-Semantik geprueft werden.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a, Fortsetzung) Kann das zusaetzliche Map/Unmap in prepare_fb/cleanup_fb sich selbst kaputtmachen, weil prepare_fb und cleanup_fb nicht paarig auf demselben Puffer laufen?",
      "antwort": "Ja, und das ist ein zweiter, vom BAR1-Thema unabhaengiger Defekt. drm_atomic_helper_prepare_planes() ruft funcs->prepare_fb() fuer JEDEN neuen Plane-State (drm_atomic_helper.c:2832-2840), drm_atomic_helper_cleanup_planes() ruft funcs->cleanup_fb() fuer jeden ALTEN Plane-State (drm_atomic_helper.c:3206-3214). Es gibt keinerlei Vergleich 'alter fb == neuer fb'. Wird derselbe Framebuffer in zwei aufeinanderfolgenden Commits benutzt, was bei stehendem Bild, bei Cursor-only-Updates und bei jedem Konsolenbetrieb der Normalfall ist, dann laeuft prepare_fb(fbX) ein zweites Mal, bevor cleanup_fb(fbX aus dem alten State) laeuft.\n\nnouveau_bo_pin() vertraegt das, weil ttm_bo_pin() den pin_count hochzaehlt und nouveau_bo_pin_locked() den Fall 'if (nvbo->bo.pin_count)' explizit behandelt (nouveau_bo.c:562-583). nouveau_bo_map() vertraegt es NICHT: ttm_bo_kmap() setzt bedingungslos 'map->virtual = NULL; map->bo = bo;' und bildet neu ab (ttm_bo_util.c:428-449). Die erste ioremap-Abbildung wird damit ueberschrieben und ist fuer immer verloren; sie wird auch beim BO-Destroy nicht eingesammelt, denn nouveau_bo_del_ttm() ruft kein ttm_bo_kunmap (nouveau_bo.c:135-155). Danach macht cleanup_fb(altes fbX) das eine kunmap, das die ZWEITE Abbildung freigibt, und setzt kmap.virtual auf NULL. Das Endergebnis ist doppelt schlecht: pro Wiederholungs-Commit ein Leck von einer kompletten Framebuffer-groessen ioremap-Flaeche, und der Panic-Callback findet kmap.virtual == NULL vor, also gerade keinen Panic-Bildschirm. Dasselbe Muster trifft einen Framebuffer, der gleichzeitig auf zwei Primary-Planes liegt (geklonte Ausgabe).",
      "beleg": "drivers/gpu/drm/drm_atomic_helper.c:2832-2840: 'for_each_new_plane_in_state(state, plane, new_plane_state, i) { ... if (funcs->prepare_fb) { ret = funcs->prepare_fb(plane, new_plane_state); ...' | drivers/gpu/drm/drm_atomic_helper.c:3206-3214: 'for_each_old_plane_in_state(state, plane, old_plane_state, i) { const struct drm_plane_helper_funcs *funcs = plane->helper_private; if (funcs->cleanup_fb) funcs->cleanup_fb(plane, old_plane_state); }' kein fb-Vergleich | drivers/gpu/drm/ttm/ttm_bo_util.c:428-430: 'map->virtual = NULL; map->bo = bo;' ohne Pruefung auf bestehende Abbildung | drivers/gpu/drm/nouveau/nouveau_bo.c:562-583: 'if (nvbo->bo.pin_count) { ... ttm_bo_pin(&nvbo->bo); goto out; }' (Pin zaehlt, kmap nicht) | drivers/gpu/drm/nouveau/nouveau_bo.c:135-155 nouveau_bo_del_ttm() ohne ttm_bo_kunmap",
      "folge_fuer_den_patch": "Das Map/Unmap muss selbst zaehlen oder idempotent sein. Minimale Form: in prepare_fb 'if (!nvbo->kmap.virtual) nouveau_bo_map(nvbo);' und in cleanup_fb nur entmappen, wenn der BO nicht noch pinned bleibt, also erst nouveau_bo_unpin_locked() und dann 'if (!nvbo->bo.pin_count) ttm_bo_kunmap(&nvbo->kmap);', alles innerhalb einer Reservierung. Damit haengt die Lebensdauer der Abbildung am pin_count, der bereits korrekt zaehlt, und die Doppel-prepare-Faelle heilen von selbst. Achtung: dann darf der Pushbuf-Pfad die Abbildung nicht mit seinem eigenen validate_mapped-Zyklus verwechseln; das tut er nicht, weil er nur bei kmap.virtual == NULL selbst mappt (nouveau_gem.c:696, :901).",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a, Fortsetzung) Was kostet ein Map/Unmap pro Atomic-Commit an Laufzeit?",
      "antwort": "Wenn Map und Unmap wirklich bei jedem Commit laufen, also ohne die pin_count-Kopplung aus dem vorigen Befund, dann pro Seitenwechsel: ein ioremap_wc ueber die volle BO-Groesse (ttm_bo_ioremap(), ttm_bo_util.c:314-338, mit size = num_pages << PAGE_SHIFT ueber PFN_UP(nvbo->bo.base.size) aus nouveau_bo.c:676) und ein iounmap. Bei 1920x1200x4 sind das 2250 Seiten Seitentabellenarbeit plus vmalloc-Bereichsverwaltung plus die TLB-Aufraeumung beim Freigeben, und zwar bei 60 Hz sechzigmal pro Sekunde. Zusaetzlich nimmt nouveau_bo_map() ttm_bo_reserve() ein zweites Mal, obwohl prepare_fb ueber nouveau_bo_pin() schon einmal reserviert und wieder freigegeben hat (nouveau_bo.c:643-651 und :672-679). Das ist reiner Zusatzaufwand auf dem Flip-Pfad, der bisher nur einen Pin-Zaehler kostet.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:676: 'ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap);' | drivers/gpu/drm/ttm/ttm_bo_util.c:445-448: 'offset = start_page << PAGE_SHIFT; size = num_pages << PAGE_SHIFT; return ttm_bo_ioremap(bo, offset, size, map);' | drivers/gpu/drm/ttm/ttm_bo_util.c:325-334: 'resource_size_t res = mem->bus.offset + offset; map->bo_kmap_type = ttm_bo_map_iomap; if (mem->bus.caching == ttm_write_combined) map->virtual = ioremap_wc(res, size);' | drivers/gpu/drm/nouveau/nouveau_bo.c:643-651 nouveau_bo_pin() mit eigenem ttm_bo_reserve/unreserve",
      "folge_fuer_den_patch": "Zusaetzliches Argument fuer die Kopplung an pin_count statt an den Commit: mit 'nur mappen, wenn noch nicht abgebildet' und 'nur entmappen, wenn pin_count auf Null faellt' faellt der Aufwand auf einmal pro Framebuffer-Lebensdauer statt einmal pro Frame. Ausserdem gehoert das Mappen dann in den bereits reservierten Abschnitt, also besser nouveau_bo_pin_locked/unpin_locked erweitern oder in prepare_fb eine gemeinsame Reservierung um Pin und Map legen, statt zweimal zu reservieren.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Wieviel BAR1-Fenster kostet eine dauerhafte Abbildung eines typischen Framebuffers, und ab welcher Karte wird das eng?",
      "antwort": "Die Kosten entsprechen exakt der BO-Groesse, denn nouveau_ttm_io_mem_reserve() laesst ueber nvif_object_map_handle() eine BAR1-VMM-Flaeche in genau dieser Groesse anlegen (nouveau_bo.c:1327-1340, umem.c:107-113 ueber mmu->func->mem.umap), und der BAR1-VMM-Adressraum ist so gross wie das PCI-BAR1 selbst: nv50_bar_oneinit() nimmt 'size = device->func->resource_size(device, NVKM_BAR1_FB)' und baut daraus die VMM (bar/nv50.c:168-176); gf100_bar_oneinit_bar() genauso mit bar_len (bar/gf100.c:96-102).\n\nKonkrete Zahlen, ungetiltes 32 bpp, echte BOs sind wegen Blocklinear-Aufrundung eher groesser:\n1920x1080x4 = 8.294.400 B = 7,91 MiB (2025 Seiten)\n1920x1200x4 = 9.216.000 B = 8,79 MiB (2250 Seiten)\n2560x1440x4 = 14.745.600 B = 14,06 MiB (3600 Seiten)\n3840x2160x4 = 33.177.600 B = 31,64 MiB (8100 Seiten)\n\nMultiplikatoren: der Panic-Callback haengt nur am Primary (wndw.c:891-894 setzt nv50_wndw_primary_helper nur bei DRM_PLANE_TYPE_PRIMARY, und nur dieser Helper hat .get_scanout_buffer, wndw.c:712-717). Es gibt eine Primary-Plane pro Head (head.c:611 uebergibt &base->plane als Primary an drm_crtc_init_with_planes). Also N Heads gleich N dauerhafte Abbildungen. Vier Heads mit 4K sind 126,6 MiB. Waehrend eines Commits kommt kurzzeitig der Faktor zwei dazu, weil prepare_fb(neu) vor cleanup_fb(alt) laeuft, bei 4K also 63,3 MiB fuer einen einzigen Head.\n\nWann wird es eng: die Zielmaschine (BAR1 256 MiB, VRAM 256 MiB, laut Aufgabenstellung gemessen) merkt bei 1920x1200 nichts, das sind 3,4 Prozent. Eng wird es bei VRAM groesser BAR1, und dass nouveau diese Konfiguration ausdruecklich unterstuetzt, steht im Code: der ganze io_reserve_lru-Verdraengungsmechanismus in nouveau_ttm_io_mem_reserve() existiert nur dafuer (nouveau_bo.c:1342-1356), und fuer Vor-Tesla begrenzt nouveau_ttm_fault_reserve_notify() die VRAM-Platzierung sogar hart auf 'mappable = resource_size(NVKM_BAR1_FB) >> PAGE_SHIFT' (nouveau_bo.c:1378, :1395-1405).\n\nEntscheidend und schlimmer als die reine Zahl: die neue dauerhafte Abbildung ist fuer die Verdraengung UNSICHTBAR. Auf die io_reserve_lru kommt ein BO nur ueber den Userspace-mmap-Fehlerpfad, naemlich nouveau_bo_add_io_reserve_lru() aus nouveau_ttm_fault() (nouveau_gem.c:57-60); nouveau_bo_map() traegt nichts ein (nouveau_bo.c:668-681). Der Panic-Framebuffer belegt BAR1-Adressraum, den der -ENOSPC-Pfad niemals zurueckholen kann, und verkleinert damit den reklaimierbaren Rest dauerhaft. Umgekehrt ist er dadurch auch vor der Verdraengung geschuetzt, was Punkt 7 der Aufgabenstellung teilweise entschaerft.\n\nKonkrete BAR1-Groessen fremder Karten kann ich aus dem Baum nicht belegen, das ist eine PCI-Eigenschaft. Fuer die Zielmaschine gilt der in der Aufgabenstellung genannte Messwert 256 MiB.",
      "beleg": "drivers/gpu/drm/nouveau/nvkm/subdev/bar/nv50.c:168-176: 'start = 0x0000000000ULL; size = device->func->resource_size(device, NVKM_BAR1_FB); if (!size) return -ENOMEM; limit = start + size; ret = nvkm_vmm_new(device, start, limit-- - start, NULL, 0, &bar1_lock, \"bar1\", &bar->bar1_vmm);' | drivers/gpu/drm/nouveau/nvkm/subdev/bar/gf100.c:96-102: 'bar_len = device->func->resource_size(device, bar_id); ... ret = nvkm_vmm_new(device, 0, bar_len, NULL, 0, key, (bar_id == NVKM_BAR2_INST) ? \"bar2\" : \"bar1\", &bar_vm->vmm);' | drivers/gpu/drm/nouveau/nouveau_bo.c:1342-1356 (io_reserve_lru-Verdraengung bei -ENOSPC) | drivers/gpu/drm/nouveau/nouveau_bo.c:1378: 'u32 mappable = device->func->resource_size(device, NVKM_BAR1_FB) >> PAGE_SHIFT;' | drivers/gpu/drm/nouveau/nouveau_gem.c:57-60: 'nouveau_bo_del_io_reserve_lru(bo); prot = vm_get_page_prot(vma->vm_flags); ret = ttm_bo_vm_fault_reserved(vmf, prot, TTM_BO_VM_NUM_PREFAULT); nouveau_bo_add_io_reserve_lru(bo);' | drivers/gpu/drm/nouveau/dispnv50/wndw.c:891-894 und :712-717",
      "folge_fuer_den_patch": "Der Verbrauch ist auf Karten mit BAR1 groesser gleich VRAM unkritisch und braucht dort keine Sonderbehandlung. Fuer VRAM groesser BAR1 sollte der Patch bewusst entscheiden: entweder den Panic-Puffer explizit auf die io_reserve_lru setzen und im Callback pruefen, ob bus.offset noch steht (dann kann er verdraengt werden, aber der Callback merkt es), oder ihn bewusst draussen lassen und im Commit-Message klar sagen, dass pro Head eine Framebuffer-Groesse BAR1 dauerhaft gebunden wird. Die Pruefung im Callback muss ohnehin sein: 'if (!nvbo->kmap.virtual) return -ENODEV;'. Zusaetzlich sollte der Patch die Abbildung auf die Primary-Plane beschraenken, was durch nv50_wndw_primary_helper ohnehin gegeben ist, aber prepare_fb ist fuer ALLE Planes derselbe Callback; ein 'if (plane->type != DRM_PLANE_TYPE_PRIMARY) skip map' spart Overlays und Cursor.",
      "sicherheit": "teilweise belegt"
    },
    {
      "frage": "(e) Betrifft die Aenderung auch dispnv04? Hat die ueberhaupt einen get_scanout_buffer?",
      "antwort": "Nein und nein. get_scanout_buffer kommt im gesamten nouveau-Verzeichnis genau einmal vor, naemlich in dispnv50/wndw.c:651 (Definition) und :716 (Eintrag in nv50_wndw_primary_helper). In dispnv04 gibt es keinen einzigen Treffer, und auch kein drm_panic.h-Include. Der Grund liegt tiefer: dispnv04 ruft nirgends drm_plane_helper_add() auf. Die Primary-Plane wird mit __drm_universal_plane_alloc() ohne Helper angelegt (dispnv04/crtc.c:1306-1311), die Overlays ebenfalls ohne Helper (dispnv04/overlay.c:299, :479). Damit ist plane->helper_private NULL, und drm_panic_register() ueberspringt solche Planes explizit: 'if (!plane->helper_private || !plane->helper_private->get_scanout_buffer) continue;' (drm_panic.c:1036-1037). Auf nv04-nv40-Hardware registriert drm_panic also null Planes, es gibt dort weder den Fehler noch die Fixflaeche.\n\nEine indirekte Beruehrung bleibt: dispnv04 ist der einzige weitere nouveau_bo_map/nouveau_bo_unmap-Nutzer im Anzeigepfad, naemlich fuer den Cursor-BO (crtc.c:1003 und :1012, disp.c:92 und :150 im Suspend/Resume-Paar, crtc.c:1322 nouveau_bo_new_map, crtc.c:771 nouveau_bo_unpin_del). Wenn der Patch die Signatur oder Semantik von nouveau_bo_map/nouveau_bo_unmap aendert, etwa durch eine neue reservierte Variante oder eine Zaehlung, muessen diese vier Stellen mitgezogen werden. Der Cursor-BO ist ein treibereigener 64x64x4-Puffer aus nouveau_bo_new_map() und niemals ein Scanout-Framebuffer, also fachlich unkritisch.",
      "beleg": "git grep -n 'get_scanout_buffer' c21bb4193868 -- drivers/gpu/drm/nouveau/ liefert nur dispnv50/wndw.c:596 (Kommentar), :651, :716 | drivers/gpu/drm/nouveau/dispnv04/crtc.c:1306-1311: 'primary = __drm_universal_plane_alloc(dev, sizeof(*primary), 0, 0, &nv04_primary_plane_funcs, modeset_formats, ARRAY_SIZE(modeset_formats), NULL, DRM_PLANE_TYPE_PRIMARY, NULL);' danach nur drm_crtc_helper_add(), kein drm_plane_helper_add() | drivers/gpu/drm/drm_panic.c:1036-1037: 'if (!plane->helper_private || !plane->helper_private->get_scanout_buffer) continue;' | git grep -n 'drm_plane_helper_add' c21bb4193868 -- drivers/gpu/drm/nouveau/dispnv04 liefert nichts | drivers/gpu/drm/nouveau/dispnv04/crtc.c:1003, :1012, :1322; dispnv04/disp.c:92, :150",
      "folge_fuer_den_patch": "Der Patch kann sich vollstaendig auf dispnv50/wndw.c beschraenken; dispnv04 braucht keine Aenderung und keinen Test. Falls jedoch eine reservierungsnehmende Variante nouveau_bo_unmap_locked() eingefuehrt wird (siehe Befund (a)), sind die dispnv04-Aufrufer mitzupruefen: disp.c:92 laeuft im nv04_display_fini-Kontext und disp.c:150 im nv04_display_init-Kontext, beide ohne gehaltene Reservierung, sie muessen also weiter die reservierungsnehmende Aussenfassung benutzen. nouveau_bo_unmap() darf deshalb nicht ersatzlos umgebaut, sondern nur um eine _locked-Variante ergaenzt werden.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(Regressionsflaeche, Zusatz) Gibt es weitere Stellen, an denen eine dauerhafte Abbildung stale werden kann, ohne dass der Callback es merkt?",
      "antwort": "Zwei, beide belegbar.\n\nErstens BO-Zerstoerung: ttm_bo_release() ruft ttm_mem_io_free(bdev, bo->resource) (ttm_bo.c:274) und ttm_bo_unmap_virtual() ruft drm_vma_node_unmap() plus ttm_mem_io_free() (ttm_bo.c:1050-1058). Beide reissen die BAR1-Unterlage ab, ohne nvbo->kmap anzufassen, und nouveau_bo_del_ttm() macht ebenfalls kein ttm_bo_kunmap (nouveau_bo.c:135-155). Der Panic-Callback kommt hier praktisch nicht in Gefahr, weil er ueber plane->state->fb geht und der Framebuffer eine Referenz auf den GEM haelt, aber eine geleckte Abbildung aus dem Doppel-prepare-Fall wird nie eingesammelt.\n\nZweitens BO-Verschiebung: nouveau_bo_move_ntfy() macht kein ttm_bo_kunmap (nouveau_bo.c:1065-1090). Hier rettet uns der Pin: prepare_fb pinnt vor dem Mappen nach VRAM (wndw.c:554, nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true)), und ttm_bo_pin() verhindert die Evakuierung. Solange Map und Unmap streng innerhalb des Pin-Fensters liegen, ist die Abbildung gegen Verschiebung sicher. Das ist ein starkes Argument dafuer, die Abbildung genau an den Pin zu koppeln und nicht an den Commit.\n\nEin dritter Punkt, der KEIN Problem ist, aber leicht als solches missverstanden wird: der Pushbuf-Pfad kann ueber nouveau_bo_wr32() (nouveau_gem.c:912) in unsere Abbildung schreiben. Das ist semantisch identisch zur eigenen Abbildung, die er sonst angelegt haette, und gibt dem Userspace keine neue Faehigkeit, weil er den BO ohnehin selbst kmappen koennte. Kein Sicherheitsproblem.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo.c:274: 'ttm_mem_io_free(bdev, bo->resource);' im Release-Pfad, direkt nach 'drm_vma_offset_remove(bdev->vma_manager, &bo->base.vma_node);' | drivers/gpu/drm/ttm/ttm_bo.c:1050-1058: 'void ttm_bo_unmap_virtual(struct ttm_buffer_object *bo) { struct ttm_device *bdev = bo->bdev; drm_vma_node_unmap(&bo->base.vma_node, bdev->dev_mapping); ttm_mem_io_free(bdev, bo->resource); }' | drivers/gpu/drm/nouveau/nouveau_bo.c:1065-1090 nouveau_bo_move_ntfy() ohne ttm_bo_kunmap | drivers/gpu/drm/nouveau/dispnv50/wndw.c:554: 'ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);' | drivers/gpu/drm/nouveau/nouveau_gem.c:912: 'nouveau_bo_wr32(nvbo, (push[i].offset + push[i].length - 8) / 4, cmd);'",
      "folge_fuer_den_patch": "Bestaetigt die Empfehlung aus Befund (a, Fortsetzung): Lebensdauer der Abbildung an pin_count koppeln, Map nach dem Pin, Unmap vor dem Unpin, beides unter einer gemeinsamen Reservierung. Damit sind Verschiebung und Zerstoerung ausgeschlossen. Es bleibt allein das Refcount-Problem aus (b) und (c), das ausserhalb dieser Kopplung geloest werden muss.",
      "sicherheit": "belegt"
    }
  ],
  "fazit": "Der Ansatz (a) ist in der vorgeschlagenen Form nicht tragfaehig, und zwar aus einem Grund, der in der Aufgabenstellung noch nicht auftaucht: die fbdev-Emulation macht bei jedem Konsolen-Blit ein drm_client_buffer_vmap_local/vunmap_local auf genau diesen Scanout-BO (drm_fbdev_ttm.c:126-133), und ttm_bo_vunmap() ruft am Ende bedingungslos ttm_mem_io_free() (ttm_bo_util.c:579), das ueber nvkm_umem_unmap() die BAR1-Flaeche ohne jeden Refcount per nvkm_vmm_put freigibt (umem.c:63-83). Eine dauerhafte nvbo->kmap-Abbildung wird dadurch bei laufender Textkonsole regelmaessig stale, ohne dass kmap.virtual auf NULL geht. Das ist der haeufige Fall, waehrend die in Punkt 7 genannte io_reserve_lru-Verdraengung der seltene ist; eine Zaehlung in nouveau_ttm_io_mem_reserve/free heilt beide. Zwei weitere Defekte kommen dazu: drm_atomic_helper ruft prepare_fb(neu) ohne fb-Vergleich vor cleanup_fb(alt) (drm_atomic_helper.c:2832-2840, :3206-3214), sodass ein wiederholt benutzter Framebuffer bei jedem Commit eine ioremap-Flaeche leckt und danach unabgebildet dasteht, und nouveau_bo_unmap() nimmt keine Reservierung (nouveau_bo.c:683-689), laeuft also unsynchronisiert gegen den Pushbuf-Pfad, der nvbo->kmap.virtual unter Reservierung liest (nouveau_gem.c:696, :901). Beides loest sich, wenn man die Abbildung an den pin_count koppelt statt an den Commit und Map wie Unmap in den reservierten Abschnitt zieht. Entwarnung gibt es bei (e): dispnv04 hat gar keinen get_scanout_buffer und ruft nirgends drm_plane_helper_add, drm_panic_register ueberspringt dort jede Plane; und bei (d): der BAR1-Verbrauch ist mit 8,79 MiB pro Head bei 1920x1200 und 31,64 MiB bei 3840x2160 auf der Zielmaschine (BAR1 256 MiB) unkritisch, aber die neue Abbildung landet nie auf der io_reserve_lru und ist deshalb auf Karten mit VRAM groesser BAR1 dauerhaft nicht reklaimierbar."
}