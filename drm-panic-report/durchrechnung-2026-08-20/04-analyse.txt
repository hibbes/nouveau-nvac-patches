{
  "befunde": [
    {
      "frage": "(a) ast: Wie wird die Abbildung angelegt, wie lange lebt sie?",
      "antwort": "ast bildet gar keinen BO ab, sondern die gesamte VRAM-Apertur des PCI-BARs einmal beim Probe. Der Panic-Callback rechnet nur einen Plane-Offset drauf. Lebensdauer = Geraetelebensdauer (devm), Anleger = ast_mm_init() im Probe-Pfad.",
      "beleg": "drivers/gpu/drm/ast/ast_mm.c:78-94: base = pci_resource_start(pdev, 0); ... ast->vram = devm_ioremap_wc(dev->dev, base, vram_size);\ndrivers/gpu/drm/ast/ast_mode.c:491-495: void __iomem *ast_plane_vaddr(struct ast_plane *ast_plane) { ... return ast->vram + ast_plane->offset; }\ndrivers/gpu/drm/ast/ast_mode.c:625-639: get_scanout_buffer -> iosys_map_set_vaddr_iomem(&sb->map[0], ast_plane_vaddr(ast_plane)); return 0;\ndrivers/gpu/drm/ast/ast_drv.h:124-129: struct ast_plane { struct drm_plane base; u64 offset; unsigned long size; };",
      "folge_fuer_den_patch": "Das Muster ist \"eine feste Abbildung, im Callback nur lesen\". Es setzt voraus, dass VRAM linear im BAR sichtbar ist. Genau diese Voraussetzung fehlt nouveau ab Tesla (siehe Befund zu nvif_object_map_handle), deshalb ist die woertliche Uebernahme nicht moeglich.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a) mgag200: gleiche Frage",
      "antwort": "Identisches Muster wie ast, sogar ohne Plane-Offset: die Abbildung ist ein devm_ioremap(_wc) des VRAM-Resource beim Probe, der Callback setzt sb->map[0] auf den VRAM-Anfang.",
      "beleg": "drivers/gpu/drm/mgag200/mgag200_drv.c:149-157: mdev->vram_res = res; mdev->vram = devm_ioremap(dev->dev, res->start, resource_size(res)); ... mdev->vram = devm_ioremap_wc(...)\ndrivers/gpu/drm/mgag200/mgag200_mode.c:549-564: struct iosys_map map = IOSYS_MAP_INIT_VADDR_IOMEM(mdev->vram); ... sb->map[0] = map; return 0;",
      "folge_fuer_den_patch": "Bestaetigt die Regel: kein einziger der etablierten VRAM-Treiber legt im Panic-Kontext etwas an. Als Vorbild fuer nouveau taugt es aus demselben Grund wie ast nicht.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a) xe: Wie loest der einzige Nutzer von ttm_bo_kmap_try_from_panic den VRAM-Fall?",
      "antwort": "xe benutzt ttm_bo_kmap_try_from_panic ausschliesslich fuer den Systemspeicher-Fall. Fuer VRAM greift es auf vram->mapping zurueck, ein devm_ioremap_wc des LMEM-BARs aus dem Probe-Pfad, und addiert per xe_res_cursor den Offset. Liegt der Puffer in nicht CPU-sichtbarem VRAM, gibt xe im Setup sauber -ENODEV zurueck.",
      "beleg": "drivers/gpu/drm/xe/display/xe_panic.c:56-70: if (xe_bo_is_vram(bo)) { struct xe_vram_region *vram = xe_bo_device(bo)->mem.vram; ... iosys_map_set_vaddr_iomem(&panic->vmap, vram->mapping + panic->res.start); } else { ... ttm_bo_kmap_try_from_panic(&bo->ttm, new_page) }\ndrivers/gpu/drm/xe/display/xe_panic.c:86-96: if (xe_bo_is_vram(bo) && !xe_bo_is_visible_vram(bo)) return -ENODEV;\ndrivers/gpu/drm/xe/xe_vram.c:59: lmem_bar->mapping = devm_ioremap_wc(&pdev->dev, lmem_bar->io_start, lmem_bar->io_size);\ndrivers/gpu/drm/xe/xe_vram.c:245: vram->mapping = lmem_bar->mapping + offset;\ndrivers/gpu/drm/xe/xe_vram_types.h:60-61: /** @mapping: pointer to VRAM mappable space */ void __iomem *mapping;",
      "folge_fuer_den_patch": "Zwei uebertragbare Ideen: erstens set_pixel() darf im Panic-Kontext eine bestehende Abbildung durchreichen und Offsets rechnen, zweitens ein sauberes -ENODEV, wenn keine Abbildung verfuegbar ist, ist der akzeptierte Ausweg. Das rechtfertigt den \"sonst sauberes Aufgeben\"-Zweig im Vorschlag.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(a) Gibt es ein Gegenbeispiel, also einen Treiber der im Callback abbildet?",
      "antwort": "Ja, amdgpu. Es ruft ttm_bo_kmap() direkt aus dem Panic-Callback, wenn der BO noch nicht abgebildet ist. Das ist derselbe Verstoss wie in nouveau. Fuer BOs mit AMDGPU_GEM_CREATE_NO_CPU_ACCESS umgeht amdgpu das Problem stattdessen mit indirektem MMIO ueber mmMM_INDEX/mmMM_DATA und braucht dafuer gar keine Abbildung.",
      "beleg": "drivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1906-1913: if (!abo->kmap.virtual && ttm_bo_kmap(&abo->tbo, 0, PFN_UP(abo->tbo.base.size), &abo->kmap)) { drm_warn(...); return -ENOMEM; }\ndrivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1843-1861: amdgpu_display_set_pixel() ... WREG32_NO_KIQ(mmMM_INDEX, ...); WREG32_NO_KIQ(mmMM_DATA, color);\ndrivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1893-1905: if (abo->flags & AMDGPU_GEM_CREATE_NO_CPU_ACCESS) { ... sb->set_pixel = amdgpu_display_set_pixel; panic_abo = abo; return 0; }",
      "folge_fuer_den_patch": "amdgpu ist kein Vorbild, sondern ein zweiter Fundort desselben Fehlers. Umgekehrt ist der amdgpu-Zweig mit dem indirekten Fenster das Vorbild fuer die abbildungsfreie Variante (siehe PRAMIN-Befund). Der Hinweis auf amdgpu gehoert in die Patch-Beschreibung, damit Jocelyn Falempe den zweiten Fall kennt.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Warum laesst sich das ast/mgag200/xe-Muster nicht eins zu eins auf nouveau uebertragen?",
      "antwort": "Weil VRAM ab Tesla nicht linear in BAR1 liegt. nouveau_ttm_io_mem_reserve() besorgt fuer jeden BO einzeln ein BAR1-Fenster ueber nvif_object_map_handle(), und nv50_mem_map() implementiert das als nvkm_vmm_get() im BAR1-VMM. Es gibt also keine treiberweite Abbildung, die man durchreichen koennte, jede Abbildung ist per BO und dynamisch.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:1310-1332: ret = nvif_object_map_handle(&mem->mem.object, &args, argc, &handle, &length); ... reg->bus.offset = handle;\ndrivers/gpu/drm/nouveau/nvkm/subdev/mmu/memnv50.c:58-63: ret = nvkm_vmm_get(bar, 12, size, pvma); ... *paddr = device->func->resource_addr(device, NVKM_BAR1_FB) + (*pvma)->addr;\ndrivers/gpu/drm/nouveau/nouveau_bo.c:1285-1289 (Pre-Tesla-Zweig): reg->bus.offset = (reg->start << PAGE_SHIFT) + device->func->resource_addr(device, NVKM_BAR1_FB);",
      "folge_fuer_den_patch": "Eine dauerhafte Abbildung muss in nouveau zwangslaeufig pro BO sein. Damit erbt man die gesamte Lebensdauer-Problematik von nvif_object_map_handle, die es bei ast/mgag200/xe gar nicht gibt. Das ist der eigentliche Kern der Aufgabe, nicht der Panic-Kontext.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Ist prepare_fb die richtige Stelle, und was koennen die Alternativen?",
      "antwort": "prepare_fb ist unter den genannten Kandidaten die beste Stelle, aber nur mit zwei Zusatzarbeiten. Bewertung im Einzelnen: (1) prepare_fb: laeuft im schlafbaren Kontext, ist bereits die Stelle, an der nach VRAM gepinnt wird, und ist symmetrisch zu cleanup_fb. Nachteil: wird pro Plane und pro Commit gerufen, also mehrfach fuer denselben BO. (2) Beim Anlegen des Framebuffers (nouveau_framebuffer_new): laeuft, bevor der BO nach VRAM gepinnt ist, der BO kann zu diesem Zeitpunkt noch in GART oder System liegen und spaeter verschoben werden, die Abbildung waere also falsch oder muesste bei jedem Move nachgezogen werden. Zusaetzlich wuerde jeder Client-FB Abbildungen belegen, auch solche, die nie auf einen Schirm kommen. (3) In nouveau_bo_pin(): zieht die Abbildung in eine allgemeine Speicherfunktion, die auch Kanal-, Notifier- und Cursor-BOs pinnt, und wuerde BAR1-Fenster fuer Objekte belegen, die nie CPU-Zugriff brauchen. (4) In .atomic_update: laeuft unter drm_atomic_helper_commit_tail, wo Fehler nicht mehr zurueckgemeldet werden koennen, und teilweise in Kontexten, in denen ioremap unerwuenscht ist. (5) Beim Registrieren des Panic-Handlers (drm_panic_register): dort existiert noch kein Framebuffer, drm_panic_register laeuft einmal pro Geraet ueber alle Planes.",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:538-592 prepare_fb, darin :553-556 ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);\ndrivers/gpu/drm/nouveau/dispnv50/wndw.c:524-535 cleanup_fb mit nouveau_bo_unpin(nvbo);\ndrivers/gpu/drm/drm_atomic_helper.c:2832-2840: for_each_new_plane_in_state(state, plane, new_plane_state, i) { ... ret = funcs->prepare_fb(plane, new_plane_state); }\ndrivers/gpu/drm/nouveau/nouveau_display.c:256-329 nouveau_framebuffer_new: nur drm_helper_mode_fill_fb_struct + drm_framebuffer_init, kein Pin, kein Ort fuer eine BAR-Abbildung\ndrivers/gpu/drm/nouveau/nouveau_bo.c:544-616 nouveau_bo_pin_locked (allgemeine Pin-Funktion, von Kanal- und Cursor-Pfaden mitbenutzt, siehe nouveau_bo.c:424-436 nouveau_bo_new_pin)\ndrivers/gpu/drm/drm_panic.c:1030-1052 drm_panic_register: laeuft ueber alle Planes des Geraets, ohne Framebuffer-Bezug",
      "folge_fuer_den_patch": "prepare_fb/cleanup_fb bleiben. Die zwei Zusatzarbeiten sind ein Zaehler fuer die Abbildung und der Schutz der io_mem-Reservierung, beide in den folgenden Befunden.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Haelt eine in prepare_fb angelegte Abbildung ueberhaupt? Wer kann sie zerstoeren?",
      "antwort": "Nein, sie haelt nicht. Es gibt in TTM an keiner Stelle einen Refcount auf die io_mem-Reservierung: ttm_mem_io_reserve() kehrt sofort zurueck, wenn bus.offset schon gesetzt ist, und sowohl ttm_bo_kunmap() als auch ttm_bo_vunmap() rufen ttm_mem_io_free() bedingungslos. nouveau haengt daran nvif_object_unmap_handle(), das das BAR1-Fenster freigibt. Der fbdev-Pfad von nouveau bildet genau denselben BO bei jedem Damage-Flush kurz ab und wieder ab. Das heisst: jede Konsolenausgabe wuerde das in prepare_fb geholte BAR1-Fenster wieder wegnehmen, waehrend nvbo->kmap.virtual weiter auf ein totes ioremap zeigt. Der Panic-Callback wuerde dann still ins Leere schreiben, was schlimmer ist als ein sauberes Aufgeben.",
      "beleg": "drivers/gpu/drm/ttm/ttm_bo_util.c:49-60: int ttm_mem_io_reserve(...) { if (mem->bus.offset || mem->bus.addr) return 0; ... }\ndrivers/gpu/drm/ttm/ttm_bo_util.c:471-482 ttm_bo_kunmap: ... ttm_mem_io_free(map->bo->bdev, map->bo->resource); map->virtual = NULL;\ndrivers/gpu/drm/ttm/ttm_bo_util.c:564-580 ttm_bo_vunmap: ... else if (!mem->bus.addr) iounmap(map->vaddr_iomem); ... ttm_mem_io_free(bo->bdev, mem);\ndrivers/gpu/drm/nouveau/nouveau_bo.c:1233-1246 nouveau_ttm_io_mem_free_locked: case TTM_PL_VRAM: nvif_object_unmap_handle(&mem->mem.object);\ndrivers/gpu/drm/nouveau/nvkm/subdev/mmu/umem.c:63-82 nvkm_umem_unmap: nvkm_vmm_put(nvkm_bar_bar1_vmm(device), &umem->bar); (kein Zaehler)\nAufrufersuche fuer den fbdev-Pfad: drivers/gpu/drm/nouveau/nouveau_drm.c:1386 DRM_FBDEV_TTM_DRIVER_OPS und nouveau_drm.c:904 drm_client_setup(drm->dev, format); drivers/gpu/drm/drm_fbdev_ttm.c:196 fb_helper->fb = buffer->fb; drivers/gpu/drm/drm_fbdev_ttm.c:122-129 ret = drm_client_buffer_vmap_local(buffer, &map); ... drm_client_buffer_vunmap_local(buffer); drivers/gpu/drm/drm_client.c:290-327 vmap_local/vunmap_local auf buffer->fb->obj[0]; drivers/gpu/drm/nouveau/nouveau_gem.c:225-226 .vmap = drm_gem_ttm_vmap, .vunmap = drm_gem_ttm_vunmap",
      "folge_fuer_den_patch": "Der Vorschlag (a) aus dem Kontext ist ohne Zusatz unvollstaendig, und zwar aus einem anderen Grund als der dort genannte Punkt 7. Der Patch braucht einen Refcount auf die io_mem-Reservierung, am sinnvollsten nouveau-lokal in nouveau_ttm_io_mem_reserve/free (Zaehler in struct nouveau_mem), sonst zerstoert die Konsole die Panic-Abbildung. Alternativ die Abbildung ganz an TTM vorbei fuehren, was aber dieselbe Fenster-Lebensdauer nicht loest.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Ist die im Kontext genannte Verdraengung (Punkt 7) wirklich das Risiko?",
      "antwort": "Nein, jedenfalls nicht fuer den Scanout-BO selbst. Auf die io_reserve_lru kommt ein BO ausschliesslich ueber den Userspace-mmap-Fehlerpfad, und der Scanout-BO steht dort nicht drauf, kann also nicht als Verdraengungsopfer gewaehlt werden. Das reale Risiko ist das im vorigen Befund gezeigte ttm_mem_io_free ohne Refcount.",
      "beleg": "Aufrufersuche ueber alle Fundstellen von io_reserve_lru in drivers/gpu/drm/nouveau/: nouveau_gem.c:57 und :60 (nouveau_ttm_fault, also mmap-Fehlerpfad) sind die einzigen Aufrufer von nouveau_bo_add_io_reserve_lru; nouveau_bo.c:144 (nouveau_bo_del_ttm) und nouveau_bo.c:1078 (nouveau_bo_move_ntfy) rufen nur die del-Variante; nouveau_bo.c:763-780 sind die Helfer selbst; nouveau_bo.c:1345-1353 ist der -ENOSPC-Verdraengungspfad; nouveau_ttm.c:343 initialisiert die Liste.",
      "folge_fuer_den_patch": "Die im Vorschlag geplante \"Absicherung gegen die Verdraengung\" adressiert das falsche Problem. Sie kann entfallen, wenn stattdessen die io_mem-Reservierung gezaehlt wird. Wer den Punkt trotzdem in der Patch-Beschreibung erwaehnt, sollte ihn korrekt als \"betrifft nur ueber mmap angefasste BOs\" darstellen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Reicht ein einfaches nouveau_bo_map() in prepare_fb, oder braucht es einen Zaehler?",
      "antwort": "Es braucht einen Zaehler. nouveau_bo_map() hat keinen, und ttm_bo_kmap() ueberschreibt map->virtual ohne Pruefung. Da prepare_fb pro Plane und pro Commit laeuft, gibt es zwei konkrete Schadfaelle: erstens ein Framebuffer, der gleichzeitig an zwei Primary-Planes haengt (Klon-Betrieb ueber zwei Heads), fuehrt zu zwei nouveau_bo_map() auf denselben BO, das zweite verliert das erste ioremap, und das erste cleanup_fb hebt die Abbildung auf, waehrend die andere Plane noch scannt. Zweitens ein Flip auf denselben Framebuffer erzeugt prepare_fb(neu) und cleanup_fb(alt) fuer denselben BO. Der Pin ist gegen beides immun, weil ttm_bo_pin() zaehlt, die Abbildung nicht.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:667-681 nouveau_bo_map: ttm_bo_reserve(); ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ttm_bo_unreserve(); (kein Zaehler)\ndrivers/gpu/drm/nouveau/nouveau_bo.c:682-690 nouveau_bo_unmap: ttm_bo_kunmap(&nvbo->kmap); (bedingungslos)\ndrivers/gpu/drm/ttm/ttm_bo_util.c:425-449 ttm_bo_kmap: map->virtual = NULL; map->bo = bo; ... return ttm_bo_ioremap(bo, offset, size, map); (keine Pruefung auf bestehende Abbildung)\ndrivers/gpu/drm/nouveau/nouveau_bo.c:583-585 nouveau_bo_pin_locked: if (nvbo->bo.pin_count) { ... ttm_bo_pin(&nvbo->bo); goto out; } (der Pin zaehlt)\ndrivers/gpu/drm/drm_atomic_helper.c:2832-2840 prepare_fb pro Plane-State",
      "folge_fuer_den_patch": "Der Patch braucht entweder einen kmap-Zaehler in struct nouveau_bo (analog zu bo.pin_count) oder ein Paar nouveau_bo_map_ref/unmap_ref. nouveau hat fuer das Idiom \"pinnen und abgebildet halten\" bereits Vorbilder (nouveau_bo_new_map, nouveau_bo_unpin_del), aber ohne Zaehler.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Lohnt es, nur die Plane abzubilden, die drm_panic tatsaechlich benutzt? Ist die feststellbar?",
      "antwort": "Die Frage stellt sich in dieser Form nicht. drm_panic waehlt keine Plane aus, sondern haengt an jede Plane mit get_scanout_buffer einen eigenen kmsg_dumper, und im Panic zeichnet jeder dieser Dumper unabhaengig. In nouveau bekommen nur Primary-Planes den Callback, also eine pro Head, und Planes ohne Framebuffer steigen im Callback sofort mit -EINVAL aus. Wichtiger noch: die Abbildung haengt in jedem Entwurf am BO, nicht an der Plane, und der BO folgt dem Framebuffer. Eine Einschraenkung auf \"die eine Plane\" waere also weder feststellbar (welcher Head im Panic aktiv sein wird, weiss man vorher nicht) noch wirksam, weil sich die Kosten nach BOs und nicht nach Planes bemessen.",
      "beleg": "drivers/gpu/drm/drm_panic.c:1036-1048: drm_for_each_plane(plane, dev) { if (!plane->helper_private || !plane->helper_private->get_scanout_buffer) continue; plane->kmsg_panic.dump = drm_panic; ... kmsg_dump_register(&plane->kmsg_panic);\ndrivers/gpu/drm/drm_panic.c:920-949 draw_panic_plane, gerufen aus drm_panic() (:955-962) fuer den jeweiligen Dumper\ndrivers/gpu/drm/nouveau/dispnv50/wndw.c:891-894: if (type == DRM_PLANE_TYPE_PRIMARY) drm_plane_helper_add(&wndw->plane, &nv50_wndw_primary_helper); else drm_plane_helper_add(&wndw->plane, &nv50_wndw_helper);\ndrivers/gpu/drm/nouveau/dispnv50/wndw.c:705-720: nur nv50_wndw_primary_helper hat .get_scanout_buffer\ndrivers/gpu/drm/nouveau/dispnv50/wndw.c:661-662: if (!plane->state || !plane->state->fb) return -EINVAL;",
      "folge_fuer_den_patch": "Keine Sonderlogik einbauen. Die natuerliche Begrenzung ergibt sich von selbst: hoechstens ein zusaetzlich abgebildeter BO pro aktivem Head, und Cursor- und Overlay-Planes sind ohnehin nicht betroffen. Falls man die Kosten weiter druecken will, waere die einzige saubere Einschraenkung, nur fuer Primary-Planes abzubilden, was durch die Helper-Trennung bereits gegeben ist.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Geht sb->pages fuer den VRAM-Fall?",
      "antwort": "Nein. Der VRAM-Manager von nouveau ab Tesla setzt kein use_tt, es gibt also gar keine ttm_tt und damit keine struct pages fuer VRAM-BOs. Entsprechend gibt ttm_bo_kmap_try_from_panic() fuer iomem NULL zurueck. Der einzige Weg zu sb->pages waere, den Scanout-Puffer nicht mehr nach VRAM zu pinnen, was prepare_fb explizit tut und was den Scanout brechen wuerde.",
      "beleg": "drivers/gpu/drm/nouveau/nouveau_ttm.c:180-195 nouveau_ttm_init_vram: man->func = &nouveau_vram_manager; ttm_resource_manager_init(...); ohne man->use_tt (das use_tt = true steht in nouveau_ttm.c:237 im GART-Pfad)\ndrivers/gpu/drm/ttm/ttm_bo_util.c:396-405: if (!bo->resource->bus.is_iomem && bo->ttm->pages && bo->ttm->pages[page]) return kmap_local_page_try_from_panic(...); return NULL;\ninclude/drm/drm_panic.h:43-50: @pages ... The panic code will use kmap_local_page_try_from_panic()\ndrivers/gpu/drm/nouveau/dispnv50/wndw.c:554: ret = nouveau_bo_pin(nvbo, NOUVEAU_GEM_DOMAIN_VRAM, true);",
      "folge_fuer_den_patch": "sb->pages ist fuer nv50 endgueltig ausgeschlossen und braucht im Patch nur eine Zeile Begruendung, keine Implementierung.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Gibt es einen Weg voellig ohne dauerhafte Abbildung?",
      "antwort": "Ja, einen ernstzunehmenden: das PRAMIN-Fenster in BAR0. Der Registerbereich BAR0 ist einmal beim Geraeteaufbau ioremapped und lebt so lange wie das Geraet, nvkm_wr32 ist ein blankes iowrite32_native darauf, und das Fenster wird durch einen einzigen Registerschreibvorgang verschoben. Die VRAM-Adresse des Scanout-Puffers liegt bereits vor: nvbo->offset ist reg->start << PAGE_SHIFT, und reg->start stammt aus mem->mem.addr, also demselben Adressraum, den nv50_instobj mit dem Fenster anspricht. Damit liesse sich ein set_pixel() bauen, das gar nichts abbildet, das auch bei VRAM groesser BAR1 funktioniert und dem niemand etwas entziehen kann. Das ist genau die Bauart, die amdgpu fuer nicht CPU-zugreifbare Puffer verwendet.",
      "beleg": "drivers/gpu/drm/nouveau/nvkm/engine/device/base.c:3173: device->pri = ioremap(mmio_base, mmio_size);\ndrivers/gpu/drm/nouveau/include/nvkm/core/device.h:130: #define nvkm_wr32(d,a,v) iowrite32_native((v), (d)->pri + (a))\ndrivers/gpu/drm/nouveau/nvkm/subdev/instmem/nv50.c:398-401: nv50_instmem_set_bar0_window_addr(...) { nvkm_wr32(device, 0x001700, addr >> 16); }\ndrivers/gpu/drm/nouveau/nvkm/subdev/instmem/nv50.c:57-72: Basis- und Offsetmasken 0xffffff00000 / 0x000000fffff, Zugriff ueber nvkm_wr32(device, 0x700000 + addr, data) unter spin_lock_irqsave(&imem->base.lock, flags)\ndrivers/gpu/drm/nouveau/nvkm/subdev/instmem/nv50.c:424 und drivers/gpu/drm/nouveau/nvkm/subdev/instmem/gh100.c:9-20: die beiden Implementierungen von set_bar0_window_addr\ndrivers/gpu/drm/nouveau/nvkm/subdev/instmem/priv.h:19: void (*set_bar0_window_addr)(struct nvkm_device *, u64 addr);\ndrivers/gpu/drm/nouveau/nouveau_bo.c:1098: nvbo->offset = (new_reg->start << PAGE_SHIFT);\ndrivers/gpu/drm/nouveau/nouveau_mem.c:159: reg->start = mem->mem.addr >> PAGE_SHIFT;",
      "folge_fuer_den_patch": "Drei Kosten sind zu nennen. Erstens ist das Fenster 1 MiB gross, ein Full-HD-Puffer braucht also mehrere Fensterwechsel, was fuer einen Panic-Schirm unkritisch ist. Zweitens teilt sich instmem dieses Fenster und cachet die Basis in imem->addr unter einem spin_lock_irqsave, ein Panic-Schreiber darf diesen Lock nicht nehmen und muesste den Cache danach invalidieren, wofuer nv50_instmem_fini (nv50.c:409-413, imem->addr = ~0ULL) das Muster liefert. Drittens gibt es heute keinen nvkm-Nutzer ausserhalb von instmem, es braeuchte also eine kleine neue nvkm-Schnittstelle. Ob wirklich jeder nv50-und-neuer-Chipsatz nv50_instmem verwendet, habe ich nicht Chipsatz fuer Chipsatz geprueft: OFFEN.",
      "sicherheit": "teilweise belegt"
    },
    {
      "frage": "(d) Waere eine im Voraus reservierte feste Abbildung nur fuer den Panic-Fall besser?",
      "antwort": "Offen, aber unattraktiv. Man koennte beim Geraeteaufbau ein BAR1-Fenster fester Groesse reservieren und dauerhaft ioremappen, muesste es aber im Panic auf die aktuellen VRAM-Seiten des Framebuffers umbiegen. Das hiesse Seitentabellen des BAR1-VMM im Panic-Kontext beschreiben plus BAR-Flush. Ob nvkm_vmm_map dabei ohne schlafende Sperren auskommt, habe ich nicht geprueft, und es waere gegenueber dem PRAMIN-Fenster ohnehin der aufwendigere Weg fuer dasselbe Ziel.",
      "beleg": "drivers/gpu/drm/nouveau/nvkm/subdev/mmu/memnv50.c:58-64: nvkm_vmm_get(bar, 12, size, pvma) und nvkm_memory_map(memory, 0, bar, *pvma, &uvmm, sizeof(uvmm)) als die beiden Schritte, die im Panic zu wiederholen waeren",
      "folge_fuer_den_patch": "Nicht verfolgen. Falls doch, muesste zuerst die Sperren-Analyse von nvkm_vmm_map nachgeholt werden.",
      "sicherheit": "offen"
    },
    {
      "frage": "(e) Ist Ansatz (a) aus dem Kontext der beste? Wenn nein, welcher?",
      "antwort": "Die Richtung stimmt, die Ausfuehrung im Kontext ist unvollstaendig. Empfehlung ist eine Serie aus zwei Teilen. Teil 1, klein und rueckportierbar: nouveau_bo_map() aus nv50_wndw_get_scanout_buffer() entfernen und nur noch eine bereits vorhandene nvbo->kmap benutzen, sonst -ENODEV. Das behebt den gemeldeten Verstoss vollstaendig und ist genau der Weg, den xe im nicht sichtbaren VRAM-Fall geht. Fuer sich allein liefert er allerdings fast nie einen Panic-Schirm, weil den Scanout-BO heute niemand dauerhaft abbildet: die einzigen anderen nouveau_bo_map-Aufrufer sind der dispnv04-Cursor und nouveau_bo_new_map. Teil 2, der eigentliche Funktionsteil: in prepare_fb abbilden und in cleanup_fb wieder freigeben, aber mit einem Zaehler fuer die Abbildung und mit einem Refcount auf die io_mem-Reservierung in nouveau_ttm_io_mem_reserve/free, sonst nimmt der fbdev-Damage-Pfad das BAR1-Fenster wieder weg. Die abbildungsfreie PRAMIN-Variante ist die robustere Loesung und die einzige, die auch bei VRAM groesser BAR1 traegt, aber sie ist neue nvkm-Schnittstelle und gehoert eher als Folgeserie aufgesetzt.",
      "beleg": "drivers/gpu/drm/nouveau/dispnv50/wndw.c:671-679: if (nouveau_bo_map(nvbo)) { drm_warn(...); return -ENOMEM; } if (nvbo->kmap.bo_kmap_type & TTM_BO_MAP_IOMEM_MASK) iosys_map_set_vaddr_iomem(&sb->map[0], (void __iomem *)nvbo->kmap.virtual);\nAufrufersuche nouveau_bo_map in drivers/gpu/drm/nouveau/: dispnv04/crtc.c:1003, dispnv04/disp.c:150, dispnv50/wndw.c:671, nouveau_bo.c:449 (nouveau_bo_new_map). Kein Aufrufer bildet einen Display-Framebuffer dauerhaft ab.\ndrivers/gpu/drm/xe/display/xe_panic.c:92-93 als Vorbild fuer das saubere -ENODEV\ndrivers/gpu/drm/nouveau/nouveau_bo.c:439-456 nouveau_bo_new_map als vorhandenes Pin-und-Abbilden-Idiom",
      "folge_fuer_den_patch": "Zwei Patches in einer Serie einreichen. Patch 1 mit Fixes-Tag auf den Commit, der get_scanout_buffer in wndw.c eingefuehrt hat, Patch 2 als Funktionsergaenzung. In der Beschreibung von Patch 1 den amdgpu-Zweitfund erwaehnen, damit Jocelyn Falempe ihn getrennt aufgreifen kann.",
      "sicherheit": "belegt"
    }
  ],
  "fazit": "Kein einziger etablierter VRAM-Treiber bildet im Panic-Callback etwas ab: ast, mgag200, bochs, sysfb und xe reichen eine beim Probe angelegte, geraetelange ioremap-Abbildung durch, xe benutzt ttm_bo_kmap_try_from_panic nur fuer Systemspeicher und gibt bei nicht sichtbarem VRAM sauber -ENODEV zurueck. Einziges Gegenbeispiel ist amdgpu, das denselben Verstoss enthaelt wie nouveau. Das ast-Muster laesst sich nicht woertlich uebernehmen, weil VRAM ab Tesla nicht linear in BAR1 liegt, sondern pro BO ueber nvif_object_map_handle und nvkm_vmm_get abgebildet wird; eine dauerhafte Abbildung muss in nouveau also pro BO sein. Der im Kontext vermutete Risikopunkt (Verdraengung ueber die io_reserve_lru) trifft den Scanout-BO nachweislich nicht, dafuer ein anderer: weder ttm_mem_io_reserve noch nvkm_umem_unmap zaehlen, und der fbdev-Damage-Pfad von nouveau vmapped und vunmapped denselben BO bei jedem Flush, wodurch eine in prepare_fb geholte Abbildung still entwertet wuerde. Empfohlen wird deshalb eine Zweiteilung: erst der kleine, rueckportierbare Patch, der nouveau_bo_map aus dem Callback entfernt, dann der Funktionsteil in prepare_fb mit Zaehler fuer die Abbildung und Refcount auf die io_mem-Reservierung; die abbildungsfreie PRAMIN-Variante ueber das BAR0-Fenster ist die robustere Fernloesung und als Folgeserie sinnvoll."
}