{
  "treffer": true,
  "schwere": "hoch",
  "beschreibung": "Die zentrale Invariante des Entwurfs (\"Lebensdauer der Abbildung = Lebensdauer des Pins\") haelt nicht. Die Entscheidung ABZUBILDEN faellt pro Plane (nv50_wndw_needs_panic_map), die Entscheidung ABZUBAUEN faellt am globalen bo->pin_count. Der pin_count wird aber auch von Pinnern hochgezaehlt, die nie abbilden und nie abbauen. Damit kann der letzte Unpin von einem fremden Pinner kommen, ttm_bo_kunmap() laeuft nie, und nvbo->kmap.virtual plus nvbo->kmap_bus_offset ueberleben auf einem UNGEPINNTEN BO.\n\nZwei belegte Fremdpinner auf genau demselben BO:\n(1) Nicht-Primary-Planes. Cursor und Overlay benutzen dasselbe prepare_fb/cleanup_fb, aber nv50_wndw_helper hat kein get_scanout_buffer. Derselbe GEM-BO als Primary-fb auf Head 0 und als Overlay-fb (zweite fb-ID auf demselben Handle genuegt) ergibt: pin_map (pin=1, kmap=X, kmap_bus_offset=X), plain pin (pin=2), Primary-cleanup (pin=1, kein kunmap, korrekt), Overlay-cleanup ueber nouveau_bo_unpin (pin=0, KEIN kunmap).\n(2) dma-buf. nouveau_gem_prime_pin/unpin gehen direkt auf nouveau_bo_pin_locked/unpin_locked, ohne jede kmap-Behandlung. Verschaerfend: prime_pin verlangt GART, nouveau_bo_pin_locked nimmt bei pin_count>0 und mem_type VRAM den Fehlerpfad, setzt ret=-EBUSY, ruft aber TROTZDEM ttm_bo_pin() und gibt den Pin nie wieder her. Ein einziger dma-buf-Export-plus-Import eines Scanout-Puffers macht pin_count damit dauerhaft ungleich 0, und die Abbildung wird fuer die restliche Lebenszeit des BO nie mehr abgebaut.\n\nFolgen:\n(a) Sobald der BO ungepinnt ist, darf TTM ihn verschieben. Jede Verschiebung laeuft durch ttm_bo_handle_move_mem(), und das ruft als Erstes ttm_bo_unmap_virtual() -> ttm_mem_io_free() -> nouveaus io_mem_free -> nvif_object_unmap_handle(). Das BAR1-Fenster wird freigegeben und kann beim naechsten nvif_object_map_handle() einem anderen Puffer zugeteilt werden, waehrend nvbo->kmap.virtual weiter hineinzeigt.\n(b) Panic-Pfad: die neue Pruefung rettet nur, solange die Offsets verschieden sind. Wird der BO spaeter wieder als Scanout-fb gepinnt, ueberspringt nouveau_bo_pin_map() wegen \"if (!nvbo->kmap.virtual)\" das Neuabbilden und frischt kmap_bus_offset NICHT auf. Der BO liegt nach der Verschiebung woanders; liefert der BAR1-Allokator dieselbe, gerade erst freigegebene Fensteradresse zurueck, ist bus.offset == kmap_bus_offset, die Pruefung geht durch, und drm_panic schreibt ein volles Bild durch eine Abbildung des ALTEN Orts.\n(c) Unabhaengig vom Panic: nvbo->kmap ist geteilter Zustand. Der Pushbuf-Reloc-Pfad prueft ausschliesslich \"!nvbo->kmap.virtual\" und schreibt sonst ueber nouveau_bo_wr32() durch die vorgefundene Abbildung, ganz ohne Offset-Vergleich. Heute ist das unmoeglich, weil die einzige kmap auf einem User-BO innerhalb einer Reservierung entsteht und vergeht (nouveau_gem.c:696-704 und :434-437). Erst dieser Patch schafft eine langlebige kmap auf einem userspace-sichtbaren GEM-BO, und damit einen unprivilegiert erreichbaren Schreibzugriff durch ein freigegebenes BAR1-Fenster.\n(d) Dazu ein sicheres Leck pro betroffenem BO: nouveau_bo_del_ttm() hat kein ttm_bo_kunmap, die ioremap-Flaeche (Framebuffer-Groesse Kernel-VA) und das BAR1-Fenster gehen dauerhaft verloren. Auf der Zielmaschine sind BAR1 und VRAM je 256 MB.\n\nDie Aussage im Commit-Text, nach dem Patch stuenden \"ausnahmslos alle Schreiber von nvbo->kmap unter der BO-Reservierung\", stimmt zwar formal, traegt aber nicht: die Reservierung serialisiert nur, sie verhindert nicht, dass ein Schreiber eine fremde, laengst veraltete Abbildung vorfindet und benutzt.",
  "beleg": "Alles aus c21bb4193868 gelesen.\n\n1) Abbau haengt am globalen Zaehler (Entwurf, nouveau_bo_unpin_unmap):\n   \"nouveau_bo_unpin_locked(nvbo); if (!bo->pin_count) { ttm_bo_kunmap(&nvbo->kmap); nvbo->kmap_bus_offset = 0; }\"\n   drivers/gpu/drm/ttm/ttm_bo.c:624 \"void ttm_bo_pin(struct ttm_buffer_object *bo)\" und :643 \"void ttm_bo_unpin(...)\": ein einziger Zaehler bo->pin_count pro BO, ohne jeden Bezug zu Abbildungen.\n\n2) Fremdpinner Plane:\n   drivers/gpu/drm/nouveau/dispnv50/wndw.c:705-710\n     \"nv50_wndw_helper = { .prepare_fb = nv50_wndw_prepare_fb, .cleanup_fb = nv50_wndw_cleanup_fb, .atomic_check = nv50_wndw_atomic_check, };\"   (kein get_scanout_buffer)\n   wndw.c:712-718 nv50_wndw_primary_helper: identisches prepare_fb/cleanup_fb PLUS \".get_scanout_buffer = nv50_wndw_get_scanout_buffer\".\n   wndw.c:891-894 \"if (type == DRM_PLANE_TYPE_PRIMARY) drm_plane_helper_add(&wndw->plane, &nv50_wndw_primary_helper); else drm_plane_helper_add(&wndw->plane, &nv50_wndw_helper);\"\n   wndw.c:524-534 nv50_wndw_cleanup_fb ist fuer beide dieselbe Funktion, :534 \"nouveau_bo_unpin(nvbo);\".\n   Overlay und Primary teilen sich die Modifier: wndw.c:879-882 \"if (type == DRM_PLANE_TYPE_CURSOR) format_modifiers = nv50_cursor_format_modifiers; else format_modifiers = nouveau_display(dev)->format_modifiers;\", derselbe gekachelte BO ist also auf beiden zulaessig.\n\n3) Fremdpinner dma-buf:\n   drivers/gpu/drm/nouveau/nouveau_prime.c:87-97 \"int nouveau_gem_prime_pin(...) { ... ret = nouveau_bo_pin_locked(nvbo, NOUVEAU_GEM_DOMAIN_GART, false); if (ret) ret = -EINVAL; return ret; }\"\n   nouveau_prime.c:100-105 \"void nouveau_gem_prime_unpin(...) { nouveau_bo_unpin_locked(nvbo); }\"  (kein kmap-Bezug in beiden)\n   Pin-Leck bei Domain-Konflikt: nouveau_bo.c:562-583\n     \"if (nvbo->bo.pin_count) { bool error = evict; switch (bo->resource->mem_type) { case TTM_PL_VRAM: error |= !(domain & NOUVEAU_GEM_DOMAIN_VRAM); ... } if (error) { NV_ERROR(drm, \\\"bo %p pinned elsewhere: ...\\\"); ret = -EBUSY; } ttm_bo_pin(&nvbo->bo); goto out; }\"\n     Der Pin wird also auch im Fehlerfall genommen (:582) und vom Aufrufer nie zurueckgegeben.\n\n4) Freigabe des BAR1-Fensters bei jeder Verschiebung:\n   drivers/gpu/drm/ttm/ttm_bo.c:121 \"static int ttm_bo_handle_move_mem(...)\", :133 \"ttm_bo_unmap_virtual(bo);\" (erste Anweisung, vor jedem Move; Aufrufer :350, :396, :865, :1159)\n   ttm_bo.c:1052-1058 \"void ttm_bo_unmap_virtual(...) { drm_vma_node_unmap(...); ttm_mem_io_free(bdev, bo->resource); }\"\n   drivers/gpu/drm/ttm/ttm_bo_util.c:62-76 \"void ttm_mem_io_free(...) { ... if (bdev->funcs->io_mem_free) bdev->funcs->io_mem_free(bdev, mem); mem->bus.offset = 0; mem->bus.addr = NULL; }\"\n   drivers/gpu/drm/nouveau/nouveau_bo.c:1233-1246 nouveau_ttm_io_mem_free_locked, :1245 \"nvif_object_unmap_handle(&mem->mem.object);\" fuer TTM_PL_VRAM\n   Neuvergabe an einen anderen Puffer: nouveau_bo.c:1322-1333 \"ret = nvif_object_map_handle(&mem->mem.object, &args, argc, &handle, &length); ... reg->bus.offset = handle;\"\n   ttm_resource_free() selbst gibt io NICHT frei (ttm_resource.c:424-443, kein ttm_mem_io_free), die Freigabe kommt ausschliesslich ueber ttm_bo_unmap_virtual / ttm_bo_kunmap / ttm_bo_vunmap / ttm_bo_release (ttm_bo.c:274).\n\n5) Skip beim Wiederanpinnen, ohne kmap_bus_offset aufzufrischen (Entwurf, nouveau_bo_pin_map):\n   \"if (!nvbo->kmap.virtual) { ret = ttm_bo_kmap(bo, 0, PFN_UP(bo->base.size), &nvbo->kmap); ... nvbo->kmap_bus_offset = bo->resource->bus.offset; }\"\n   Beide Zuweisungen stehen INNERHALB des if.\n\n6) Ungeprueftes Weiterbenutzen von nvbo->kmap ausserhalb des Panic-Pfads:\n   drivers/gpu/drm/nouveau/nouveau_gem.c:696-704 \"if (!nvbo->kmap.virtual) { ret = ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(nvbo->bo.base.size), &nvbo->kmap); ... nvbo->validate_mapped = true; }\"  -> bei vorhandener Abbildung wird die fremde genommen und validate_mapped bleibt false\n   nouveau_gem.c:737 \"nouveau_bo_wr32(nvbo, r->reloc_bo_offset >> 2, data);\" (gleiches Muster nochmals :901-912)\n   nouveau_bo.c:828-839 nouveau_bo_wr32 nimmt ttm_kmap_obj_virtual(&nvbo->kmap, &is_iomem) und schreibt per iowrite32_native, ohne jede Gueltigkeitspruefung.\n   Heutiger Zustand (deshalb heute unmoeglich): nouveau_gem.c:434-437 \"if (unlikely(nvbo->validate_mapped)) { ttm_bo_kunmap(&nvbo->kmap); nvbo->validate_mapped = false; }\" vor ttm_bo_unreserve() in :441.\n\n7) Kein Abbau bei BO-Zerstoerung: nouveau_bo.c:137-155 nouveau_bo_del_ttm enthaelt \"WARN_ON(nvbo->bo.pin_count > 0); nouveau_bo_del_io_reserve_lru(bo); nv10_bo_put_tile_region(...)\", aber kein ttm_bo_kunmap.",
  "empfehlung": "1. Die Abbildung nicht an bo->pin_count koppeln. Der Zaehler gehoert nicht dem Anzeigecode: Overlay/Cursor-Planes und dma-buf zaehlen mit, bilden aber nicht ab.\n\n2. Besser: nvbo->kmap fuer diesen Zweck gar nicht benutzen. Die Panic-Abbildung in privaten Zustand legen, den nur dispnv50 sieht, zum Beispiel ein eigenes struct ttm_bo_kmap_obj (oder ein iosys_map per ttm_bo_vmap/ttm_bo_vunmap) im nv50_wndw_atom des Plane-States. Dann gilt:\n   - strikte Symmetrie prepare_fb pro neuem Plane-State, cleanup_fb pro altem Plane-State, also kein Leck und kein doppeltes Abbilden, ganz ohne Zaehler\n   - der Einwand des Entwurfs (\"nouveau_bo hat nur einen kmap-Slot ohne Refcount\") entfaellt vollstaendig, denn der Slot wird nicht mehr angefasst\n   - das gefaehrliche \"if (!nvbo->kmap.virtual) ueberspringen\" verschwindet, jeder prepare_fb erzeugt eine frische, zum aktuellen Ort passende Abbildung\n   - der Pushbuf-Pfad und nouveau_bo_wr32 koennen die Panic-Abbildung nicht mehr sehen und nicht mehr erben\n   Der Panic-Callback liest sie dann aus dem Plane-State (nv50_wndw_atom(plane->state)), analog zu ast, das die Abbildung ebenfalls treiberprivat haelt und im Callback nur durchreicht.\n\n3. Wenn nvbo->kmap doch beibehalten wird: einen EIGENEN Zaehler in struct nouveau_bo fuehren, der ausschliesslich von nouveau_bo_pin_map/nouveau_bo_unpin_unmap veraendert wird, und kmap_bus_offset bei JEDEM pin_map neu setzen, nicht nur beim Erzeugen. Zusaetzlich ttm_bo_kunmap in nouveau_bo_del_ttm ergaenzen, damit ein Restleck wenigstens nicht ueber die BO-Lebenszeit hinausreicht.\n\n4. Die Gueltigkeitspruefung ueber bus.offset beibehalten, aber im Commit-Text ehrlich als notwendige, nicht hinreichende Bedingung beschreiben. Sie erkennt nur den Fall bus.offset == 0 oder eine andere Adresse. Gibt der BAR1-Allokator dieselbe, gerade freigegebene Adresse zurueck, sagt der Vergleich nichts aus.\n\n5. Den Satz \"That also keeps every writer of nouveau_bo.kmap under the buffer reservation\" aus dem Commit-Text streichen oder umformulieren. Er suggeriert eine Sicherheit, die die Reservierung nicht liefert: sie serialisiert, aber sie garantiert nicht die Gueltigkeit der vorgefundenen Abbildung.\n\n6. Getrennt melden und einreichen, unabhaengig von drm_panic: nouveau_bo_pin_locked nimmt bei Domain-Konflikt den Pin und meldet gleichzeitig -EBUSY (nouveau_bo.c:577-582). Ueber nouveau_gem_prime_pin ist das aus dem Userspace ausloesbar und hinterlaesst einen dauerhaften Pin."
}