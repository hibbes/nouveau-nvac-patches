{
  "befunde": [
    {
      "frage": "(a) Unterschied ttm_bo_kmap/ttm_bo_kunmap gegen ttm_bo_vmap/ttm_bo_vunmap: Wer braucht eine Reservierung, wer liefert iosys_map, wer passt zu iosys_map_set_vaddr_iomem?",
      "antwort": "ttm_bo_vmap/ttm_bo_vunmap verlangen die Reservierung ausdruecklich und liefern ein fertig getaggtes iosys_map. ttm_bo_kmap/ttm_bo_kunmap verlangen sie nur stillschweigend und liefern ein struct ttm_bo_kmap_obj, dessen iomem-Eigenschaft der Aufrufer selbst per Bitmaske auswerten und per Cast in ein iosys_map ueberfuehren muss. Zweiter Unterschied: kmap kann einen Teilbereich abbilden, vmap immer nur den ganzen BO. iosys_map_set_vaddr_iomem gehoert damit zur kmap-Welt; bei vmap ist es ueberfluessig, weil ttm_bo_vmap es selbst aufruft.",
      "beleg": "Reservierung: ttm_bo_vmap hat dma_resv_assert_held(bo->base.resv) in drivers/gpu/drm/ttm/ttm_bo_util.c:503, ttm_bo_vunmap dieselbe Zusicherung in :568. In ttm_bo_kmap (:424-449) und ttm_bo_kunmap (:459-482) steht keine einzige Zusicherung. iosys_map: ttm_bo_vmap ruft iosys_map_set_vaddr_iomem(map, vaddr_iomem) in :528 und iosys_map_set_vaddr(map, vaddr) in :549 selbst auf; struct iosys_map mit union vaddr/vaddr_iomem plus bool is_iomem in include/linux/iosys-map.h:109-115. kmap dagegen liefert struct ttm_bo_kmap_obj {void *virtual; ...} (include/drm/ttm/ttm_bo.h:155-165) mit den Typen ttm_bo_map_iomap = 1 | TTM_BO_MAP_IOMEM_MASK (:159) und ttm_bo_map_premapped = 4 | TTM_BO_MAP_IOMEM_MASK (:162), Maske 0x80 in :140. Genau diese Handarbeit steht heute in drivers/gpu/drm/nouveau/dispnv50/wndw.c:676-679, samt Zwangs-Cast (void __iomem *)nvbo->kmap.virtual in :677. Teilbereich: ttm_bo_kmap nimmt start_page/num_pages und prueft sie in :434-437, ttm_bo_vmap bildet immer bo->base.size (:516, :523) beziehungsweise ttm->num_pages (:545) ab. Nebenbefund: ttm_bo_kmap kann fuer eine einzelne, cached Seite auf plain kmap() ausweichen (:359-368), was vmap nie tut.",
      "folge": "Fuer den Panic-Puffer ist ttm_bo_vmap die richtige Schnittstelle: ganzer BO, korrekt getaggtes iosys_map, kein Cast, und die Reservierungspflicht wird per lockdep erzwungen statt nur angenommen. Der Block wndw.c:676-679 faellt ersatzlos weg, sb->map[0] wird schlicht das gespeicherte iosys_map.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b) Darf ttm_bo_vmap mehrfach auf denselben BO? Gibt es einen Zaehler, oder ueberschreibt die zweite Abbildung die erste?",
      "antwort": "Weder noch. Es gibt keinen Zaehler, und die zweite Abbildung ueberschreibt die erste auch nicht: sie legt eine zweite, unabhaengige Abbildung an. Genau das ist die Falle. Fuer Systemspeicher ist das nur verschwenderisch, fuer iomem ist es kaputt, weil sich beide Abbildungen einen nicht gezaehlten Bus-Zustand in der ttm_resource teilen und der ERSTE ttm_bo_vunmap ihn fuer alle abraeumt.",
      "beleg": "Kein Zaehler in ttm_bo_vmap (drivers/gpu/drm/ttm/ttm_bo_util.c:498-553) und keiner in ttm_bo_vunmap (:564-581). Der geteilte Zustand: ttm_mem_io_reserve kehrt sofort zurueck, wenn mem->bus.offset oder mem->bus.addr schon gesetzt sind (:52-53), der zweite vmap holt also KEINEN zweiten Bus-Handle, ioremapt aber erneut (:515-523). ttm_mem_io_free (:62-76) hat keinen Zaehler: es prueft nur, ob bus.offset/bus.addr gesetzt sind (:68-69), ruft dann bedingungslos bdev->funcs->io_mem_free (:71-72) und nullt bus.offset/bus.addr (:74-75). ttm_bo_vunmap ruft es in :579. Bei Systemspeicher greift die Schutzpruefung :68-69, weil nouveau dort weder bus.offset noch bus.addr setzt, deshalb ist nur iomem betroffen. Was bei nouveau dabei zerstoert wird: nouveau_ttm_io_mem_free -> nouveau_ttm_io_mem_free_locked ruft fuer TTM_PL_VRAM bedingungslos nvif_object_unmap_handle (drivers/gpu/drm/nouveau/nouveau_bo.c:1244-1246, gesperrt in :1363-1371). Darunter nvkm_umem_unmap gibt das BAR1-Fenster per nvkm_vmm_put frei (drivers/gpu/drm/nouveau/nvkm/subdev/mmu/umem.c:63-83, :73), das nv50_mem_map zuvor per nvkm_vmm_get geholt hatte (drivers/gpu/drm/nouveau/nvkm/subdev/mmu/memnv50.c:56-62). Auch auf der nvkm-Ebene gibt es keinen Zaehler: nvkm_umem_map liefert -EEXIST, wenn schon abgebildet (umem.c:94-95). Und drm_gem_vmap_locked zaehlt ebenfalls nicht (drivers/gpu/drm/drm_gem.c:1413-1430), das frueher dort vorhandene vmap_use_count existiert nicht mehr.",
      "folge": "Ein privates ttm_bo_vmap pro Plane-State ist NICHT automatisch sicher. Sobald derselbe BO zweimal abgebildet ist, hebelt der erste vunmap das BAR1-Fenster des anderen aus: stille Verfaelschung, dieselbe Schwereklasse wie beim verworfenen Entwurf, nur eine Ebene tiefer. Die Abbildung selbst muss also pro BO geteilt und gezaehlt werden, das iosys_map im Plane-State darf nur die Quittung sein.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b, Fortsetzung) Ist der Doppelabbildungs-Fall ueberhaupt erreichbar, oder theoretisch?",
      "antwort": "Erreichbar, und zwar auf drei unabhaengigen Wegen. Der haeufigste ist fbcon: drm_fbdev_ttm bildet den Scanout-BO bei JEDEM Schadensbereich-Blit kurz ab und wieder ab. Jedes dieser Abbauen wuerde eine dauerhafte Panic-Abbildung desselben BO entkernen.",
      "beleg": "drm_fbdev_ttm_damage_blit ruft drm_client_buffer_vmap_local(buffer, &map) in drivers/gpu/drm/drm_fbdev_ttm.c:126 und drm_client_buffer_vunmap_local(buffer) in :133, innerhalb desselben Aufrufs. drm_client_buffer_vmap_local geht ueber drm_gem_vmap_locked (drivers/gpu/drm/drm_client.c, Funktion ab :290), das ohne Zaehler auf obj->funcs->vmap durchreicht (drivers/gpu/drm/drm_gem.c:1420). nouveau setzt .vmap = drm_gem_ttm_vmap (drivers/gpu/drm/nouveau/nouveau_gem.c:225), und drm_gem_ttm_vmap ist ein blosses ttm_bo_vmap (drivers/gpu/drm/drm_gem_ttm_helper.c:72), drm_gem_ttm_vunmap ein blosses ttm_bo_vunmap (:89). Weg zwei: zwei Primary-Planes koennen denselben Framebuffer tragen (Klon-Betrieb), beide durchlaufen nv50_wndw_prepare_fb (dispnv50/wndw.c:538) und nv50_wndw_cleanup_fb (:524). Weg drei: jeder dma-buf-Importeur ueber dieselbe .vmap-Zuweisung.",
      "folge": "Der Weg ueber fbcon ist der entscheidende. Er trifft auch die Loesung ueber privaten Zustand im nv50_wndw_atom, und er trifft sie im Normalbetrieb, nicht in einem Randfall. Wer die Abbildung dauerhaft halten will, MUSS also auch nouveaus .vmap/.vunmap durch einen gezaehlten Helfer ersetzen, sonst reisst der erste fbcon-Blit die Panic-Abbildung unter dem Panic-Pfad weg.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(b, Fortsetzung) Und die heutige Fassung mit nvbo->kmap: was passiert dort bei mehrfachem Aufruf?",
      "antwort": "Sie leckt. nouveau_bo_map ueberschreibt nvbo->kmap ohne vorheriges kunmap, die vorige ioremap-Adresse ist dann unerreichbar.",
      "beleg": "nouveau_bo_map (drivers/gpu/drm/nouveau/nouveau_bo.c:668-680) ruft ttm_bo_kmap(&nvbo->bo, 0, PFN_UP(...), &nvbo->kmap) in :676 ohne zu pruefen, ob nvbo->kmap.virtual schon gesetzt ist. ttm_bo_kmap setzt map->virtual = NULL in drivers/gpu/drm/ttm/ttm_bo_util.c:432 und bildet danach neu ab (:443 oder :447). Der Aufrufer im Panic-Pfad, dispnv50/wndw.c:671, ruft es ungeschuetzt. Ausloesbar mehrfach ueber debugfs: debugfs_trigger_write ruft draw_panic_plane (drivers/gpu/drm/drm_panic.c:973-984, :981), registriert in :992-999, allerdings nur unter CONFIG_DRM_PANIC_DEBUG (:1002). Zum Vergleich: amdgpu schuetzt genau dagegen mit if (!abo->kmap.virtual && ttm_bo_kmap(...)) in drivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1907-1908.",
      "folge": "Kein Argument gegen die Neufassung, aber ein Beleg mehr dafuer, dass nvbo->kmap kein tauglicher Ort fuer eine geteilte Abbildung ist. Der neue Weg raeumt das mit weg.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c) Was muss beim Abbauen unter Reservierung stehen? Ist es zulaessig, in cleanup_fb zu reservieren?",
      "antwort": "Unter Reservierung stehen muss der ttm_bo_vunmap selbst; das ist keine Konvention, sondern per dma_resv_assert_held erzwungen. Und ja, in cleanup_fb zu reservieren ist zulaessig: nv50_wndw_cleanup_fb tut es heute schon, ueber nouveau_bo_unpin. Beide Aufrufkontexte duerfen schlafen.",
      "beleg": "Zwang: dma_resv_assert_held in ttm_bo_vunmap, drivers/gpu/drm/ttm/ttm_bo_util.c:568. Der Abbau darf ausserdem nicht atomar sein, weil er ueber ttm_mem_io_free (:579) in nouveau_ttm_io_mem_free laeuft, das mutex_lock(&drm->ttm.io_reserve_mutex) nimmt (drivers/gpu/drm/nouveau/nouveau_bo.c:1368). Bereits vorhandene Reservierung in cleanup_fb: nv50_wndw_cleanup_fb ruft nouveau_bo_unpin (dispnv50/wndw.c:534), das ttm_bo_reserve macht (nouveau_bo.c:658) und ttm_bo_unreserve (:662). Kontext: drm_atomic_helper_cleanup_planes wird in nv50_disp_atomic_commit_tail aufgerufen (dispnv50/disp.c:2429), und commit_tail laeuft entweder als Arbeitseintrag auf system_dfl_wq (:2497 zusammen mit :2439-2444) oder direkt aus dem Ioctl-Faden (:2499). Beide schlafen duerfen.",
      "folge": "Der Abbau gehoert in dieselbe Reservierung wie das Entpinnen: einmal ttm_bo_reserve, dann ttm_bo_vunmap, dann nouveau_bo_unpin_locked (nouveau_bo.c:617-637, das seinerseits dma_resv_assert_held in :622 fuehrt), dann ttm_bo_unreserve. Spiegelbildlich in prepare_fb: eine Reservierung fuer nouveau_bo_pin_locked und den vmap. Das spart eine Reservierung und schliesst das Fenster zwischen Entpinnen und Abbauen.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c, Fortsetzung) cleanup_fb bekommt den ALTEN Plane-State. Traegt privater Zustand im Plane-State dann ueberhaupt?",
      "antwort": "Ja, die Paarung ist strikt eins zu eins, aber nur, weil sie in drei verschiedenen Pfaden zusammengesetzt wird. Auf dem Erfolgspfad kommt der alte State, auf beiden Fehlerpfaden der neue. In allen drei Faellen ist es genau der State, mit dem prepare_fb aufgerufen wurde.",
      "beleg": "Erfolgspfad: drm_atomic_helper_cleanup_planes iteriert for_each_old_plane_in_state und ruft funcs->cleanup_fb(plane, old_plane_state) (drivers/gpu/drm/drm_atomic_helper.c:3209-3214). Fehlerpfad innerhalb von prepare_planes: fail_prepare_fb ruft cleanup_fb(plane, new_plane_state) (:2876-2887). Abbruch nach erfolgreichem prepare: drm_atomic_helper_unprepare_planes ruft ebenfalls cleanup_fb(plane, new_plane_state) (:2917-2922). prepare_fb selbst wird mit new_plane_state gerufen (:2838). Die Zusicherung steht auch in der Schnittstellenbeschreibung: 'The helpers will call @cleanup_fb with matching arguments for every successful call to this hook' (include/drm/drm_modeset_helper_vtables.h:1219-1220). Ebendort der Hinweis, dass genau ein vmap eigentlich nach begin_fb_access/end_fb_access gehoert und nur ein pin nach prepare_fb (:1211-1217).",
      "folge": "Privater Zustand im nv50_wndw_atom traegt fuer die BUCHFUEHRUNG, wer die Abbildung angefordert hat. Er traegt NICHT fuer die Abbildung selbst, siehe (b). Die Doku-Stelle :1211-1217 ist ausserdem ein absehbarer Review-Einwand von Jocelyn Falempe beziehungsweise Thomas Zimmermann: warum nicht begin_fb_access. Antwort dafuer bereithalten: weil die Abbildung den Commit ueberleben muss, sonst ist sie im Panic-Moment nicht da.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(c, Fortsetzung) Welche Fallen lauern konkret in nv50_wndw beim Einbau eines neuen State-Feldes?",
      "antwort": "Drei. Erstens wird der Plane-State beim Duplizieren NICHT genullt, sondern feldweise kopiert, ein neues Feld ist also Muell. Zweitens verliert prepare_fb heute schon auf zwei Fehlerpfaden das Pin, ein neues Feld wuerde dort dieselbe Luecke erben. Drittens haengt get_scanout_buffer nur an Primary-Planes, prepare_fb/cleanup_fb aber an allen.",
      "beleg": "Erstens: nv50_wndw_atomic_duplicate_state benutzt kmalloc_obj (dispnv50/wndw.c:733), nicht kzalloc, und kopiert dann Feld fuer Feld (:736-744). Nur nv50_wndw_reset nullt (kzalloc_obj, :760). Ein neues iosys_map muss in duplicate_state ausdruecklich per iosys_map_clear geleert werden. Zweitens: nv50_wndw_prepare_fb pinnt in :554, entpinnt bei ctxdma-Fehler in :562, kehrt aber bei Fehler von drm_gem_plane_helper_prepare_fb (:580-581) und von nv50_head_atom_get_new (:587-588) OHNE Entpinnen zurueck. Der Helfer holt das nicht nach: fail_prepare_fb ueberspringt genau die fehlgeschlagene Plane (drm_atomic_helper.c:2880-2881, 'if (j >= i) continue;', wobei i der Index der fehlgeschlagenen Plane ist). Drittens: nouveau hat zwei Vtables, nv50_wndw_helper ohne get_scanout_buffer (wndw.c:704-709) und nv50_wndw_primary_helper mit (:711-717), zugewiesen nach Plane-Typ in :891-894. Beide teilen sich prepare_fb/cleanup_fb (:706-707 gegen :713-714).",
      "folge": "Der Patch muss iosys_map_clear in duplicate_state setzen, darf auf den Fehlerpfaden :580-581 und :587-588 nichts Neues liegenlassen (und sollte das dortige Pin-Leck gleich mit beheben, als eigener Vorpatch der Serie), und darf nur dann abbilden, wenn plane->helper_private->get_scanout_buffer gesetzt ist beziehungsweise plane->type == DRM_PLANE_TYPE_PRIMARY. Damit faellt der Overlay-Teil des zweiten Angriffs weg.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d) Gibt es Treiber, die ttm_bo_vmap fuer einen Scanout-Puffer dauerhaft halten?",
      "antwort": "Fuer einen Scanout-Puffer: keinen einzigen. Die Treiber, die im Panic-Pfad eine bestehende Abbildung durchreichen, halten sie ausserhalb von TTM (Dauer-ioremap der BAR oder ein fbdev-vma). Und amdgpu, der einzige weitere TTM-Treiber mit get_scanout_buffer, hat exakt denselben Fehler wie nouveau, sogar noch ohne Reservierung.",
      "beleg": "Vollstaendige Liste der Implementierungen: grep '\\.get_scanout_buffer' unter drivers/gpu/drm ergibt tidss, i915, nouveau, hyperv, amdgpu (viermal), shmobile, rcar-du, vesadrm/sysfb, bochs, virtio, ipuv3, ast. ast reicht ast_plane_vaddr durch (drivers/gpu/drm/ast/ast_mode.c:635, in :625-639), das ist kein TTM, sondern die dauerhaft ioremapte VRAM-Blende, dieselbe, die schon ast_handle_damage benutzt (:546). i915 reicht fuer den fbdev-Framebuffer die vorhandene Abbildung durch, intel_fbdev_get_map (drivers/gpu/drm/i915/display/intel_plane.c:1607-1608, Definition drivers/gpu/drm/i915/display/intel_fbdev.c:563-568). amdgpu: bevorzugt ganz ohne Abbildung, per amdgpu_display_set_pixel ueber mmMM_INDEX/mmMM_DATA (drivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1843-1862, gesetzt in :1903-1905), und nur als Rueckfallebene ttm_bo_kmap mitten im Panic-Pfad (:1907-1911) plus derselben Handarbeit mit TTM_BO_MAP_IOMEM_MASK (:1912-1915). Anders als nouveau nimmt amdgpu dort nicht einmal eine Reservierung, waehrend nouveau_bo_map wenigstens ttm_bo_reserve haelt (drivers/gpu/drm/nouveau/nouveau_bo.c:672). Alle ttm_bo_vmap-Aufrufer im Baum: drm_gem_ttm_helper, loongson, xe (Tests und lrc), qxl, vmwgfx, drm_gem_vram_helper. Keiner davon fuer einen Panic-Scanout.",
      "folge": "Es gibt kein Vorbild zum Abschreiben. Der Patch betritt Neuland und muss das im Anschreiben sagen. Positiv: Falempe kennt den amdgpu-Pfad, und der Hinweis, dass amdgpu dieselbe Verletzung ohne Reservierung begeht, ist ein starkes Argument dafuer, die Loesung sauber zu bauen statt zu kopieren.",
      "sicherheit": "belegt"
    },
    {
      "frage": "(d, Fortsetzung) Gibt es ein Vorbild fuer das Halten EINER geteilten ttm_bo_vmap-Abbildung ueber mehrere Nutzer?",
      "antwort": "Ja, drei voneinander unabhaengige, alle nach demselben Muster: EIN iosys_map pro BO, ein ganzzahliger Zaehler daneben, alles unter gehaltener Reservierung, ttm_bo_vmap nur beim Uebergang null auf eins, ttm_bo_vunmap nur beim Uebergang eins auf null.",
      "beleg": "drm_gem_vram_helper: gbo->vmap_use_count (include/drm/drm_gem_vram_helper.h:56-61), drm_gem_vram_vmap mit dma_resv_assert_held (drivers/gpu/drm/drm_gem_vram_helper.c:350), Zaehlerpruefung :352-353, ttm_bo_vmap nur wenn iosys_map_is_null (:360-364), ++count und *map = gbo->map (:367-368); drm_gem_vram_vunmap mit assert_held (:387), Warnung bei Zaehler null (:389-390), Gleichheitspruefung des uebergebenen map (:392-393), Rueckkehr bei --count > 0 (:395-396). Dazu der Abraeumhaken beim Verschieben: drm_gem_vram_bo_driver_move_notify warnt, wenn der Zaehler nicht null ist, und ruft dann ttm_bo_vunmap plus iosys_map_clear (:488-495 ff.). qxl: bo->map_count, qxl_bo_vmap_locked mit assert_held (drivers/gpu/drm/qxl/qxl_object.c:160), Zaehlerzweig :162-165, ttm_bo_vmap :167, map_count = 1 :172, *map = bo->map :181; qxl_bo_vunmap_locked :239-250 mit assert_held :241 und --map_count/Rueckkehr :245-247. loongson: lbo->vmap_count, drivers/gpu/drm/loongson/lsdc_gem.c:71-95 und :97-113. Wichtig: qxl und loongson stellen dafuer eigene .vmap/.vunmap-Zeiger, gehen also NICHT ueber drm_gem_ttm_vmap.",
      "folge": "Das ist die Bauanleitung. Uebertragen auf nouveau: ein Feld map plus ein Zaehler in struct nouveau_bo, ein gezaehltes nouveau_bo_vmap_locked/nouveau_bo_vunmap_locked, und .vmap/.vunmap in nouveau_gem.c:225 von drm_gem_ttm_vmap auf diese Helfer umgestellt. Nur so faengt der Zaehler auch den fbcon-Blit und die dma-buf-Importeure ein. Das iosys_map im nv50_wndw_atom bleibt daneben bestehen, als Quittung und fuer die Gleichheitspruefung beim Abbau, genau wie drm_gem_vram_vunmap sie in :392-393 macht.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Bleibt nach alledem ein Rest, den auch ein gezaehlter geteilter vmap nicht abdeckt?",
      "antwort": "Ja, einer, und der ist nicht durch Zaehlen zu erledigen: der -ENOSPC-Pfad von nouveau_ttm_io_mem_reserve entzieht einem fremden BO die IO-Abbildung, ohne irgendeinen Zaehler zu befragen. Ein per Userspace gemmapter Scanout-BO steht dafuer bereit, auch wenn er gepinnt ist.",
      "beleg": "nouveau_ttm_io_mem_reserve, out-Marke: bei ret == -ENOSPC nimmt es den ersten Eintrag von drm->ttm.io_reserve_lru, entfernt ihn, ruft drm_vma_node_unmap und nouveau_ttm_io_mem_free_locked und nullt bus.offset/bus.addr, dann goto retry (drivers/gpu/drm/nouveau/nouveau_bo.c:1342-1358). Auf die Liste kommt ein BO ueber nouveau_ttm_fault: nouveau_bo_add_io_reserve_lru nach ttm_bo_vm_fault_reserved (drivers/gpu/drm/nouveau/nouveau_gem.c:57-60). nouveau_bo_add_io_reserve_lru/del filtern nicht nach pin_count (nouveau_bo.c:763-781); entfernt wird nur beim Verschieben (nouveau_bo_move_ntfy, :1078) und beim Zerstoeren (:144). Ein Pin verhindert nur das Verschieben (drivers/gpu/drm/ttm/ttm_bo.c:843-845), nicht das Entziehen der IO-Abbildung. Ausserdem: nouveau_bo_move_ntfy raeumt nvbo->kmap NICHT ab, anders als drm_gem_vram_bo_driver_move_notify (drm_gem_vram_helper.c:488-495) es tut. Auf der Zielmaschine kann -ENOSPC nicht eintreten, weil BAR1 = VRAM = 256 MB, auf Karten mit VRAM > BAR schon.",
      "folge": "Der Patch geht upstream fuer alle Karten, nicht nur fuer den Macmini3,1. Zwei ehrliche Auswege: entweder den Scanout-BO beim Anfordern der Panic-Abbildung von der io_reserve_lru nehmen (nouveau_bo_del_io_reserve_lru) und beim Abbau nicht wieder eintragen, dann kann der Entzugspfad ihn nicht mehr treffen; oder den Zaehler auch im Entzugspfad pruefen und dort einen anderen BO waehlen. Ersteres ist kleiner und braucht keine neue Sperrreihenfolge. So oder so gehoert dieser Punkt ausdruecklich in die Patch-Beschreibung, sonst faellt er im Review als Luecke auf.",
      "sicherheit": "belegt"
    },
    {
      "frage": "Waere der einfachere Weg, statt einer Abbildung gar keine zu benutzen, also set_pixel wie amdgpu?",
      "antwort": "Fuer nv50 nicht belegbar in dieser Runde. amdgpus Weg beruht auf einem indirekten Registerfenster mmMM_INDEX/mmMM_DATA, das den ganzen VRAM ohne CPU-Abbildung erreicht. Ob nv50 ein aequivalentes Fenster hat, habe ich nicht geprueft; ich behaupte es deshalb weder in die eine noch in die andere Richtung.",
      "beleg": "amdgpu_display_set_pixel benutzt WREG32_NO_KIQ(mmMM_INDEX, ...) und WREG32_NO_KIQ(mmMM_DATA, color), Adressberechnung ueber amdgpu_res_first (drivers/gpu/drm/amd/amdgpu/amdgpu_display.c:1843-1862). nouveaus vorhandene set_pixel-Fassungen nv50_set_pixel und nv50_set_pixel_swizzle schreiben dagegen ueber iosys_map_wr(&sb->map[0], ...) (dispnv50/wndw.c:632, :647), setzen die Abbildung also voraus und loesen das Problem gerade nicht.",
      "folge": "Nicht als Hauptweg vorschlagen, aber im Anschreiben als geprueft-und-nicht-weiterverfolgt erwaehnen, damit der Review nicht danach fragt. Wer ihn doch verfolgen will, muss zuerst belegen, dass NV50 ein Registerfenster in den VRAM hat.",
      "sicherheit": "offen"
    }
  ],
  "fazit": "Antwort auf die Linsenfrage: ttm_bo_vmap/ttm_bo_vunmap ist die richtige Schnittstelle, ttm_bo_kmap nicht. vmap erzwingt die Reservierung per dma_resv_assert_held (ttm_bo_util.c:503, :568), liefert ein fertig getaggtes iosys_map (:528, :549) und macht den Handarbeitsblock samt Zwangs-Cast in wndw.c:676-679 ueberfluessig. kmap dagegen bildet Teilbereiche ab, hat keine Zusicherung und zwingt dem Aufrufer die iomem-Auswertung auf.\n\nDer entscheidende Befund geht aber ueber die gestellte Frage hinaus und trifft den jetzt bevorzugten Weg: ttm_bo_vmap hat KEINEN Zaehler, und die zweite Abbildung ueberschreibt die erste nicht, sondern legt eine zweite an. Beide teilen sich den Bus-Zustand in der ttm_resource, den ttm_mem_io_reserve nur einmal aufbaut (ttm_bo_util.c:52-53) und den ttm_mem_io_free ungezaehlt fuer alle abraeumt (:62-76, gerufen aus :579). Bei nouveau bedeutet dieses Abraeumen die Freigabe des BAR1-Fensters per nvif_object_unmap_handle (nouveau_bo.c:1244-1246) und nvkm_vmm_put (umem.c:73). Erreichbar ist das nicht theoretisch, sondern im Normalbetrieb: drm_fbdev_ttm_damage_blit bildet den Scanout-BO bei jedem Blit ab und wieder ab (drm_fbdev_ttm.c:126, :133) und laeuft ueber nouveaus .vmap = drm_gem_ttm_vmap (nouveau_gem.c:225) genau in dieses ttm_bo_vunmap. Ein privates ttm_bo_vmap im nv50_wndw_atom wuerde also vom ersten fbcon-Blit entkernt: stille Verfaelschung, dieselbe Schwereklasse wie beim verworfenen Entwurf.\n\nEmpfehlung fuer die Neufassung, aus drei in-tree-Vorbildern abgeleitet (drm_gem_vram_helper mit vmap_use_count, qxl mit map_count, loongson mit vmap_count): die Abbildung PRO BO teilen und zaehlen, nicht pro Plane-State anlegen. Also ein iosys_map plus Zaehler in struct nouveau_bo, gezaehlte Helfer unter gehaltener Reservierung, und .vmap/.vunmap in nouveau_gem.c:225 von drm_gem_ttm_vmap auf diese Helfer umstellen, damit fbcon und dma-buf mitgezaehlt werden. Das iosys_map im nv50_wndw_atom bleibt daneben als Quittung, gibt die strikte prepare/cleanup-Symmetrie (belegt ueber drm_atomic_helper.c:2838, :2886, :2921, :3213 und drm_modeset_helper_vtables.h:1219-1220) und erlaubt die Gleichheitspruefung beim Abbau wie in drm_gem_vram_vunmap. Abbilden nur dort, wo get_scanout_buffer haengt, also nur an Primary-Planes (wndw.c:711-717, :891-894), analog i915 (intel_plane.c:1640-1645).\n\nDrei Fallen fuer den Einbau: nv50_wndw_atomic_duplicate_state nullt nicht (kmalloc_obj, wndw.c:733, feldweise Kopie :736-744), ein neues Feld muss ausdruecklich per iosys_map_clear geleert werden. nv50_wndw_prepare_fb verliert auf zwei Fehlerpfaden (:580-581, :587-588) schon heute das Pin, weil der Helfer die fehlgeschlagene Plane ueberspringt (drm_atomic_helper.c:2880-2881); dort darf nichts Neues liegenbleiben. Und ein Rest bleibt, den kein Zaehler abdeckt: der -ENOSPC-Pfad von nouveau_ttm_io_mem_reserve (nouveau_bo.c:1342-1358) entzieht einem fremden, auch gepinnten BO die IO-Abbildung, sofern er ueber nouveau_gem.c:57-60 auf der io_reserve_lru steht. Auf dem Macmini3,1 kann das nicht feuern (BAR1 = VRAM = 256 MB), auf groesseren Karten schon; der saubere Ausweg ist, den Scanout-BO beim Anfordern der Panic-Abbildung von dieser Liste zu nehmen.\n\nKein Treiber im Baum haelt heute eine ttm_bo_vmap-Abbildung dauerhaft fuer einen Scanout-Puffer. ast reicht einen Dauer-ioremap durch (ast_mode.c:635), i915 ein vorhandenes fbdev-vma (intel_plane.c:1607-1608), und amdgpu, der einzige weitere TTM-Treiber mit get_scanout_buffer, begeht dieselbe Verletzung wie nouveau, sogar ohne Reservierung (amdgpu_display.c:1907-1915). Das gehoert ins Anschreiben.\n\nQuelltextbasis: /home/neo/linux-nouveau-patches, Commit c21bb4193868. Alle zitierten Dateien ausserhalb von nvkm/subdev/pci und nvkm/engine/device sind gegenueber der Basis unveraendert (per git diff --stat geprueft)."
}