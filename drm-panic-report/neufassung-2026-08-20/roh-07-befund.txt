{
  "beschreibung": "Der Patch ist in der vorliegenden Form nicht einreichbar und wuerde bei drm-misc bzw. nouveau zurueckgehen. Sechs Formmaengel, drei davon schwer.\n\n1. BLOCKER: Kein Signed-off-by. Die Datei 0001-commit-message.txt endet nach \"Cc: stable@vger.kernel.org # v6.13+\". Ohne DCO-Zeile ist der Patch nicht anwendbar. Die Behauptung \"checkpatch clean\" im Deliverable ist falsch: checkpatch wurde offenkundig nur auf den nackten Diff losgelassen. Auf der Fassung, die tatsaechlich versendet wuerde (Kopf + Changelog + \"---\" + Diff), meldet scripts/checkpatch.pl --strict \"ERROR: Missing Signed-off-by: line(s)\", total 1 errors, 2 warnings. Dazu kommt: das Deliverable ist ein `git diff` plus separate Textdatei, kein `git format-patch`-Ergebnis. Kein From:, kein Date:, kein Subject:, kein \"---\"-Trenner. `git am` kann das nicht lesen.\n\n2. HOCH: Fixes: und Cc: stable sind fuer den groessten Teil des Diffs schlicht falsch, und der Patch ist nicht aufgeteilt. Von 147 hinzugefuegten Zeilen liegen rund 100 in nouveau_bo.{c,h} und nouveau_gem.c und beheben einen zweiten, unabhaengigen Fehler: die fehlende Zaehlung der Kernel-CPU-Abbildung. Der ist nicht durch 1d26c846f3ff entstanden. `.vmap = drm_gem_ttm_vmap` steht seit 49a3f51dfeee (v5.11-rc1, November 2020) in nouveau_gem.c, also gut vier Jahre vor dem drm_panic-Commit. Ein einziges `Fixes: 1d26c846f3ff` schreibt den vmap-Fehler dem falschen Commit zu und weist das Stable-Team an, ein treiberweites Umbau-Stueck nach v6.13 zurueckzuportieren.\n   Der Stable-Vertrag wird dabei gleich dreifach gerissen (Documentation/process/stable-kernel-rules.rst:10-11): \"It cannot be bigger than 100 lines, with context\" (der Diff hat 335 Zeilen, 147 Additionen), \"It must be obviously correct and tested\" (kein Tested-by, und Abschnitt 4 des Deliverables sagt selbst \"Kein Vorbild zum Abschreiben ... dieser Patch betritt Neuland\" und bittet Falempe ums Testen).\n   Besonders unangenehm: Abschnitt 3 des Deliverables beschreibt die richtige Zweiteilung (1/2 Refcount in nouveau_bo/nouveau_gem, 2/2 der eigentliche Panic-Fix mit dem Fixes:) sauber und praezise, und liefert dann trotzdem einen Patch. Der Autor weiss also, dass die Form falsch ist. Wird geteilt, haengt 2/2 an 1/2, und genau dafuer gibt es die dokumentierte Schreibweise `Cc: <stable@vger.kernel.org> # <sha>: <subject>` (stable-kernel-rules.rst:84-98). Sie fehlt.\n\n3. MITTEL: Der Changelog behauptet mehr, als der Code hergibt. Die tragende Satz lautet: \"->prepare_fb() runs once per new plane state and ->cleanup_fb() once per old plane state, on the success path and on both failure paths, so the pairing is exact.\" Das ist nachweislich falsch. drm_atomic_helper.c:2876-2886 ueberspringt in fail_prepare_fb genau die Plane, deren prepare_fb fehlgeschlagen ist (`if (j >= i) continue;`). Fuer diese Plane laeuft cleanup_fb nicht. Der Entwurf ist nur deshalb heil, weil die Abbildung als letzte Anweisung von nv50_wndw_prepare_fb steht, hinter allen Fehlerausgaengen. Genau dieser entscheidende Umstand steht nirgends im Changelog, nur in der Begleitprosa. Ein Reviewer prueft diesen Satz zuerst, findet ihn widerlegt und misstraut danach dem Rest. Ausserdem ruft der Fehlerpfad cleanup_fb auf dem NEUEN State auf, nicht dem alten, was die Formulierung \"once per old plane state\" ebenfalls nicht abdeckt.\n\n4. MITTEL: Die drei ueberraschendsten Hunks kommen im Changelog gar nicht vor.\n   a) nouveau_bo_unmap() nimmt jetzt ttm_bo_reserve() und WARNt bei Fehlschlag. Das ist eine neue, blockierende Sperrnahme in einer Funktion, die aus dispnv04/crtc.c:1012, dispnv04/disp.c:92 und nouveau_bo_unpin_del() (nouveau_bo.c:412, benutzt von Fence-, Channel- und dmem-Abbau) gerufen wird. Kein Wort davon im Changelog.\n   b) .vmap/.vunmap wechseln von ttm_bo_vmap()/ttm_bo_vunmap() auf ttm_bo_kmap()/ttm_bo_kunmap(). Das sind nicht dieselben APIs: ttm_bo_kmap nimmt fuer einseitige, cached TT-Puffer den kmap()-Pfad (ttm_bo_util.c:359-366), ttm_bo_vmap immer vmap() (ttm_bo_util.c:544-548). Betroffen ist jeder dma-buf-Importeur eines nouveau-BO. Der Changelog sagt nur \"funnel every kernel CPU mapping through nvbo->kmap\" und verschweigt den API-Tausch.\n   c) In nouveau_gem_pushbuf_reloc_apply und im Suffix-Fixup wechselt die Wache von `!nvbo->kmap.virtual` auf `!nvbo->validate_mapped`. Semantische Aenderung: der Pushbuf nimmt jetzt eine eigene Referenz auf einen bereits fremd abgebildeten BO, wo er frueher die fremde Abbildung mitbenutzt und nie abgebaut hat. Weder Kommentar noch Changelog-Zeile.\n\n5. MITTEL: Fehlende Kommentare an den subtilen Stellen. `WARN_ON(asyw->panic_map);` steht nackt in nv50_wndw_atomic_destroy_state (wndw.c:743). Die Begruendung, warum es nicht feuern kann (nv50_wndw_reset nur ueber drm_mode_config_reset mit plane->state == NULL, drm_plane_cleanup erst nach drm_atomic_helper_shutdown), fuellt im Bericht einen Absatz und steht im Quelltext nirgends. Genauso fehlt am Ende von nv50_wndw_prepare_fb der Hinweis, dass die Plazierung hinter den beiden Fehlerausgaengen Absicht ist, was nach Punkt 3 der eigentliche Sicherheitsbeweis ist. Positiv anzumerken: der Kommentarstil `/* Text\\n * Text\\n */` passt zum Bestand in wndw.c (siehe den Blackwell-Kommentar bei :574-576), und die fehlende Leerzeile vor WARN_ON entspricht dem lokalen Stil der Datei (nv50_wndw_atomic_duplicate_state macht es genauso). Das sind also keine Maengel.\n\n6. NIEDRIG: Fehlende Herkunftszeilen. Es gibt einen oeffentlichen Bericht auf der Liste vom 15.08.2026 mit Message-ID 178681005908.3524476.7115000150741026287@gmail.com und eine Antwort von Falempe. Ein `Link:` oder `Closes:` darauf fehlt (submitting-patches.rst:545). Reported-by entfaellt zu Recht, weil selbst berichtet, aber die im Changelog behauptete Beobachtung \"On a Mac mini 3,1 ... this turns every panic into a second oops\" haengt ohne nachpruefbaren Verweis in der Luft, und ein Tested-by gibt es nicht. Ebenfalls niedrig: `# v6.13+` ist laut stable-kernel-rules.rst:121-122 ueberfluessig, weil das Stable-Team die Version aus dem Fixes: ableiten kann. In der Praxis verbreitet, aber es ist Rauschen.\n\nKorrekt geprueft und nicht zu beanstanden: Fixes-SHA 1d26c846f3ff ist echt, Titel stimmt, Autor Jocelyn Falempe, erstmals in v6.13 (git describe --contains: v6.13-rc1~122^2~2^2~17). Betreffzeile 58 Zeichen, laengste Body-Zeile 69 Zeichen. Die fuenf Dateien sind zwischen c21bb4193868 und HEAD (72a21ddc97d9) tatsaechlich unveraendert, `git diff --stat` gegen die fuenf Pfade liefert leer.",
  "beleg": "checkpatch auf der versandfertigen Fassung (Kopf + Changelog + \"---\" + Diff):\n  ERROR: Missing Signed-off-by: line(s)\n  total: 1 errors, 2 warnings, 0 checks, 294 lines checked\nDer Deliverable-Text behauptet \"0 errors, 2 warnings\".\n\n/home/neo/projects/nouveau-nvac-patches/drm-panic-report/0001-commit-message.txt, letzte zwei Zeilen:\n  Fixes: 1d26c846f3ff (\"drm/nouveau: Add drm_panic support for nv50+\")\n  Cc: stable@vger.kernel.org # v6.13+\nDanach nichts mehr. Kein Signed-off-by, kein Link, kein Tested-by.\n\nAlter des mit-behobenen zweiten Fehlers:\n  git log -S\"drm_gem_ttm_vmap\" -- drivers/gpu/drm/nouveau/nouveau_gem.c\n  -> 49a3f51dfeee \"drm/gem: Use struct dma_buf_map in GEM vmap ops and convert GEM backends\", 2020-11-03\n  git describe --contains 49a3f51dfeee -> v5.11-rc1~206^2~10^2~34\nDer Fixes: verweist dagegen auf einen Commit aus v6.13-rc1.\n\nUmfang gegen die Stable-Regel:\n  Documentation/process/stable-kernel-rules.rst:10  \"- It must be obviously correct and tested.\"\n  Documentation/process/stable-kernel-rules.rst:11  \"- It cannot be bigger than 100 lines, with context.\"\n  Der Diff: 147 Additionen, 17 Loeschungen, 335 Zeilen Datei.\n  Documentation/process/stable-kernel-rules.rst:121-122 \"Note, such tagging is unnecessary if the stable team can derive the appropriate versions from Fixes: tags.\"\n\nWiderlegung des Changelog-Satzes \"on the success path and on both failure paths, so the pairing is exact\":\n  drivers/gpu/drm/drm_atomic_helper.c:2876-2886\n    2876: fail_prepare_fb:\n    2877:   for_each_new_plane_in_state(state, plane, new_plane_state, j) {\n    2880:           if (j >= i)\n    2881:                   continue;\n    2885:           if (funcs->cleanup_fb)\n    2886:                   funcs->cleanup_fb(plane, new_plane_state);\n  Die fehlgeschlagene Plane wird uebersprungen, cleanup_fb laeuft fuer sie nicht. Zudem: new_plane_state, nicht old.\n\nAPI-Tausch bei ->vmap, im Changelog nicht erwaehnt:\n  drivers/gpu/drm/ttm/ttm_bo_util.c:359-366 (ttm_bo_kmap_ttm, kmap()-Pfad bei num_pages == 1 && ttm_cached)\n  drivers/gpu/drm/ttm/ttm_bo_util.c:544-548 (ttm_bo_vmap, immer vmap())\n\nNeue Sperrnahme in nouveau_bo_unmap(), im Changelog nicht erwaehnt, Aufrufer geprueft:\n  drivers/gpu/drm/nouveau/nouveau_bo.c:412  (nouveau_bo_unpin_del)\n  drivers/gpu/drm/nouveau/dispnv04/crtc.c:1012\n  drivers/gpu/drm/nouveau/dispnv04/disp.c:92\n  drivers/gpu/drm/nouveau/dispnv50/wndw.c:536 (neu)\n\nNackter WARN_ON ohne Begruendung:\n  drivers/gpu/drm/nouveau/dispnv50/wndw.c:742-744\n    struct nv50_wndw_atom *asyw = nv50_wndw_atom(state);\n    WARN_ON(asyw->panic_map);\n    __drm_atomic_helper_plane_destroy_state(&asyw->state);\n\nFehlender Verweis auf den Bericht:\n  Documentation/process/submitting-patches.rst:545 \"followed by a Closes: tag pointing to the report, unless the report is not available on the web.\"\n  Der Bericht ist im Web: Message-ID 178681005908.3524476.7115000150741026287@gmail.com, siehe SEND-LOG-2026-08-15.txt.\n\nWas stimmt:\n  git log -1 1d26c846f3ff -> \"drm/nouveau: Add drm_panic support for nv50+\", Jocelyn Falempe, 2024-10-22\n  git describe --contains 1d26c846f3ff -> v6.13-rc1~122^2~2^2~17, git tag --contains -> v6.13\n  git diff --stat c21bb4193868 HEAD -- <die fuenf Dateien> -> leer",
  "empfehlung": "1. Signed-off-by ergaenzen und den Patch als `git format-patch` erzeugen, nicht als `git diff` plus Textdatei. Danach checkpatch.pl --strict noch einmal auf die .patch-Datei laufen lassen, nicht auf den Diff. Die Aussage \"checkpatch clean\" ist erst dann belegt.\n\n2. In zwei Patches teilen, genau an der Naht, die Abschnitt 3 selbst beschreibt:\n   1/2 \"drm/nouveau: reference count the kernel CPU mapping of a buffer object\" mit nouveau_bo.{c,h} und nouveau_gem.c. Eigenes Fixes:, das auf die Herkunft des vmap-Fehlers zeigt, nicht auf den drm_panic-Commit. 49a3f51dfeee ist der Kandidat, aber vorher pruefen, ob nouveau vor ef350898ae22 ueberhaupt einen Nutzer hatte, der den Scanout-Puffer im Betrieb vmapt. Wenn nein, ist ef350898ae22 (\"drm/nouveau: Run DRM default client setup\", v6.13) der ehrlichere Fixes:, dann passt auch v6.13+.\n   2/2 \"drm/nouveau/kms: don't map the scanout buffer in panic context\" mit atom.h und wndw.c. Nur hier gehoert Fixes: 1d26c846f3ff hin.\n   Cc: stable nur auf 2/2 setzen und in 1/2 die Abhaengigkeit ausschreiben, Form nach stable-kernel-rules.rst:84-98. Wenn 1/2 dem Stable-Team zu gross erscheint, das lieber im Anschreiben ansprechen, als beide Fixes in einen 335-Zeilen-Patch zu pressen.\n\n3. Den falschen Satz im Changelog ersetzen. Statt \"on the success path and on both failure paths, so the pairing is exact\" schreiben, was tatsaechlich gilt: drm_atomic_helper_prepare_planes() ueberspringt in fail_prepare_fb die Plane, deren prepare_fb fehlgeschlagen ist, deshalb steht die Abbildung als letzte Anweisung von nv50_wndw_prepare_fb, hinter allen Fehlerausgaengen. Denselben Satz zusaetzlich als Kommentar an die Fundstelle im Code.\n\n4. Drei Absaetze in den Changelog von 1/2 nachtragen: nouveau_bo_unmap() nimmt jetzt die Reservierung, ->vmap/->vunmap wechseln von ttm_bo_vmap auf ttm_bo_kmap (mit dem Hinweis auf den kmap()-Pfad bei einseitigen Puffern), und die Pushbuf-Wache wechselt von kmap.virtual auf validate_mapped. Alle drei sind aus dem Diff nicht selbsterklaerend.\n\n5. WARN_ON in nv50_wndw_atomic_destroy_state kommentieren, ein Zweizeiler genuegt: reset laeuft nur ueber drm_mode_config_reset() mit plane->state == NULL, drm_plane_cleanup() erst nach drm_atomic_helper_shutdown().\n\n6. Link: auf den lore-Faden setzen (Form: https://lore.kernel.org/all/178681005908.3524476.7115000150741026287@gmail.com/). Wenn Falempe testet, sein Tested-by abwarten und aufnehmen, bevor Cc: stable stehen bleibt. `# v6.13+` kann weg, das Fixes: reicht.\n\n7. Betreff des KMS-Teils auf `drm/nouveau/kms:` umstellen, das ist der Prefix, den die juengsten dispnv50-Commits benutzen (72a21ddc97d9, 456c4f5ff0c8). Fuer den BO-Teil bleibt `drm/nouveau:` richtig.",
  "schwere": "blocker",
  "treffer": true
}