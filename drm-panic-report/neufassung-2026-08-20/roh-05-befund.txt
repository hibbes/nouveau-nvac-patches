{
  "treffer": true,
  "schwere": "hoch",
  "beschreibung": "nouveau_bo_map_locked() prueft im Refcount-Schnellweg nicht, ob die vorhandene Abbildung noch zur aktuellen ttm_resource des Puffers gehoert. Bei kmap_count > 0 gibt es kommentarlos 0 zurueck (nouveau_bo.c:690-693). nv50_wndw_prepare_fb() wertet nur diesen Rueckgabewert aus und setzt daraufhin asyw->panic_map = true (wndw.c:605-606). get_scanout_buffer() glaubt der Marke und reicht nvbo->kmap.virtual an drm_panic weiter (wndw.c:692-698), das durch diesen Zeiger SCHREIBT (nv50_set_pixel, wndw.c:632 und :647).\n\nDer Patch stellt genau dazu selbst eine Bedingung auf, erzwingt sie aber nirgends: \"A mapping that outlives the reservation additionally requires the buffer object to be pinned, so that ttm_bo_kunmap() sees the same ttm_resource that ttm_bo_kmap() saw\" (nouveau_bo.c:678-681). Und derselbe Patch schafft die erste API, die diese Bedingung verletzen kann, naemlich nouveau_gem_object_vmap() als .vmap (nouveau_gem.c:222-238, verdrahtet :250). Ueber drm_gem_dmabuf_vmap() (drm_prime.c:727-732) beziehungsweise drm_gem_vmap() (drm_gem.c:1459-1468) ueberlebt eine so genommene Abbildung die Reservierung, und sie ist von der Anheftung voellig unabhaengig: drm_gem_map_attach() heftet an (drm_prime.c:614-622, ueber nouveau_gem_prime_pin nach GART, nouveau_prime.c:88-98), drm_gem_map_detach() loest die Anheftung wieder (drm_prime.c:637-651), waehrend vmap/vunmap davon unberuehrt bleiben. Nach \"attach, vmap, detach\" hat der Puffer kmap_count == 1 bei pin_count == 0.\n\nDamit ist die Voraussetzung des Patches gebrochen, und die Folge ist deterministisch, nicht bloss eine Rennbedingung: nv50_wndw_prepare_fb() heftet mit contig = true an (wndw.c:559). Fuer Tesla und neuer setzt nouveau_bo_pin_locked() bei !nvbo->contig dann force und evict (nouveau_bo.c:553-560) und erzwingt bei pin_count == 0 erst eine Auslagerung nach GART und danach eine Validierung nach VRAM (:585-596). Es sind also zwei garantierte Umzuege, ohne jeden Speicherdruck.\n\nBeim Umzug raeumt nouveau nichts ab. nouveau_bo_move_ntfy() nimmt den Puffer nur aus der io_reserve_lru und fasst nvbo->kmap nicht an (nouveau_bo.c:1135-1169), anders als drm_gem_vram_bo_driver_move_notify(), das der Commit-Text ausdruecklich als Vorbild nennt. TTM raeumt derweil die Unterlagen der alten Abbildung ab: ttm_bo_handle_move_mem() ruft ttm_bo_unmap_virtual() (ttm_bo.c:121, :133), das ttm_mem_io_free() ausfuehrt (ttm_bo.c:1052-1058), das seinerseits nouveau_ttm_io_mem_free_locked() ruft, welches fuer VRAM bedingungslos nvif_object_unmap_handle() ausfuehrt und das BAR1-Fenster freigibt (nouveau_bo.c:1240-1246). Beim Ziel VRAM ist es noch schaerfer: ttm_bo_move_to_ghost() setzt wegen dst_use_tt == false bo->ttm = NULL und haengt die alten TT-Seiten an ein Geister-BO, das sofort freigegeben wird (ttm_bo_util.c:614-636). Die alte Abbildung war in diesem Fall ein vmap/kmap genau dieser Seiten.\n\nDanach ruft nv50_wndw_prepare_fb() nouveau_bo_map() (wndw.c:605). nouveau_bo_map_locked() sieht kmap_count == 1, zaehlt hoch und meldet Erfolg (nouveau_bo.c:690-693). panic_map wird gesetzt. nvbo->kmap.virtual zeigt aber weiter auf die Abbildung von vor dem Umzug. Da bo_kmap_type dann nicht TTM_BO_MAP_IOMEM_MASK traegt, nimmt get_scanout_buffer() den Systemspeicher-Zweig (wndw.c:697-698) und uebergibt drm_panic einen Kernel-Zeiger auf freigegebene Seiten. Ein Panic schreibt dort Pixel hinein. Das ist ein Schreibzugriff nach Freigabe, in genau dem Zeiger, den der Patch neu einfuehrt.\n\nZwei Nebenwirkungen derselben Ursache: erstens laeuft der spaetere ttm_bo_kunmap() dann auf map->bo->resource, also auf der NEUEN Ressource (ttm_bo_util.c:478), und gibt deren io-Abbildung frei, waehrend er die alte virtuelle Adresse abbildet. Zweitens ist die alte Fassung an dieser Stelle nicht betroffen gewesen: sie rief in get_scanout_buffer() ttm_bo_kmap() unbedingt neu (der gemeldete Fehler), und nouveau_bo_map() bildete unbedingt neu ab, ueberschrieb also nvbo->kmap. Das war ein Leck, aber nie ein veralteter Zeiger. Der Zaehler macht aus dem Leck eine stille Verfaelschung, also dasselbe Muster, an dem der erste Entwurf gestorben ist, nur eine Ebene tiefer.\n\nEhrliche Einschraenkung zur Ausloesbarkeit: ich habe im Baum keinen dma-buf-Verbraucher gefunden, der ohne Anheftung abbildet. Alle gefundenen Aufrufer von dma_buf_vmap_unlocked() gehen ueber eine Anheftung (videobuf2-dma-contig.c:104, videobuf2-vmalloc.c:374, videobuf2-dma-sg.c:314, drm_gem_dma_helper.c:590), und drm_gem_fb_vmap() wird nur von Schattenebenen-Helfern und vkms benutzt. Die Reihenfolge \"attach, vmap, detach, vunmap\" ist von der dma-buf-Schnittstelle aber ausdruecklich erlaubt und wird von nichts verhindert, und derselbe Zustand entsteht auch dann, wenn ein Verbraucher die Anheftung vor dem vunmap loest. Der Bruch liegt also nicht in einer exotischen Rennbedingung, sondern darin, dass der Patch eine Bedingung formuliert und die einzige Stelle, die sie pruefen koennte, sie nicht prueft.",
  "beleg": "drivers/gpu/drm/nouveau/nouveau_bo.c:678-681 (unerzwungene Bedingung im Kommentar), :690-693 (Schnellweg ohne Ressourcenpruefung), :553-560 und :585-596 (contig erzwingt Auslagerung plus Validierung, also zwei Umzuege), :1135-1169 (nouveau_bo_move_ntfy laesst nvbo->kmap stehen), :1240-1246 (nvif_object_unmap_handle bedingungslos); drivers/gpu/drm/nouveau/nouveau_gem.c:222-238 und :250 (neues .vmap ohne Anheftungspflicht); drivers/gpu/drm/nouveau/nouveau_prime.c:88-98 (Anheftung nach GART); drivers/gpu/drm/nouveau/dispnv50/wndw.c:559 (Anheftung contig=true), :605-606 (Abbildung und panic_map), :692-698 (drm_panic bekommt nvbo->kmap.virtual), :632 und :647 (Schreibzugriff durch den Zeiger); drivers/gpu/drm/drm_prime.c:614-622 (attach heftet an), :637-651 (detach loest), :727-732 (vmap unabhaengig davon); drivers/gpu/drm/drm_gem.c:1459-1468 (Abbildung ueberlebt die Reservierung); drivers/gpu/drm/ttm/ttm_bo.c:121, :133 (handle_move_mem ruft unmap_virtual), :1052-1058; drivers/gpu/drm/ttm/ttm_bo_util.c:62-76 (io_mem_free), :478 (kunmap nimmt die aktuelle Ressource), :614-636 (move_to_ghost setzt bo->ttm = NULL, alte Seiten werden freigegeben); Gegenprobe drivers/gpu/drm/drm_gem_vram_helper.c:488-495 (das im Commit-Text zitierte Vorbild raeumt beim Umzug ab).",
  "empfehlung": "Drei Massnahmen, die erste ist Pflicht:\n\n1. Die Abbildung an die Ressource binden. In struct nouveau_bo neben kmap und kmap_count die Identitaet der abgebildeten Ressource merken (Zeiger auf die ttm_resource oder das Paar mem_type/start) und in nouveau_bo_map_locked() im Schnellweg pruefen. Bei Abweichung nicht stillschweigend hochzaehlen, sondern WARN_ON und -EINVAL zurueckgeben. Das kostet vier Zeilen und macht aus der stillen Verfaelschung ein lautes, diagnostizierbares Scheitern, in dessen Folge panic_map einfach false bleibt und es nur keinen Panic-Schirm gibt.\n\n2. Beim Umzug abraeumen, wie das im Commit-Text zitierte Vorbild. In nouveau_bo_move_ntfy() (nouveau_bo.c:1135-1169) analog zu drm_gem_vram_bo_driver_move_notify() (drm_gem_vram_helper.c:488-495) ein WARN_ON(nvbo->kmap_count) setzen, ttm_bo_kunmap(&nvbo->kmap) ausfuehren und kmap_count auf 0 stellen. Der Aufruf sitzt bereits unter der Reservierung (nouveau_bo.c:1232), also ist das sperrtechnisch umsonst zu haben. Damit ueberlebt keine Abbildung einen Umzug mehr, und die Bedingung aus dem Kommentar wird zu einer erzwungenen Invariante.\n\n3. Den Panic-Pfad selbst enger fassen. In nv50_wndw_prepare_fb() panic_map nur setzen, wenn die Abbildung wirklich zum angehefteten VRAM-Puffer gehoert, also zusaetzlich nvbo->bo.resource->mem_type == TTM_PL_VRAM und nvbo->kmap.bo_kmap_type & TTM_BO_MAP_IOMEM_MASK pruefen. Konsequent dazu den Systemspeicher-Zweig in get_scanout_buffer() (wndw.c:697-698) streichen oder durch return -ENODEV ersetzen: fuer einen mit NOUVEAU_GEM_DOMAIN_VRAM angehefteten Scanout-Puffer ist er unerreichbar, und genau ueber ihn laeuft im geschilderten Ablauf der Schreibzugriff nach Freigabe.\n\nErgaenzend fuer das Anschreiben an Jocelyn Falempe: der Punkt \"Verschieben eines abgebildeten, nicht gepinnten BO\" steht im Abschnitt \"Was der Patch nicht loest\" als vorbestehend und weder verschlimmert noch behoben. Das trifft so nicht zu. Vorbestehend war eine private, veraltete Abbildung des jeweiligen Aufrufers. Neu ist, dass diese Abbildung durch nvbo->kmap plus Zaehler zum geteilten Alleinstueck wird, das jeder spaetere Abbilder ungeprueft erbt und das der Panic-Pfad als gueltig ausgewiesen bekommt. Der Satz sollte umgeschrieben und die Massnahmen 1 und 2 in den Patch aufgenommen werden."
}