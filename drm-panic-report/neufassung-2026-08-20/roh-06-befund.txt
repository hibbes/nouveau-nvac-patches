{
  "treffer": false,
  "schwere": "keiner",
  "beschreibung": "Ich habe im Panic-Pfad nach der Aenderung keine verbotene Operation mehr gefunden. Kein Sperrerwerb, keine Speicherzuteilung, kein Schlafen, kein Warten.\n\nVollstaendige Aufstellung dessen, was `nv50_wndw_get_scanout_buffer()` (wndw.c:665-720) nach dem Patch noch tut, jeweils bis in die Tiefe verfolgt:\n\n1. `nouveau_drm(plane->dev)`: `return dev->dev_private;` (nouveau_drv.h:317-320). Reine Zeigerlesung.\n2. `plane->state` und `plane->state->fb`: Lesen. Von drm_panic.h:106-107 ausdruecklich als sicher zugesichert, weil `drm_atomic_helper_swap_state()` den Zeigertausch unter `drm_panic_lock()` vornimmt (drm_atomic_helper.c:3352-3362, Klammerung durch drm_panic_lock/unlock verifiziert).\n3. `nv50_wndw_atom(plane->state)`: `container_of` (atom.h:186). Keine Laufzeitoperation. Der Typ ist garantiert, weil sowohl `nv50_wndw_atomic_duplicate_state()` (wndw.c:749) als auch `nv50_wndw_reset()` (wndw.c:781) ausschliesslich `struct nv50_wndw_atom` anlegen und es keinen dritten Erzeuger gibt.\n4. `nouveau_gem_object(fb->obj[0])`: `container_of`.\n5. `nvbo->comp`, `fb->format->num_planes`, `armw->panic_map`, `nvbo->kmap.bo_kmap_type`, `nvbo->kmap.virtual`: reine Feldlesungen.\n6. `iosys_map_set_vaddr_iomem()` (iosys-map.h:196-201) und `iosys_map_set_vaddr()`: zwei Zuweisungen, kein Aufruf.\n7. `nouveau_framebuffer_get_layout()` (nouveau_display.c:164-178): liest `fb->flags`, `fb->modifier` beziehungsweise `nvbo->mode`/`nvbo->kind`, ruft nur `nouveau_decode_mod()`, das ebenfalls nur rechnet. Keine Sperre, kein nvif-Aufruf.\n8. `nouveau_get_gob_height()` und `nouveau_get_gobs_in_block()` (dispnv50/tile.h:27, :48): reine Arithmetik in `static inline`.\n9. Die spaeter aufgerufenen `nv50_set_pixel()`/`nv50_set_pixel_swizzle()` (wndw.c:650-663, :620-648) schreiben ueber `iosys_map_wr()`, das fuer iomem in `writel()` endet (iosys-map.h:411-418, :366-370). Keine Sperre.\n\nDer eigentliche Beweis, dass die durchgereichte Abbildung im Panic-Moment noch gueltig ist, traegt ebenfalls. Ich habe versucht, ihn zu brechen, und es gelingt nicht:\n\na) Solange `kmap_count > 0`, kann kein zweiter Eigentuemer `ttm_bo_kunmap()` ausloesen, denn `nouveau_bo_unmap_locked()` (nouveau_bo.c:710-722) baut nur beim Uebergang auf 0 ab, und `nouveau_bo_map_locked()` (nouveau_bo.c:684-707) betritt `ttm_bo_kmap()` nur bei Zaehlerstand 0. `nvbo->kmap` ist damit zwischen `prepare_fb` und `cleanup_fb` unveraenderlich, genau wie drm_panic.h:114-120 es verlangt.\nb) Nach dem Patch gibt es im Treiber genau ein `ttm_bo_kmap()` und ein `ttm_bo_kunmap()`, beide in diesen zwei Helfern. Per grep ueber `drivers/gpu/drm/nouveau/` verifiziert, kein `ttm_bo_vmap`, `ttm_bo_vunmap`, `drm_gem_ttm_vmap`, `drm_gem_ttm_vunmap` mehr.\nc) Die einzige Stelle, die das BAR1-Fenster hinter der Abbildung ohne `ttm_bo_kunmap()` einziehen koennte, ist der -ENOSPC-Zweig von `nouveau_ttm_io_mem_reserve()` (nouveau_bo.c:1412-1424). Der nimmt nur Opfer von `drm->ttm.io_reserve_lru`, und dort landet ein abgebildeter Puffer nicht mehr: `nouveau_bo_map_locked()` entfernt ihn vorher (nouveau_bo.c:698), und `nouveau_bo_add_io_reserve_lru()` haengt ihn nicht wieder ein (nouveau_bo.c:832-838). Ich habe die Aufrufer gesucht: es gibt genau einen, `nouveau_ttm_fault()` (nouveau_gem.c:62), und der haelt ueber `ttm_bo_vm_reserve()` (nouveau_gem.c:51) dieselbe Reservierung, unter der `kmap_count` geschrieben wird. Es gibt also kein Fenster, in dem der Puffer mit `kmap_count > 0` auf der Liste steht.\nd) Alle uebrigen Wege zu `ttm_mem_io_free()` habe ich abgesucht (`grep` ueber drivers/gpu/drm/ttm/): ttm_bo.c:274 (Freigabe, verlangt Refcount 0 und `WARN_ON_ONCE(bo->pin_count)` bei ttm_bo.c:255), ttm_bo.c:1057 via `ttm_bo_unmap_virtual()`, das nur aus `ttm_bo_handle_move_mem()` (ttm_bo.c:133), dem Swapout (ttm_bo.c:1175) und `ttm_bo_shrink()` (ttm_bo_util.c:1110) kommt, ttm_resource.c:894/:920 aus dem memcpy-Move. Alle setzen einen Umzug voraus, der fuer den in `nv50_wndw_prepare_fb()` (wndw.c:554) gepinnten Puffer ausgeschlossen ist (\"Moving of pinned BOs is forbidden\").\ne) Die Symmetrie von `panic_map` habe ich gegen die Helfer geprueft: `drm_atomic_helper_prepare_planes()` ruft `prepare_fb` fuer jeden neuen Plane-State (drm_atomic_helper.c:2831-2839) und im Fehlerfall `cleanup_fb` fuer die bereits erfolgreichen (:2874-2883), `drm_atomic_helper_unprepare_planes()` fuer alle neuen (:2911-2916), `drm_atomic_helper_cleanup_planes()` fuer alle alten (:3209-3214). Kein Zweig laesst eines von beiden aus. nouveau selbst haelt sich daran (dispnv50/disp.c:2466, :2503, :2429).\nf) Ich habe geprueft, ob der Zustand, den `cleanup_fb` gerade abbaut, jemals `plane->state` sein kann, denn dort gibt es tatsaechlich ein Fenster zwischen `nouveau_bo_unmap()` und `armw->panic_map = false` (wndw.c:535-538). Er kann es nicht: `cleanup_planes` laeuft ueber die alten States, nachdem `swap_state` `plane->state` auf den neuen gesetzt hat, und `unprepare_planes` laeuft ueber die neuen States, solange `plane->state` noch der alte ist. In beiden Faellen ist der bearbeitete State nie der aktive.\ng) Ein asynchroner Plane-Update, der `plane->state->fb` hinter dem Ruecken von prepare_fb austauschen wuerde, existiert in nouveau nicht: `grep atomic_async_update\\|atomic_async_check` ueber drivers/gpu/drm/nouveau/ liefert nichts.\nh) Nebenlaeufigkeit ist ohnehin ausgeschlossen, weil `panic()` die uebrigen CPUs vor dem Dump anhaelt (kernel/panic.c:565/:567 gegenueber kernel/panic.c:685).\n\nDer Vollstaendigkeit halber, weil ich es beim Absuchen der Aufrufer gefunden habe und es nicht unterschlagen will, auch wenn es ausserhalb meiner Spur liegt: die Abbildung wird jetzt pro Plane-State genommen und gegeben, also bei einem doppelt gepufferten Compositor pro Seitenwechsel. Jeder Wechsel loest damit `nouveau_bo_map_locked()` mit Zaehlerstand 0 aus, also `nvif_object_map_handle()` (nouveau_bo.c:1391) samt `nvkm_vmm_get()` fuer das ganze Objekt und ein `ioremap_wc()` ueber die volle Puffergroesse (ttm_bo_util.c:326), und beim Aufraeumen des alten States `iounmap()` plus `nvif_object_unmap_handle()`. Das ist kein Panic-Verstoss, gehoert aber in die Spur Regressionsflaeche.",
  "beleg": "Panic-Eintritt und Sperre: drivers/gpu/drm/drm_panic.c:927 (`drm_panic_trylock`), :930 (Callback-Aufruf), :948 (unlock); include/drm/drm_panic.h:133-134 (raw_spin_trylock_irqsave), :110-120 (Zusicherung fuer prepare_fb/cleanup_fb-Zustand).\nCallback nach dem Patch: drivers/gpu/drm/nouveau/dispnv50/wndw.c:665-720, insbesondere :677 (`armw = nv50_wndw_atom(plane->state)`), :688-693 (`if (!armw->panic_map) return -ENODEV;` statt des frueheren `nouveau_bo_map()`), :695-698 (nur iosys_map-Zuweisungen), :705 (`nouveau_framebuffer_get_layout`).\nTiefe der verbleibenden Aufrufe: drivers/gpu/drm/nouveau/nouveau_drv.h:317-320; drivers/gpu/drm/nouveau/dispnv50/atom.h:186; drivers/gpu/drm/nouveau/nouveau_display.c:164-178; drivers/gpu/drm/nouveau/dispnv50/tile.h:21-23, :27, :48; include/linux/iosys-map.h:196-201, :366-370, :411-418.\nRefcount und seine Schranken: drivers/gpu/drm/nouveau/nouveau_bo.c:684-707 (map_locked, del_io_reserve_lru bei :698, ttm_bo_kmap bei :701, Zaehler bei :690-692 und :705), :710-722 (unmap_locked, Abbau nur bei Uebergang auf 0 bei :718-721), :832-838 (LRU-Sperre).\nWiderruf des BAR1-Fensters: drivers/gpu/drm/nouveau/nouveau_bo.c:1412-1424 (-ENOSPC-Opferwahl, nur aus io_reserve_lru), :1300-1319 (io_mem_free_locked); drivers/gpu/drm/ttm/ttm_bo_util.c:49-53 (frueher Rueckweg von ttm_mem_io_reserve), :62-76 (ttm_mem_io_free ohne Zaehler), :478 (aus ttm_bo_kunmap); Aufrufer von ttm_bo_unmap_virtual: drivers/gpu/drm/ttm/ttm_bo.c:133, :1052-1058, :1175, drivers/gpu/drm/ttm/ttm_bo_util.c:1110; Pin-Schranke drivers/gpu/drm/ttm/ttm_bo.c:255.\nEinziger LRU-Wiedereinhaenger: drivers/gpu/drm/nouveau/nouveau_gem.c:44-68, Reservierung bei :51, add bei :62.\nSymmetrie der Helfer: drivers/gpu/drm/drm_atomic_helper.c:2831-2839, :2874-2883, :2911-2916, :3209-3214, :3352-3362 (Tausch unter drm_panic_lock); nouveau: drivers/gpu/drm/nouveau/dispnv50/disp.c:2429, :2466, :2503.\nCPU-Stopp vor Dump: kernel/panic.c:565, :567, :685.\nKein Async-Update in nouveau: grep auf `atomic_async_update|atomic_async_check` in drivers/gpu/drm/nouveau/ ohne Treffer.",
  "empfehlung": "Aus Sicht der Panic-Kontext-Regeln ist nichts zu aendern. Der Callback nimmt keine Sperre, teilt keinen Speicher zu, schlaeft nicht und wartet nicht, und die Abbildung, die er durchreicht, ist gegen jeden Weg abgesichert, der sie unter ihm wegziehen koennte.\n\nZwei Punkte wuerde ich trotzdem festhalten, beide klein:\n\n1. Der Fehlerwert `-ENODEV` fuer den Fall ohne Abbildung ist unauffaellig, aber `-EOPNOTSUPP` oder `-EAGAIN` traefe die Lage besser, denn das Geraet existiert ja. `draw_panic_plane()` (drm_panic.c:931-932) wertet den Wert nur auf ungleich null aus, also ist es reine Lesbarkeit fuer den Review.\n\n2. Die im Patch selbst schon benannte Luecke beim Laufzeit-Suspend (nouveau_display.c:770-779) ist die einzige verbliebene Stelle, an der der Panic-Pfad auf eine tote BAR schreiben kann. Sie ist ausserhalb dieser Spur, aber sie ist der einzige Rest, bei dem der Panic-Kontext noch etwas anfasst, das ihm nicht gehoert. Ein `panic_map`-Leeren unter `drm_panic_lock()` in `nouveau_display_suspend()` waere der Ort, und der Hinweis darauf gehoert ins Anschreiben an Falempe, damit er es beim Test mitnimmt.\n\nFuer die Weiterarbeit: die Spur Regressionsflaeche sollte sich die Abbildung pro Seitenwechsel ansehen (nvif_object_map_handle plus ioremap_wc der vollen Puffergroesse bei jedem Flip, auf GSP-Chips ein Firmware-RPC pro Bild). Das ist der Einwand, mit dem ein Reviewer diesen Patch am ehesten anhalten wird, und er liegt nicht im Panic-Kontext."
}