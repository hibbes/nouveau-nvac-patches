# nvac-boot-s3 Implementation Plan (07.09.2026)

> Inline-Ausfuehrung in der Session vom 07.09.2026. Schritte mit Checkbox.

**Goal:** Ein OpenRC-Dienst faehrt nach jedem Boot einmal einen deep-S3-Zyklus, bevor der erste Display-Zyklus des Nutzers kommt (Spec: `docs/specs/2026-09-07-nvac-boot-s3-design.md`).

**Architecture:** Bash-Skript mit Umgebungs-Overrides fuer alle Pfade/Schwellen und PATH-aufgeloesten Kommandos, dazu ein OpenRC-Init-Skript nach dem Muster `boot-blackscreen`. Selbsttest als eigenes Skript mit Kommando-Shims in einem Tempverzeichnis.

**Tech Stack:** bash, OpenRC, util-linux rtcwake, flock.

## Global Constraints
- Kommando-Reihenfolge exakt wie `recover()` in `/usr/local/bin/nvac-s3-unwedge-watchdog.py`, plus Stop/Start von `nvac-s3-unwedge` aussen herum.
- rtcwake 30 s; Watchdog-Wert 40 s unangetastet.
- Marker vor dem rtcwake schreiben.
- Telegram nur bei Anomalie.

---

### Task 1: Selbsttest (rot)
**Files:** Create `tools/nvac-boot-s3/nvac-boot-s3-selftest.sh`
- [x] Shims fuer pgrep, rc-service, rtcwake, dmesg, ping, ip, klaus-send schreiben (loggen argv nach calls.log)
- [x] Faelle: disabled, done-marker, prior-S3, stale-boot, no-compositor, happy-path-sequence, waits-for-min-uptime, rtcwake-failure-restarts-watchdogs, nic-bounce, evo-timeouts-alert, force, marker-before-rtcwake, lock
- [x] Ausfuehren: `bash tools/nvac-boot-s3/nvac-boot-s3-selftest.sh tools/nvac-boot-s3/nvac-boot-s3.sh` -> muss FAIL melden (Skript fehlt)

### Task 2: Skript (gruen)
**Files:** Create `tools/nvac-boot-s3/nvac-boot-s3.sh`
- [x] Minimal so schreiben, dass alle Faelle gruen sind
- [x] Selbsttest erneut: ALL PASS
- [x] `bash -n`, shellcheck falls vorhanden

### Task 3: Init-Skript + Installation
**Files:** Create `tools/nvac-boot-s3/nvac-boot-s3.initd`; install nach `/usr/local/bin/nvac-boot-s3.sh` und `/etc/init.d/nvac-boot-s3`
- [x] `rc-update add nvac-boot-s3 default`, `rc-service nvac-boot-s3 start` (im laufenden Boot: Skip "not a fresh boot" oder "S3 already happened" erwartet, Logzeile pruefen)

### Task 4: Live-Test (Freigabe Marek 07.09.)
- [x] `sudo NBS_FORCE=1 /usr/local/bin/nvac-boot-s3.sh`; danach: RESULT-Zeile, beide Waechter laufen (`rc-service ... status`), Netz ok, EVO ohne Timeouts, Session lebt
- [x] Kernel-Log-Auszug des Zyklus sichern (Suspend-Eintritt-Dauer)

### Task 5: Repo + Memory
- [x] Kopien + Spec + Plan committen und pushen
- [x] Memory: Projektdatei und MEMORY.md-Zeile, KG-Beobachtung

---
Erledigt 07.09.2026 18:11: alle Tasks, Live-Lauf rc=0 dt=32,7 s, Commit f76726e.
